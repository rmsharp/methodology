# Release History — narrated per-version semantics

One narrated entry per shipped version, oldest first. This is the extracted body of
[`CLAUDE.md` §Versioning](../CLAUDE.md#versioning), which still owns the current-version line and
the boundary rule; only the per-version list lives here.

**Why it is a separate file.** `CLAUDE.md` is auto-loaded into every session's context, so its size
is a recurring cost paid by every session whether or not any release history is needed. This list
was **86.7%** of that file (45,874 of 52,909 bytes). Extracting it is `starter-kit/BOOTSTRAP.md`
Step 5 applied to this repository's own memory file — the always-loaded file stays scannable, the
accumulated record moves to a sibling read on demand, and the pointer is a plain Markdown link,
never an `@`-import.

**Boundary vs. [`CHANGELOG.md`](../CHANGELOG.md) — unchanged by the move (v3.1).** This file owns
*released-version semantics*: one narrated entry per shipped version. The root `CHANGELOG.md` action
ledger owns the *per-action operational timeline*, including non-release work (housekeeping,
doc-only PRs, adopter coordination, backlog grooming). Where the two overlap — a release — the
ledger carries a **one-line pointer**, never a re-narration, so the two ledgers cannot diverge.
Existing ledger entries cite the `CLAUDE.md#versioning` anchor, which still resolves.

**Adding a release:** append its entry to the end of this list, and update the "Current version"
line in `CLAUDE.md` §Versioning and `README.md` §What's New in lockstep.

---

Key additions by version:

- **v1.0:** Initial 9 principles, 6 phases, 4 workstreams
- **v1.1:** Protocol erosion detection, ghost session prevention, minimum handoff requirements, 4 new failure modes (#14-17)
- **v1.2:** Planning session discipline, evidence-based inventory, phase boundaries
- **v2.0:** Methodology Dashboard — portfolio health scanner with dual-mode detection, health/risk scoring, methodology compliance checking, live HTML dashboard with auto-refresh, color-coded terminal output, starter-kit inclusion
- **v2.1:** CHANGELOG.md and ROADMAP.md templates, three-file task tracking split, migration guide for monolithic backlogs, dashboard compliance updates, 4 new failure modes (#20-23), Artifact Integrity safeguards, build equivalent step in BOOTSTRAP, documentation project adaptations
- **v2.2:** `bin/sync` + `bin/status` distribution tooling (dual-mode commit/ignore, dual-source local/github), CLAUDE_TEMPLATE.md customization seam, BOOTSTRAP.md rewrite, drift-safety guard on sync
- **v2.3:** Combined release covering two contributions. (1) **Research Documentation workstream** — adapts the methodology for research papers, technical reports, dissertations, and regulatory analyses; source-corpus management, claim-source audit pattern, 19 documented anti-patterns, toolchain adaptation table (Quarto/LaTeX/Sphinx/Pandoc/AsciiDoc/Markdown), Audit Mode for fresh-eyes review of existing repositories. (2) **SESSION_RUNNER content release** from rad-con audit (issue #6, #7) — new Phase 3E Runtime Smoke Test (renumber 3E→3F, 3F→3G), new FM #24 (build-passes-ship-it, appended — FMs 1–23 unchanged), Phase 1B "structural control" framing, Planning Sessions grep anecdote, 5 new Learnings rows.
- **v2.4:** Multi-session campaigns promoted to first-class layer — new vocabulary and document home for campaign templates that extend a workstream. New section in `ITERATIVE_METHODOLOGY.md` (§Multi-Session Campaigns); new orientation step in `SESSION_RUNNER.md` (Phase 1 multi-session campaign check); new `workstreams/TEMPLATE_CAMPAIGN.md`. No principle, phase, gate, or workstream changes. Realized examples: `workstreams/RESEARCH_EXHAUSTIVE_VERIFICATION_CAMPAIGN.md` (extends Research Documentation) and `workstreams/INHERITED_CODEBASE_FAMILIARIZATION_CAMPAIGN.md` (extends Audit).
- **v2.5:** Render-dependency completeness discipline (resolves upstream issue #12, drafted from `docs/planning/font-discipline-proposal.md`). Universal principle in `starter-kit/SAFEGUARDS.md` (new "Verify Render-Dependency Completeness" sub-section under "Verify the Build Equivalent"): build success is not asset-use success. Two-tier check: post-render is a **hard rule** (part of build-equivalent — e.g., `pdffonts` confirms all configured font faces embedded); pre-render / setup is a **soft prompt** that fires at Phase 0 when render-dep config changes (e.g., `fc-list`, `kpsewhich`). Concrete commands in `workstreams/RESEARCH_DOCUMENTATION_WORKSTREAM.md` — new Render Verification bullet, new Verification Checklist item, new "Render-Dep Check" column on the toolchain matrix (per-toolchain static + post-render commands for Quarto/LaTeX/Sphinx/Pandoc/AsciiDoc), and new anti-pattern #20 "Silent render-dependency fallback" (derived from the joy/ SBL BibLit case: 47 sessions of italic markup silently rendered as upright Latin glyphs). `workstreams/RESEARCH_EXHAUSTIVE_VERIFICATION_CAMPAIGN.md` Render Verification section gains a render-dep completeness line so the campaign inherits the discipline. No principle, phase, gate, or workstream changes; no FM renumbering.
- **v2.6:** Skill-recommendation convention — new content layer alongside phases, principles, workstreams, and campaigns. Principle: *methodology recommends; methodology does not reimplement.* New `starter-kit/RECOMMENDED_SKILLS.md` (canonical index with per-skill known-good SHAs for external skills) + new `ITERATIVE_METHODOLOGY.md` §Recommended Skills (principle paragraph + index pointer). Conceptual content distilled from the 2026-05-02 Pocock-skills audit lands as new FM #25 (horizontal slicing, appended — 1–24 unchanged), `SESSION_RUNNER.md` Phase 3F and `ITERATIVE_METHODOLOGY.md` Phase 6 step 8 gain an explicit "remove debug instrumentation before commit" gate (cites `/diagnose`), new `starter-kit/CONTEXT_TEMPLATE.md` + Phase 2 read-step (cites `/grill-with-docs`), new Issue Lifecycle section in `DEVELOPMENT_WORKSTREAM.md` (cites `/triage`), new Debugging Sessions session type (cites `/diagnose`), new Refactor Heuristics in `ARCHITECTURE_WORKSTREAM.md` (cites Ousterhout + `/improve-codebase-architecture`), new optional Phase 2.5 Pre-Create Grill (cites `/grill-me`), pre-commit + git-guardrails paragraphs in `BOOTSTRAP.md` Step 10 (cite `/setup-pre-commit` + `/git-guardrails-claude-code`). Existing-content refactors: `SESSION_RUNNER.md` Phase 3E body shortened to cite `/verify` + `/run` (intent preserved); `AUDIT_WORKSTREAM.md` gains a Light Recommended-Skills callout citing `/code-review`, `/review`, `/security-review`. Audit doc lands at `docs/audits/2026-05-02-mattpocock-skills-evaluation.md` with a drop/keep/refactor Implementation Status header. Phase 2 step renumbered 1→2..7→8; `HOW_TO_USE.md` Phase 2 cross-references all cite by name, never by step number, so the renumber is safe. No principle, phase, gate, or workstream changes. Adopters who never use the recommended skills continue to operate the methodology as written.
- **v2.6.1:** Docs-only dot release bundling four external contributions (rmsharp PRs #18/#23/#25/#27). (1) **Skill-citation discipline completed** (#18): `/init` actually cited in `BOOTSTRAP.md` Step 4 with a skip path for existing CLAUDE.md projects; `/fewer-permission-prompts` dropped from the index (no natural citation site); new "A skill is not a phase" principle paragraph in `ITERATIVE_METHODOLOGY.md` §Recommended Skills (a skill that pulls a session across a hard gate is FM #2 wearing a tool costume — gates bind regardless of loaded tools); new "Skills not recommended (and why)" 10-row subsection in `RECOMMENDED_SKILLS.md` surfacing the v2.6 audit's deliberate omissions plus hedged post-audit entries (`/handoff`, `/prototype`). (2) **Learnings-routing fix** (#25, closes #24): `SESSION_RUNNER.md` Phase 3C dual-audience recomposition — adopters record learnings in `CLAUDE.md` Adaptations, canonical repo appends to the seed table; false "table starts empty" caption corrected; `HOW_TO_USE.md` retargeted. (3) **CLAUDE.md size budget** (#27, closes #26): `BOOTSTRAP.md` Step 5 size-budget rule + extract-to-`PROJECT_LEARNINGS.md` overflow valve + plain-link (not `@`-import) pointer warning. (4) `tools/__pycache__/` gitignored (#23). No principle, phase, gate, workstream, or FM changes; FM count stays 25.
- **v2.6.2:** Docs-only dot release resolving issue #22 (Pocock skill enablement gap, rmsharp investigation) under **strategy C (Reactive)**: enablement is per-request via adopter `CLAUDE.md` adaptations or `/grill-with-docs` lazy-creation — no methodology-shipped templates (preserves "methodology recommends; methodology does not reimplement"). (1) **Bounding axis:** new "Phases spanned (compression risk)" column on `RECOMMENDED_SKILLS.md`'s external-skills table — verbatim-verified per-skill phase spans at the pinned SHAs (`/grill-with-docs` + `/improve-codebase-architecture` = HIGH, Phase 2→3→5; `/diagnose` = medium by design); reading-guidance paragraph (plan the stop point before invoking high-compression skills; "A skill is not a phase" cross-ref); in-file forward-compat note to restate in vertical-slice vocabulary if #20/#21 land. (2) **`/triage` contradiction:** resolved by footnote — citation stands, adopters establish the label mapping themselves per `DEVELOPMENT_WORKSTREAM.md` §Issue Lifecycle instead of running the declined `/setup-matt-pocock-skills`. Issues #20/#21 remain open and unprejudged. No principle, phase, gate, workstream, or FM changes.
- **v2.7:** Vertical-slice model — the first change to the unit of "1 and done" (issues #20/#21, adopted by operator decision 2026-06-12). One deliverable MAY be a **verified vertical slice** (one capability end to end) under four mandatory gates: (a) layer set pre-declared in a plan-mode contract before any code (prior-session approval satisfies; implementing session re-verifies at Orient), (b) checkpoint commit at every layer boundary (5-file cap unchanged, clarified *per-commit* in `SAFEGUARDS.md`), (c) full build/test matrix + exhaustive grep at *each* boundary, (d) per-surface faithful verification ("all tests ran" is not automatically faithful). **Evidence-gated, not self-certified** — missing per-boundary artifacts revert the session to horizontal scope at the last clean checkpoint. Recoverability, not verifiability, is the ceiling on slice size; seven boundary types stay non-collapsible (irreversible migrations, operator-approval gates, plan ↔ implementation, cross-toolchain cutovers, live-device verification, unfaithful-suite surfaces, recoverability ceiling). New `SESSION_RUNNER.md` §Vertical Slice Sessions; Principle 9 extended; guardrails: new **FM #26** "mega-session masquerading as a vertical slice" (appended — 1–25 unchanged) + degradation row, FM #17 anti-erosion clause (the allowance ADDS a gate, removes no step), FM #18 discriminator + degradation-row exception for pre-declared slices, 6-phase-model mirroring note. Touch-ups: `RECOMMENDED_SKILLS.md` phase-map guidance restated in vertical-slice vocabulary (v2.6.2's forward-compat note discharged); §Recommended Skills "compress two sessions into one" sentence reworked. Strict horizontal "1 and done" remains the default and the fallback; no phase, gate, or workstream changes.
- **v2.7.1:** Docs-only naming-consistency dot release resolving issue #15. The two decimal-suffixed phase labels in `ITERATIVE_METHODOLOGY.md` — `Phase 1.5: Claim the Session` and `Phase 2.5: Pre-Create Grill` — are renamed to **`Phase 1B`** and **`Phase 2B`**, adopting the letter-suffix convention `SESSION_RUNNER.md` already uses for `1B: Claim the Session`. The grep inventory for issue #15 (which scoped only Phase 2.5) surfaced `Phase 1.5` as a second decimal outlier *and* a latent cross-doc split (`SESSION_RUNNER.md`/`CLAUDE.md` said `1B` while `ITERATIVE_METHODOLOGY.md`/campaigns/README said `1.5`); standardizing both closes it. Live/operative sites updated: `ITERATIVE_METHODOLOGY.md` (both headers, each gaining a one-line "letter suffix = inserted bridge step; does not subdivide a numbered phase; no Phase 1A/2A" clarifier, + 2 body refs), `SESSION_RUNNER.md` task-mapping row, `RECOMMENDED_SKILLS.md` (2 sites), and the 3 campaign/workstream files (`INHERITED_CODEBASE_FAMILIARIZATION_CAMPAIGN.md`, `TEMPLATE_CAMPAIGN.md`, `RESEARCH_EXHAUSTIVE_VERIFICATION_CAMPAIGN.md`, 2 refs each). Dated changelog/record prose is left verbatim by design — earlier `README.md` "What's New" entries (v1.1 names `Phase 1.5`, v2.6 names `Phase 2.5`), the `CLAUDE.md` v2.6 note, and the frozen `docs/planning`/`docs/audits` records keep their original tokens; this note is the bridge. The "9 principles, 6 phases, 12 quality gates" framing is unchanged — letter suffixes mark inserted bridge steps and never increment the phase count. No principle, phase, gate, workstream, or FM changes.
- **v2.7.2:** Docs-only dot release bundling three external contributions (rmsharp PRs #29/#30/#31), each resolving one PR-14-review issue. (1) **Agent-level memory generalization** (#29, closes #16): `starter-kit/CONTEXT_TEMPLATE.md`'s "CONTEXT.md vs auto-memory" guidance is renamed "CONTEXT.md vs **agent-level memory**" — the second persistence layer is now named by capability (persistence scoped to the agent across all projects) rather than by Claude Code's brand term, with a one-line gloss that agents implement it under different names (Claude Code's auto-memory, Cursor's memories, Cody's preferences); keeps the methodology agent-independent. (2) **Workstream skill-citation convention** (#30, closes #17): `starter-kit/RECOMMENDED_SKILLS.md` §How this index is used gains a **count-threshold rule** — one recommended skill is cited inline at the point of recommendation (e.g. `DEVELOPMENT_WORKSTREAM.md` §Issue Lifecycle → `/triage`; `ARCHITECTURE_WORKSTREAM.md` §Refactor Heuristics → `/improve-codebase-architecture`), while two or more skills for one workstream are promoted to a dedicated `## Recommended Skills` section with a purpose→skill table (e.g. `AUDIT_WORKSTREAM.md`'s `/code-review`/`/review`/`/security-review`). A single-row table is heavier than the inline citation it would replace, so the section earns its overhead only at ≥2 skills; `workstreams/TEMPLATE_WORKSTREAM.md` gains a matching optional "Recommended Skills" scaffold (delete-if-one, keep-if-≥2). (3) **Cross-reference completeness Learning** (#31, closes #19): the v2.6 self-review heuristic is formalized as **Learning #7** in the `SESSION_RUNNER.md` Learnings table (appended — 1–6 unchanged) plus a Phase 3F pre-commit hook — when a change adds a citation/index/glossary/cross-reference or modifies a numbered set (FMs, principles, phases, anti-patterns, learnings), `git diff` shows only the present-side edit, so grep each cited destination and each nearby set-size claim before committing. No principle, phase, gate, workstream, or FM changes; FM count stays 26.
- **v2.8:** First non-docs change since v2.2's distribution tooling — `bin/sync`/`bin/status` expanded from the legacy 3-file scope to the full Option-B corpus (21 files), resolving the **B1** campaign (issue #32) across four external contributions (rmsharp PRs #33/#34/#35/#37). (1) **Link reconciliation + `bin/check-links`** (#33, Phase 2): distributed files' cross-boundary relative links rewritten to resolve in the **adopter** layout (the "link-topology paradox" — a relative link can't be verbatim-correct in both the canonical and adopter layouts unless source/target are siblings in both; extends the convention `SESSION_RUNNER.md` already followed), validated by a new `bin/check-links` against a simulated adopter tree (wired into `bin/tests.sh`); the two `docs/audits/…` refs in `RECOMMENDED_SKILLS.md` move to absolute GitHub URLs. Accepted tradeoff: cross-boundary links in `ITERATIVE_METHODOLOGY.md`/`workstreams/*` resolve in an installed project but 404 when the canonical repo is browsed on GitHub. (2) **Expanded `bin/sync` over a shared manifest** (#34, Phase 3): the flat `SYNC_FILES` tuple becomes a `(src, dest, disposition)` manifest in new **`bin/_manifest.py`** — the single source of truth (plan Decision 5) imported by `sync`, `status`, and `check-links`; `sync` honors dest subdirs and adds a **`seed`** disposition (write-if-absent, adopter-owned thereafter, never clobbered even with `--force`); only `tracked` dests are gitignored/tracking-checked so seed files stay committable. (3) **Per-file `bin/status`** (#35, Phase 4, closes #32): one row per distributed file (`Project · File · Disposition · Status`) over the shared manifest, so drift in any framework doc — not just the legacy three — is reported; SEED files report `present`/`absent` and an absent seed is never flagged as drift; also fixes a latent `--source=github` 404 on framework/`workstreams/` paths. (4) **Checker hygiene** (#37, Phase 5, closes #36): `bin/check-links --tree` no longer fabricates adopter-owned placeholder files in the tree it validates — they are treated as allowed-absent rather than created (the Phase 3 `--tree` mode had written up to 5 empty files into the validated tree), guarded by a Test 14 regression assertion. Test suite grew 28 → 51. No principle, phase, gate, workstream, or FM changes; FM count stays 26.
- **v2.9:** Combined release covering two additive contributions that merged together (rmsharp PRs #38/#39). **(1) Hands-on tutorial track** (#38) — a new `docs/tutorials/` learning layer parallel to the document hierarchy: a progressive, *do-one-thing-per-lesson* curriculum with a checkpoint at every step, run against the learner's own repo or a bundled sample. Ships the **core trio** — `T1_setup.md` (install + first bootstrap), `T2_first_session.md` (+ `T2_worked_transcript.md`, a full annotated session), `T5_cautionary.md` (where the guardrails catch you) — plus the series index `docs/tutorials/README.md`, a `TUTORIAL_TEMPLATE.md`, and a bundled `sample-project/` (a todo-CLI with one feature *deliberately absent* so a tutorial session implements it end to end; `pytest` 7/7 green). Discoverability pointers added to `README.md` (new **Tutorials** section + repo-tree entry) and `starter-kit/BOOTSTRAP.md`. The tutorials are a **canonical-only learning aid — deliberately NOT in the `bin/_manifest.py` distribution corpus** (adopters do not receive them via `bin/sync`); the distributed-file pointer to them uses an absolute GitHub URL per the v2.8 link-topology convention. Roadmap T3/T4/T6/T7/T8 remain. **(2) Reasoning-tier generalization** (#39) — promotes the "match effort to stakes" guidance from a Research-Documentation-only paragraph to a **cross-cutting convention**. New brand-neutral core section `ITERATIVE_METHODOLOGY.md` §**Matching Reasoning Effort to Stakes** (a maxim + prose: reasoning tier ∝ *blast radius × irreversibility × compounding cost* — the same risk lens Principle 3 and Principle 9 already use; the inverse also holds — cheap/reversible/mechanical work warrants a lighter default). The agent-specific *how* lives in the recommendation layer: new `starter-kit/RECOMMENDED_SKILLS.md` §**Reasoning Effort** (a per-work-type example-settings table whose brand tokens are framed illustrative). Per-domain tier notes cite the rule into the heavy workstreams (`ARCHITECTURE`/`AUDIT`/`DEVELOPMENT`/`RESEARCH_DOCUMENTATION`), the lighter `DESIGN`, a planning-session deepest-mode line + checklist item in `SESSION_RUNNER.md`, both campaigns (`INHERITED_CODEBASE_FAMILIARIZATION`/`RESEARCH_EXHAUSTIVE_VERIFICATION`), and a `TEMPLATE_WORKSTREAM` scaffold; the Research-Documentation paragraph is recomposed in place to cite the new owner rather than re-derive it (cite-don't-restate). An anti-erosion clause (cross-referencing FM #17) states a higher tier sharpens a phase but never licenses skipping orientation, the stub, close-out, any hard gate, or widening a session beyond its one declared deliverable. No principle, phase, gate, workstream, or FM changes; FM count stays 26.
- **v3.0:** Relicensed under the **MIT License** (issue #43, rmsharp). The bespoke source-available `LICENSE` is replaced with verbatim standard MIT text (`Copyright (c) 2025-2026 Terrell Deppe (KJ5HST)`). Major version bump because the *legal terms* change, not the methodology content. MIT grants use/copy/modify/distribute/sublicense/**sell** with attribution retained, **dropping** the previous license's no-resale / no-productization restriction (commercial *operational* use was already permitted; selling the methodology as a standalone product was not, and now is — by author decision). MIT also adds the standard `AS IS` warranty/liability disclaimer the custom license lacked and removes the "itself" commercial-use ambiguity. Chosen over a hardened-custom alternative that would have kept the no-resale restriction while closing the same two defects. Lockstep doc updates: `README.md` §License and the `CLAUDE.md` license line now point to MIT; `LICENSE` is not a `bin/_manifest.py`-distributed file, so adopters receive no synced change. No principle, phase, gate, workstream, or FM changes; FM count stays 26.
- **v3.0.1:** REUSE-compliance metadata + README badges (rmsharp PR #44; no methodology content change). Makes the repo's already-MIT licensing (v3.0) **machine-readable** and **REUSE Spec 3.3 compliant**: new **`REUSE.toml`** with a single `path = "**"` bulk-annotation rule (`SPDX-FileCopyrightText: 2025-2026 Terrell Deppe (KJ5HST)`, `SPDX-License-Identifier: MIT`) plus new **`LICENSES/MIT.txt`** — `reuse lint` reports 53/53 files covered for both copyright and license, 0 problems. Deliberately **additive**: the bulk rule means **no existing framework file gained an inline SPDX header** (the ~50 Markdown docs stay header-free), and the only edited file is `README.md`, which gains two badges under the H1 — a static shields `License: MIT` navigation badge (→ `LICENSE`) and a live REUSE-compliance badge that scans the default branch via `api.reuse.software` (the repo was registered there, so the badge renders *compliant*/green against the merge commit). **Patch** bump because this is licensing *metadata/tooling*, not a terms or content change — v3.0 already set the MIT terms; this only encodes them for machine analysis. **Canonical-only** — `REUSE.toml` and `LICENSES/` are not part of the `bin/_manifest.py` distribution corpus (same class as `LICENSE`), so adopters receive no synced change. Shipped as annotated tag + GitHub Release `v3.0.1` at the PR #44 merge commit — a patch *on top of* v3.0 (`v3.0` was left untouched, not re-pointed). No principle, phase, gate, workstream, or FM change; FM count stays 26.
- **v3.1:** First behavioral framework change since v2.8/v2.9 — makes this repo's `CHANGELOG.md` a dependable cross-source **action ledger** (backlog items, repository issues, ad-hoc work), the authoritative answer to *"what was done here, ever?"* (rmsharp PR #46; multi-session campaign S2–S7 against the ratified fork-only plan `docs/planning/changelog-authoritative-ledger-gate-plan.md`, decisions D1–D7). **The central finding reshaped the design:** a close-out write-gate *alone* is not dependable — an adversarial dependability pass returned `is_dependable: false` against eight high-severity escape paths (ghost/crashed session, out-of-band commit, in-progress hand-off, absent-ledger adopter, *canonical repo has no runner to gate on*, non-commit actions, multi-commit sessions, checklist self-exemption). So the ledger rests on **two mechanisms**: **gate-on-write** at close-out *and* **reconcile-on-read** at Orient. Six components: **(A)** new **FM #27 "Unrecorded action"** (appended — FMs 1–26 byte-unchanged, not renumbered — so the **failure-mode count moves 26 → 27**, the first new FM since v2.7's #26), a Degradation-Detection row, and a `SESSION_RUNNER.md` Phase 3F / `ITERATIVE_METHODOLOGY.md` Phase-6-step-8 close-out step that appends one dated, source-tagged entry per action, keyed to a mechanical fact (`### YYYY-MM-DD · [issue #<N>] | [BL-<N>] | [ad hoc]`, newest on top; backlog items removed from `BACKLOG.md` in the same commit). **(B)** a Phase 0 **reconcile-on-read** backstop — extends ghost-session detection to diff `git log` since the ledger frontier and backfill undocumented commits *before* new work, with a 1B `CHANGELOG: pending` crash breadcrumb (the dependability keystone; it adds a step to Phase 0, not a numbered phase). **(C)** a dashboard **ledger-lag freshness** monitor — git-only, **advisory RISK only** (never a hard fail), in both byte-identical twins, `DASHBOARD_VERSION` 2.6.1 → 2.7.0, splitting the flat `+2` doc score into `+1 present / +1 fresh`. **(D)** a greppable entry format + `starter-kit/CHANGELOG.md` seed recomposed from Keep-a-Changelog to the action-ledger shape (still a `seed`) + a `SESSION_NOTES`↔`CHANGELOG` division of labor (transient vs cumulative; shared key = the commit SHA). **(E)** this repo **dogfoods** its own rule with a root `CHANGELOG.md` distinct from this §Versioning ledger — a release carries a one-line **pointer** here, cite-don't-restate, so the two ledgers cannot diverge. **(D1)** a shipped `.githooks/pre-commit` co-staging gate — the mechanical enforcement where a repo has no root runner (escape #5): it refuses a commit changing tracked content unless `CHANGELOG.md` is co-staged, bypassable with `git commit --no-verify` (reconcile-on-read is the guarantee), skipping merges/rebases and absent-ledger repos; enabled per-clone via `git config core.hooksPath .githooks`. The close-out ledger step is propagated into **every** session-type + campaign checklist (escape #8) and codified as new **Learning #8**. **6 phases, 12 quality gates, four session types, and every workstream are unchanged** — the only structural additions are FM #27 (count → 27), the Phase 0 reconcile step, and the close-out ledger step. Shipped as annotated tag + GitHub Release `v3.1` at the PR #46 merge commit.
- **v3.2:** Dashboard fair-scoring for **document-only / research** repositories (fork backlog BL-5, rmsharp PR #50) — the first behavioral change to `methodology_dashboard.py` scoring since v3.1's Component-C freshness monitor. The code-centric rubric penalized a doc-only repo (the Research-Documentation workstream population — papers, dissertations, technical reports, regulatory analyses — and partly the framework's own doc corpus) for having nothing to unit-test: **0/20 Testing**, a HIGH "No test infrastructure" risk, a misleading `doc_to_source_ratio`, and a false "Large files" flag on a big chapter. The dashboard now **detects** a doc-only repo and **reshapes** its scoring. Detection (`detect_doc_only`) is marker-override → source-loc cap → corpus-disjunction: a bidirectional **`.methodology-profile`** marker (`doc-only` | `code`) wins; otherwise a **200 source-LOC cap** — which *guarantees* a mixed tooling repo with real code is never misclassified — gives way to a corpus check (a real doc corpus **or** a render toolchain, the latter catching pure-LaTeX/Quarto repos whose `.tex`/`.qmd` are not counted as docs, so their doc-LOC is ~0). When doc-only, the 2nd health slot (dict key `testing`, kept stable for JSON/portfolio/radar) is filled by a new **Render/Verification** score — an **honest static proxy**: a static scan cannot execute a render, so it scores render/verification *configuration* only (a render toolchain **6** + the v2.5 render-dependency check à la `pdffonts`/`fc-list`/`kpsewhich`, anti-pattern #20 **6** + a docs-render / link-check CI pipeline **5** + Research-Documentation verification artifacts **3** = 20), never render *success*, and is labeled a proxy on the card. The code-centric **No test infrastructure** / thin-coverage risks are **suppressed** for doc-only repos and replaced with render/verification advisories; the "**Large files**" risk is fixed (for *all* repos) to fire only on a *source* file — scanning for the largest oversized source file rather than only `largest[0]`, so a big chapter never trips it and a non-source #1 (e.g. a lockfile) never masks a real large source file below it. Two fold-in polish items: `.gitignore` covers `starter-kit/__pycache__/`, and **Signal F** (unmigrated `[x]` `BACKLOG.md` done-marks) is gated on methodology adoption so it can't fire on a non-adopter sibling. Ships `tools/test_methodology_dashboard.py` — the **first functional scoring tests** (29 stdlib-`unittest` cases, canonical-only; imports only the `tools/` module and byte-compares the twin, so no `starter-kit/__pycache__` is generated) — wired into `bin/tests.sh` (**51 → 54** checks, incl. a twin-diff + a `DASHBOARD_VERSION`-equality assertion). Both byte-identical twins; `DASHBOARD_VERSION` **2.7.0 → 2.8.0**. Design was pressure-tested by a judge panel + adversarial synthesis; the implementation was then hardened by an adversarial review that found and fixed **6 real defects** (a BOM-prefixed marker override silently dropped, `largest[0]`-only large-file masking, a `fmt_ratio` mislabel, a false footnote inequality, a tautological cap test, an untested render-dependency advisory). The dashboard remains **advisory — no hard gate**; **no principle, phase, gate, workstream, or FM change**, and the failure-mode count **stays 27**. Shipped as annotated tag + GitHub Release `v3.2` at the PR #50 merge commit.
- **v3.3:** **Durable close-out receipt** — the mandatory close-out *handoff/report* becomes a durable, machine-checkable artifact, closing the last close-out sub-step that produced no git-tracked file (rmsharp [PR #52](https://github.com/KJ5HST/methodology/pull/52); ratified fork-only plan `docs/planning/close-out-receipt-durable-artifact-plan.md`). **The motivating failure:** a session could finish its work, commit with a clean `CHANGELOG.md` line, and then fall silent — the handoff lived only in the transient `SESSION_NOTES.md` (overwritten next session) or the unpersisted Phase 3G spoken report, so a *skipped* report was invisible (the pre-commit hook saw its co-staged ledger; Phase 0 reconcile saw `frontier == HEAD`). Four layers, mirroring v3.1's ledger architecture exactly — **gate-on-write AND reconcile-on-read**: **(1)** a **`HANDOFFS.md` receipt** — one per-session ` ```handoff ` block (a structural twin of the action ledger, **SEED**-seeded to adopters) carrying the six Phase 3D minimum-handoff fields plus the two 1–10 scores as machine-checkable keys; written `status: pending` at the Phase 1B claim (a committed, fresh-clone-durable crash breadcrumb) and completed `status: complete` at Phase 3D. **(2)** **`bin/check-handoff`** (python3 stdlib, **canonical-only** — not in `bin/_manifest.py`; adopters copy it if they want it) asserts a receipt's presence and structural completeness (fence-isolated block, integer *distinct* scores, a `path:line` token in `key_files`, a sha-or-`pending` in `what_was_done`) plus anti-pattern lints (rejects "pick next from backlog", "need to verify", bare placeholders), **never semantic quality**; wired into `bin/tests.sh` (Tests 21–22). **(3)** a **Phase 0 reconcile backstop** — a missing or still-`pending` receipt for a session that left commits is reconstructed `status: reconciled` at the next Orient, folded into the one write Phase 0 already permits. **(4)** **framing** — **FM #6** (skip close-out) strengthened to name the receipt (**failure-mode count stays 27 — no new FM**), a Degradation-Detection row (commits landed but the receipt was never completed), new **Learning #9** (a handoff is dependable only as a durable machine-checkable artifact), and a *recommended, never-shipped* agent-harness session-end hook in `SAFEGUARDS.md`/`BOOTSTRAP.md` for the in-session catch (the methodology recommends; it does not reimplement). The write-step is reconciled into **every** session-type and campaign close-out checklist (Learning #8 meta-gate). **Honest ceiling, stated throughout the docs:** the receipt makes a skipped report *durable and auto-detected*, not *prevented* — the checker verifies structure not quality (a green check is not a good handoff), and a session that leaves no commit and no 1B stub still escapes; true dependability rests on reconcile-on-read at the next Orient, the same instruction-class ceiling as the ledger. **Adopters receive** the receipt discipline (the synced Phase 3D write-step + Phase 0 reconcile) and the `HANDOFFS.md` seed; the checker and the stop-hook are canonical-only / recommendation-layer. Built and closed out as a pre-declared **vertical slice** (P1–P6, checkpoint commit + full verification at each layer boundary) on a hybrid model split, then hardened by a 6-lens adversarial review (default-to-refute) that found and fixed **7 defects** — notably two regex holes in the `key_files` and `what_was_done` checks. The repo **dogfoods** the change: its own root `HANDOFFS.md` carries the first real receipt, `bin/check-handoff`-green. **No principle, phase, gate, or workstream change; the failure-mode count stays 27.** Shipped as annotated tag + GitHub Release `v3.3` at the PR #52 merge commit.
- **v3.4:** **Completeness-critic review lens** (rmsharp, upstream issue [#55](https://github.com/KJ5HST/methodology/issues/55)) — promotes **Learning #7** (cross-reference completeness at self-review) and **Learning #8** (close-out-gate checklist propagation) from authoring-time self-checks to an explicit **review-time lens**: a review or audit pass now owes a whole-corpus sweep — not just the diff — for enumerations, worked examples, indexes, and count-claims a change made stale elsewhere, whenever that change adds, renames, or removes a concept, artifact, file, step, or numbered-set member. **The motivating gap:** the v3.3 close-out-receipt work (#52/#53) shipped a clean 6-lens adversarial review, yet left `HOW_TO_USE.md`, the `README.md` repo tree, and three tutorials describing close-out *without* the new receipt — none were in the diff, so no diff-scoped lens looked at them; a human had to ask "has documentation been fully updated?" (fixed in #54). Three files: **`SESSION_RUNNER.md`** new **Learning #10** (table was 1–9); **`AUDIT_WORKSTREAM.md`** new anti-pattern **#9** "Diff-scoped blind spot" (list was 1–8), a new Verification Checklist bullet, and a note that `/code-review`/`/review`/`/security-review` are diff-scoped by design so the sweep stays methodology-owned; **`ITERATIVE_METHODOLOGY.md`** one sentence added to §Review/Audit Sessions citing the new Learning and the operative checklist step. The design was drafted, then adversarially checked by a 4-lens review (acceptance-criteria coverage, numbering/citation fact-check, a reflexive Learning-#7 self-check for other stale cross-references, and placement/precedent judgment) before commit — 2 lenses clean, 2 raised real findings (a mis-anchored insertion point in `AUDIT_WORKSTREAM.md`'s Recommended Skills section; a citation missing the `starter-kit/` path prefix used elsewhere in the repo), both fixed. All three touched files are `bin/_manifest.py`-distributed, so adopters receive this discipline via `bin/sync`. **No principle, phase, gate, or workstream change; the failure-mode count stays 27.** Shipped as annotated tag + GitHub Release `v3.4` at the merge commit.
- **v3.5:** **Capability-tiered review** (rmsharp, fork backlog BL-7) — an **elective** addition to `starter-kit/SESSION_RUNNER.md` §Vertical Slice Sessions codifying a pattern the operator observed live in the v3.3 close-out-receipt slice's own hybrid model split: a pre-declared slice's layers may be delegated to a lighter/faster capability tier only where an objective, checkable gate (a test suite, an exhaustive grep-reconciliation, a mechanical spec) proves correctness — the gate proves it, not the tier's judgment — while the strongest tier keeps judgment-heavy layers and reviews every delegated layer's output **before that layer's checkpoint commit lands**. Stated twice in different words to foreclose two plausible failure-mode-#26 misreadings: review is **additional evidence for gates (c)/(d), never a substitute** for the build/test matrix, and it is **not a fifth gate** — a single-tier slice satisfies (c)/(d) exactly as before. Four files, ~300 words, no new heading: **`starter-kit/SESSION_RUNNER.md`** the core paragraph + new **Learning #11**; **`ITERATIVE_METHODOLOGY.md`** a one-sentence routing pointer in §Matching Reasoning Effort to Stakes; **`starter-kit/RECOMMENDED_SKILLS.md`** an illustrative Claude-Code-specific addendum after §Reasoning Effort (brand names confined to this file only, matching how the existing Reasoning Effort table already handles this); **`docs/tutorials/T5_cautionary.md`** a corollary citing this repo's own close-out-receipt slice as the worked "doing it right" counter-example to FM #26. Preceded by an operator-approved 3-candidate design panel (extend-in-place / full-parallel-treatment / anchor-to-vertical-slice, scored on 4 lenses) that put every open decision — placement, naming, scope, and three optional extras — to the operator before implementation; then a 4-lens adversarial review (guardrail fidelity, citation/numbering fact-check, voice & agent-independence, completeness-critic whole-corpus sweep per the new v3.4 Learning #10) that unanimously caught and fixed one real defect (brand names leaking into the brand-neutral core file's new Learning row). **No new phase, gate, principle, or workstream; the failure-mode count stays 27.** All four touched files are `bin/_manifest.py`-distributed except the tutorial (canonical-only), so adopters receive the core/recommendation discipline via `bin/sync`. Shipped as annotated tag + GitHub Release `v3.5` at the PR #57 merge commit.
- **v3.6:** **Dashboard signal-integrity campaign** (rmsharp; upstream issues [#59](https://github.com/KJ5HST/methodology/issues/59)/[#60](https://github.com/KJ5HST/methodology/issues/60)/[#61](https://github.com/KJ5HST/methodology/issues/61) plus five unfiled defects; ratified fork-only plan `docs/planning/dashboard-signal-integrity-plan.md`, executed as a multi-session campaign **one layer per session**). The largest behavioral change `methodology_dashboard.py` has taken: `DASHBOARD_VERSION` **2.8.0 → 2.10.2**, both byte-identical twins, unit suite **29 → 197** (`bin/tests.sh` unchanged at 84 — the campaign added no shell checks). **One root cause, eight defects:** every one was *a proxy presented as a semantic finding* — a 110-point weighted sum rendered as a percentage; `.exists()` at the repo root rendered as "adoption"; a checkbox regex rendered as "completed work not migrated"; any file whose name starts with `CHANGELOG` rendered as "has an action ledger"; and a signal that never executes rendered as "no finding". The motivating case was a **false GREEN on a real adopter**, which silenced the exact finding the operator needed. **Layer 1** (2.9.0) *scale honesty* — the checklist weights summed **110** while `compliance_score` was rendered as a percentage, and methodology was the only one of five dimensions with no clamp (compliance 110 → **22/20**, total **102/100**; one adopter's card read "Methodology Compliance (110%)" live). Derived `METHODOLOGY_MAX`, producer-side `compliance_pct`, and `HANDOFFS.md` added to the checklist — shipped to adopters as a SEED since v3.3 and never scored. Deriving the grid `headers` from `METHODOLOGY_ITEMS` was required for correctness, not cleanup: a hard-coded 8-string header list over 9 item cells misaligns the grid. `dashboard_version` is now stamped into each `dashboard_history.jsonl` entry. **Layer 2** (2.9.1) *ledger identity* — a `docs/` product changelog masked a missing action ledger; **archive shadowing** returned `CHANGELOG-archive.md` over `CHANGELOG.md` (`-` = 0x2D sorts before `.` = 0x2E); and Signal F was unreachable whenever no changelog existed, so the strictly worse case went silent. **Layer 3** (2.9.2) *backlog shape* — the done-mark predicate was checkbox-only and blind to table backlogs (a real 643-line table backlog counted **0** against **256** true done-rows). Six formats now, and ratified decision **D4: abstention is a first-class result** — a count travels with the convention it was read under, because a `0` from a format the scanner cannot read is indistinguishable from a genuinely clean backlog, and that silence *is* the defect. **Layer 4** (2.10.0, closes #59) *repo role* — the canonical repo scanning itself scored compliance 5 / health 43 with a false medium risk *"Partial methodology adoption (5%)"*; structural role detection plus `FRAMEWORK_ITEMS` now scores a framework publisher against what it actually owes, with the `methodology` dict key rendering as "Framework Integrity". **Layer 5** *completeness-critic doc sweep* — v3.4's own Learning #10 dogfooded against Layers 1–4; the plan predicted 6 sites, the corpus held **20 across 7 files**. **Layer 7** (2.10.1) — appended, never renumbered, and deliberately executed *before* Layer 6 so no release would ship with a known live defect in this subsystem: `bin/sync` installs the scanner to the adopter **root** while `DOC_ONLY_SOURCE_LOC_MAX` is 200, so the source-cap short-circuit fired before the corpus check and **installing the methodology destroyed the doc-only fair-scoring v3.2 exists to provide** (live since v3.2, found by Layer 5's late review). **The ratified fix was insufficient, and only measurement showed it** — excluding just the scanner *flipped* the defect, because `bin/sync` also installs 21 markdown files that satisfy the corpus check unaided, so a 148-own-LOC repo went `code → doc-only` and **lost a true** "No test infrastructure" risk. The shipped predicate is root-anchored **and** content-verified, with the four ordinary SEED names evidence-gated so a repo that never ran `bin/sync` keeps its own docs. **Layer 6** — this close-out, plus new **Learning #12** (when an invariant is mechanical, encode it as a test; a review-time grep is a human step that silently stops happening — the `HANDOFFS.md` checklist gap survived two releases of exactly the greps Learnings #7/#10 prescribe). **Layer 8** (2.10.2) — appended, never renumbered, and added *after* the release narration was already written, because the pre-PR adversarial review found the campaign's own defect class one level up in Layer 7's own fix. `FRAMEWORK_INSTALLED_DOCS` discounted 17 names unconditionally on the stated ground that "nobody writes a root `SESSION_RUNNER.md` by coincidence" — but six of the 17 are ordinary root filenames (`BOOTSTRAP.md`, `SAFEGUARDS.md`, `RECOMMENDED_SKILLS.md`, `CONTEXT_TEMPLATE.md`, `CLAUDE_TEMPLATE.md`, and `SESSION_RUNNER.md` itself), matched by bare path with no content check and no evidence gate. **This was a true regression against v3.5, reproduced against both scanners:** a documentation project that never installed the framework, whose corpus was its own 302-line root `BOOTSTRAP.md`, had that file subtracted from its own doc corpus, flipped `doc-only → code`, and gained a false HIGH "No test infrastructure" — v3.2's exact false penalty, re-created a *second* time by the fix for its mirror. Worse, one coincidental root name set the evidence flag that unlocks the seed fold-in, so the same repo's own `CHANGELOG.md` and `ROADMAP.md` were discounted too — one accident defeating the very gate Layer 7 built to protect those four. The tiers now partition by what a name actually proves: only a `docs/methodology/` path is self-evidencing (nothing lands there by accident), while the six root names are evidence-gated and count only once `FRAMEWORK_AMBIGUOUS_EVIDENCE_MIN` of them co-occur — which every real `bin/sync` install and every manual Option-B copy clears, so no install silently stops being discounted. Layer 8 also gates the classification itself on **having no tests**: a repo with a real suite is never a document project, which had stopped being true — the tutorials' own sample project, a Python CLI with a green `pytest` suite, classified `doc-only` once `bin/sync` discounted the markdown around it and then drew a *"no tests"* advisory **on a passing suite**, a signal contradicted by the same metrics dict that emits it. Six new tests, three of them RED against the pre-Layer-8 scanner; the live 10-repo fleet is **completely unmoved** (0 of 10 change class, score, or risk set), so the layer is purely corrective. **Honest limits, stated rather than fixed:** one case of the campaign's own defect class remains **open and disclosed rather than quietly shipped** — the framework-doc discount subtracts the four SEED names (`CHANGELOG.md`, `SESSION_NOTES.md`, `HANDOFFS.md`, `ROADMAP.md`) whenever the framework is detected as installed, but `bin/sync` *leaves a seed alone* when the adopter already has one, so a document project whose corpus is concentrated in those filenames can still read as `code` after installation (reproduced: `doc_only` True → False with a false HIGH "No test infrastructure", health 28 → 47). **This** case is genuinely **not a regression** — v3.5, 2.10.1 and 2.10.2 were run side by side against the same fixture and all three return the identical wrong result (`doc_only` False, false HIGH "No test infrastructure", health 49/47/47) — and `README.md` / `BOOTSTRAP.md` name the gap and point at `.methodology-profile`; closing it in code is a follow-on layer. That claim is now scoped deliberately, because the pre-PR review found the one case where an unqualified version of it was **false** — the ambiguous-filename discount above — and Layer 8 closed that rather than weakening the sentence. The distinction is the whole point: an adopter's own file being discounted *after they installed* is a known limit of a heuristic, while a non-adopter's file being discounted is a defect, and only measurement separated the two. Beyond that: the dashboard remains **advisory — never a hard gate**; **presence is still not use** (the checklist scores `.exists()` and cannot tell a maintained runner from an empty stub); and the corrected scale produces a **one-time trend discontinuity**, since `dashboard_history.jsonl` carries no scale marker — the new `dashboard_version` stamp is what makes that step interpretable rather than a phantom regression. **No principle, phase, gate, or workstream change; the failure-mode count stays 27.** Only `starter-kit/methodology_dashboard.py`, `starter-kit/BOOTSTRAP.md`, `HOW_TO_USE.md`, and `starter-kit/SESSION_RUNNER.md` are `bin/_manifest.py`-distributed, so that is what adopters receive via `bin/sync`; the tests, the plan, and the tutorials are canonical-only.
