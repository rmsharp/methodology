# HANDOFFS.md — archive: 2026-09-08 → 2026-09-08

Retired records from [`HANDOFFS.md`](../../HANDOFFS.md), moved here so the live ledger stays small enough to read
in one pass. Same format, same newest-on-top order — this is the same ledger, continued.

Holds **2 record(s), 2026-09-08 → 2026-09-08**. Cut key: `2026-09-08`. Counts here are computed from the file
itself, never carried forward. This shard is frozen: it states no forward-looking rule,
because the live file owns those and a copy of one was wrong a day after it was written.

---

```handoff
session: S154
date: 2026-09-08
status: complete
self_score: 8
predecessor_score: 8
active_task: **THE TRIM IS DONE AND VERIFIED: `HANDOFFS.md` 245,254 → 42,938 B at `bad0489`, 27 receipts → 4.** This close-out's own receipt then returns it to ~54 KB — the steady state at four, well under `CEILING_BYTES` 65,536 — so quote the delta, not a moving size. `methodology_trim.py --check` now **exits 0, "trigger does not fire"**. 23 records (2026-08-12 → 2026-09-04) are frozen in [`docs/archive/HANDOFFS-through-2026-09-04.md`](docs/archive/HANDOFFS-through-2026-09-04.md) with its `.verify.sh`. **The operator released the S129 deprioritisation AND, separately, approved the `--force`** — two decisions, asked one at a time, neither inferred. **Fork-internal: nothing pushed, no PR, no tag, no comment.** `main` is now **6 commits ahead of `origin/main`** (`f8531cf`); `upstream/main` `512c2ed` and `read-set-budgets` `598c459` untouched; 0 open PRs.
what_was_done: **Six commits: `1695734f` claim, `aa1c476` the raw trim, `bad0489` the fold, `f61c9d5` close-out, `7e5d205` the drifted anchors, plus this answer-slot repair.** **THE `--force` GATE WAS FOUND AT THE CLAIM — S153's recipe omits it and does not run.** It said *"`--check`, `--write`, then `verify.sh`"*; `--write` alone **exits 2 and writes nothing** (`SRF_RED` 3.7022 vs `78a29f8`), and the refusal is **cut-independent** — `--cut 4` reproduces it. So the flag that needed a decision was the one the recipe left out. **EVERY NUMBER PUT TO THE OPERATOR WAS BUILT FIRST**, in a `--no-local` clone, never predicted. **BOTH COMMIT SHAPES WERE RUN.** The tool ends *"one ledger, one shard, one entry, **one commit**, one revert"*; `:69-73` orders a hand fold of the generated pointer block into the archive table. **Folded INSIDE the trim commit the shipped proof FAILS** — `L2 FRONT MATTER lost 1 line(s)`, exit 1 — **folded in the NEXT commit it exits 0**, the proof pinning to the trim commit. New **Learning #58** (1,455 B; the first draft was 1,511 B and `bin/check-learnings` refused it — said shorter, budget not raised). **THE FRONT-MATTER BYTES CAME OUT OF FALSE CLAIMS, NOT SHORT ONES:** the N=4 warrant resting on the 56,750 B detector floor (§11.5 (5) forbids it) with its stale *"near 52 KB"*; the S127 warrant pointer, **dangling exactly as §11.5 (4) predicted**, S152's trim having moved those entries (now `docs/archive/CHANGELOG-through-2026-09-02.md:2070`, found by grep — the live file still matches `S127` twice, so a presence check would have called it healthy); and the S7/S8 example, every referent of which this trim archived. **7,570 → 7,096 B, headroom 46 → 72 B.**
next_steps: **(1) PUT §11.3 ITEM 1 — THE PR'S SCOPE — TO THE OPERATOR AS A FOUR-PAYLOAD DECISION, AND RUN EACH PAYLOAD FIRST.** Unchanged from S153 and still the upstream plan's next phase; this session did not touch it. The four: **Phase 1+2** (on `port/framework-learnings-extraction` = `7d5b186` today), **Phase 3**, **Phase 4**, **`starter-kit/methodology_trim.py`** (113,629 B, distributed, never upstream). Start: `sed -n '555,580p' docs/planning/upstream-read-set-pr-plan.md`. **Learning #57 and now #58 both say the same thing: build each shape and run `bash bin/tests.sh` before offering it.** §11.3 item 2 already loads the dice — merging Phases 1+3+4 leaves `--source=github` installing nothing while the trimmer is absent. **Opening the PR and pushing are each a SEPARATE go-ahead.** **(2) DRAFT THE PR BODY (§11.3 item 7) — BLOCKED BY (1).** Writing it is fork-side and needs no go-ahead; opening it does. **(3) FIX `HOW_TO_USE.md:774`: 27 → 28. UNBLOCKED, do it if (1) stalls.** Re-verified this session: that line reads *"A table of 27 documented agent tendencies"*; the highest FM row in `starter-kit/SESSION_RUNNER.md` is **28**. Count by highest row number — `grep -c '^| [0-9]* | \*\*'` returns **43**, that file holding several numbered tables. **Do NOT touch `README.md`'s v3.2–v3.6 sections**; 27 is correct there. **(4) THE HEADER RESERVE IS NOW AN OPERATOR QUESTION — ASK, DO NOT RAISE IT.** The front matter sits at **7,096 of 7,168 B (99%)** and blocked **two** correct edits this session; I reverted my own comment edit rather than ship 7 B of headroom. The A1 fit uses **44,032 of 65,536 B — 21,504 B of slack** — so raising `HEADER_RESERVE_BYTES` is arithmetically free, which is exactly why S153 forbade doing it silently (`bin/check-handoff:612`, the re-derive-from-the-ceiling move). Put the trade to the operator; do not decide it. **(5) FIX `starter-kit/methodology_trim.py:1014` `choose_cut`** so `resulting(k)` counts the pointer block and regenerated-field growth before testing `stops(b)`. Untouched here — an explicit `--cut 4` bypasses it. Distributed: wants upstream consideration, not a quiet fork fix. **(6) DELETE THE FIVE MERGED BRANCHES ON `origin`** — all five confirmed still present this session (`git ls-remote --heads origin <b>` returns 1 each): `docs/learning-13-handoff-predictions pr1/framework-learnings-extraction pr2/ledger-trimmer pr3/apparatus-extraction pr4/context-budget-gate`. **(7) PUSH `main` — 6 commits ahead of `origin/main`.** (6) and (7) are outward-facing on the fork: confirm first.
key_files: `HANDOFFS.md:8` (the retention paragraph, rewritten — N=4 is an operator decision, and the warrant now cites `docs/archive/CHANGELOG-through-2026-09-02.md:2070` instead of dangling), `:69-73` (the `<!-- NEXT TRIMMING SESSION -->` comment — **it still does NOT say "separate commit"; that is Learning #58's job because the sentence would not fit the reserve**), `:67` (the archive table, row 12 added; the `n` column sums to 139). `bin/check-handoff:663` (`HEADER_RESERVE_BYTES = 7168`) and `:662` (`CEILING_BYTES = 65536`) — the two constants item (4) is about. `bin/tests.sh:3047` (`fmbytes39`, line-anchored because this file quotes its own fence in prose) and `:3097` (the A2 assertion). `starter-kit/methodology_trim.py:1014` (`choose_cut`, item (5)), `:1093`/`:1103` (`build_pointer_block` / `insert_pointer` — **verified live this session; BL-50's detail cites `:940-950`, which has drifted**). `starter-kit/FRAMEWORK_LEARNINGS.md` row 58 (last line). `docs/planning/srf-red-refusal-adjudication.md:417` (§11, **read before quoting §§1–10** — §11.2 supersedes §2 in part).
gotchas: **(1) THE FOLD MUST BE ITS OWN COMMIT.** Folded into the trim commit, the shipped `.verify.sh` fails `L2 FRONT MATTER lost 1 line(s)`. Both shapes were run; do not "simplify" the two commits into one. Learning #58. **(2) `check-learnings` SAYS `contiguous 1..57` AND THE HIGHEST ROW IS 58 — THAT IS BL-44, NOT A DEFECT YOU FOUND.** 57 rows present, `#14` a documented reserved gap; the checker derives N from `len(rows)`. Exit 0 is right, the sentence is wrong. I verified this before reporting it, and so should you. **(3) THE FRONT MATTER HAS 72 B; any edit to it is a byte negotiation** — measure with `bin/tests.sh`'s `fmbytes39` (line-anchored; a bare substring search stops at the fence quoted in the prose and measures ~400 B). **(4) READ EXIT CODES BARE.** I wrote `EXIT=${PIPESTATUS[0]}` after a pipe once this session and got an **empty string** — zsh, not bash. Run the command bare on its own line. **(5) `bin/tests.sh` MUTATES AND RESTORES BOTH LIVE LEDGERS** — back them up outside the repo; never edit `HANDOFFS.md` while it runs. **(6) `--force` HERE IS NOT A PRECEDENT** — approved for this file, this regime, this evidence, as S132's was for `CHANGELOG.md`. The next trim asks again. **(8) AN ALL-NUMERIC ABBREVIATED SHA IS INVISIBLE TO `bin/check-handoff`.** `SHA_RE` (`:211`) requires *at least one hex letter* — *"a bare 7+ digit decimal is not a sha"* — so this session's claim commit, which abbreviates to `1695734`, failed the answer-slot rule and turned `bin/tests.sh` **304/1 → 303/2** the moment a fixture pushed this receipt below the newest. Not a ledger defect: the checker cannot tell a real sha from a decimal. **Abbreviate to the first length that includes a letter** (`git rev-parse --short=8`) whenever the short form is all digits. **(7) `context_budget.py` NOW REPORTS `HANDOFFS.md` AS `warn` WITH A 77% DENSITY DRIFT** — density 2.3648 B/token was measured at 186,617 B and the file is 42,938 B. Correct behaviour, predicted by §11.3 for the S132 trim; `measured_bytes` was deliberately NOT bumped, since raising it without re-deriving the density writes a false provenance record.
runtime_smoke: **THIS REPO SHIPS NO APPLICATION; THE BUILD-EQUIVALENT IS `bash bin/tests.sh`.** Phase 0 baseline and close-out both **304 passed / 1 failed / 0 skipped, exit 1 read bare**, and diffed **ROW FOR ROW** by normalised assertion label: **303 rows both sides, zero status flips, zero added, zero removed.** The one failure is Test 9 `github source dry-run failed`, pre-existing — 3 of 27 `bin/_manifest.py` sources absent from `upstream/main`, `bin/sync` exits before writing; no fork-internal trim can flip it. **The deliverable's own proof, run AFTER committing as the procedure requires: `bash docs/archive/HANDOFFS-through-2026-09-04.md.verify.sh` → exit 0**, *"L1, L2/front-matter, L3 hold"*, source pinned to the trim commit `aa1c476`, *"27 before = 4 retained + 23 archived"*. Run on the raw trim in a clone (0), after the fold in a clone (0), and here (0), plus the negative control — the one-commit shape — which **failed (1)**. Checkers, each bare: `check-links` **0** (105 links / 23 files — the new shard's links resolve), `check-learnings` **0** (57 rows, 0 over 1,500 B), `check-handoff` **0** at close-out (it read **1** while this receipt was `status: pending` — the crash breadcrumb working). `bin/model-report` **0**, and **it reads the new shard** — 14 files, live 0 + archived 48, `HANDOFFS-through-2026-09-04.md` contributing 5: the S133 glob fix held, so this trim did **not** gut the tool the way §11.3 records the `CHANGELOG.md` trim doing. `context_budget.py` **exit 2**, pre-existing (read-set total). Both live ledgers backed up outside the repo and `shasum -c` verified byte-identical after every suite run. **NOT EXERCISED:** any adopter tree, upstream CI (neither side has `.github/workflows`), the pushed path, the eight other frozen archive proofs, the maintainer's machine.
changelog_ref: CHANGELOG.md "2026-09-08 · [ad hoc] S154 close-out — `HANDOFFS.md` trimmed under operator-approved `--force`, 245,254 B → 42,938 B, 27 receipts → 4", plus the tool's own "Ledger trim" entry, the "pointer-block fold" entry, the drifted-anchors entry, and the S154 claim entry
commit: 1695734f (claim — 8 chars, see gotcha (8)) + aa1c476 (the trim) + bad0489 (the fold) + f61c9d5 (close-out) + 7e5d205 (the drifted anchors) + this commit
```

**Self-assessment: 8/10.** Plus: found the `--force` gate rather than tripping over it — the
predecessor's recipe would have run `--write`, got exit 2 and written nothing — then put it back to
the operator with §2's *"the trim must not be forced"* quoted **against** my own deliverable. Built
**both** commit shapes and ran the proof on each, which is the only reason I know the tool's own
*"one commit"* shape fails. Three budgets refused an edit and all three times I took the cut, not the
constant. **Minus:** three measured iterations fitting a comment I then reverted; I typed
`EXIT=${PIPESTATUS[0]}` after a pipe; and I wrote a sha I had not read (`0d7f1b5` for `7e5d205`) into
this receipt, caught only by re-running.

**Predecessor (S153): 8/10.** Its three gates were real and exact — Test 34's three receipts; **46 B**
of headroom (7,122 vs 7,168, to the byte); re-derive the dry run **after** the claim commit. And
`next_steps` (4)'s **ask, do not infer** is why there are two clean operator decisions on record
instead of one assumed. **Not 9: the recipe it handed forward does not run.** *"`--check`, `--write`,
then `verify.sh`"* — `--write` exits **2** and writes nothing, `SRF_RED` refusing. It ran `--check`,
which says nothing about the refusal, and evidently never the dry run — so the one flag needing an
operator decision is the one omitted. That is **Learning #57, authored in that same session**,
applied to its own handoff.
```handoff
session: S153
date: 2026-09-08
status: complete
self_score: 8
predecessor_score: 8
active_task: **TIER-2 (a) IS SETTLED: FREEZE (iii).** The operator chose it from the three options `port-branch-identity-adjudication.md` §6 Tier 2 deliberately declined to rank, deferred as a whole on 2026-08-29 (`1593cb5`). Upstream receives the **46-row table as of 2026-08-27**, `starter-kit/FRAMEWORK_LEARNINGS.md` held byte-identical to blob `b21854cc`. He made two further calls at the same gate: the §5(b) `:92-94` rewrite rides along **as a second commit, not an amend**, and the branch stays **LOCAL — not pushed**. **`port/framework-learnings-extraction` is now `upstream/main` + 2 commits** (`30ddf26` + `7d5b186`) and verified. **NO PR, NO PUSH, NO TAG, NO COMMENT** — `git ls-remote --heads origin <branch>` empty, `upstream/main` `512c2ed` and `read-set-budgets` `598c459` unchanged, 0 open PRs on either repo.
what_was_done: **Four commits: `35d9eeb` claim, `231bdbd` chore(history), `7d5b186` the branch rewrite, plus this close-out.** **EVERY CONSEQUENCE IN THE DECISION TABLE WAS RUN, AND THAT IS WHAT FOUND THE DEFECT:** each option's payload went through `bash bin/tests.sh` in a real worktree at `30ddf26` — freeze **113/1** (control), refresh **113/1**, **option (v) as written 110/4**. The `ed22ace` blob §5 names by SHA predates Phase 2's compaction, so rows **#12 (2,400 B)** and **#13 (1,572 B)** breach the 1,500 B budget *this branch itself adds* to `bin/check-learnings` (`ROW_BUDGET_BYTES`: 0 on `upstream/main`, 7 on `30ddf26`). Recoverable — Phase 2's compacted rows #1–#13 return **113/1** at 14,072 B — but as posed across three documents for ten days, **no document had ever executed it**. New **Learning #57** (1,247 B). **TWO OF MY OWN INSTRUMENTS WERE WRONG AND WERE CAUGHT BEFORE THE NUMBER REACHED THE OPERATOR:** the absent-artifact count read **14 of 46** until basenames were resolved (**9**, which independently reproduces S123's recorded figure); the session-citation count read **35** until restricted to the entry's stated S35–S119 range (**32**, exactly as written). `7d5b186` also corrected the bullet's own undercount — *"Seven … across 10 of the 46 rows"* is **8 across 11**, `ledger-trimmer-design.md` omitted.
next_steps: **Each item opens with an imperative and carries its first command; where a gate exists it is named before the verb.** **(1) PUT §11.3 ITEM 1 — THE PR'S SCOPE — TO THE OPERATOR AS A FOUR-PAYLOAD DECISION, exactly as Tier-2 (a) was put, and RUN EACH PAYLOAD FIRST.** This is the upstream plan's next phase and the one thing now blocking a PR body. Tier-2 (a) settled the table's *content*; it did not settle *which phases the branch carries*. The four: **Phase 1+2** (on the branch today), **Phase 3**, **Phase 4**, and **`starter-kit/methodology_trim.py`** (113,629 B, distributed, never upstream). Start: `sed -n '555,580p' docs/planning/upstream-read-set-pr-plan.md`. **Learning #57 applies directly — build each payload in a worktree and run `bash bin/tests.sh` before offering it as an option.** **Opening the PR and pushing are each a SEPARATE go-ahead; this session had neither.** **(2) DRAFT THE PR BODY (§11.3 item 7) — BLOCKED BY (1), since its scope section is the answer to (1).** It does not exist; the two `issue75-upstream-pr*.md` drafts are a different, unsent PR. It owes answers to §6 dragons 1, 2 and 4. Writing it is fork-side and needs no go-ahead; **opening it does.** **(3) FIX `HOW_TO_USE.md:774`: change 27 → 28. UNBLOCKED, do it if (1) stalls.** Verified this session: that line reads *"A table of 27 documented agent tendencies"* and the highest FM row number in `starter-kit/SESSION_RUNNER.md` is **28**. `HOW_TO_USE.md` is TRACKED, so the stale figure ships to every adopter. **Do NOT touch `README.md`'s v3.2–v3.6 sections** — 27 is correct there as shipped. Count by highest FM row number; `grep -c '^| [0-9]* | \*\*'` returns 34 because that file holds several numbered tables. **(4) ASK THE OPERATOR TO RELEASE `HANDOFFS.md`, THEN TRIM IT — ASK, DO NOT INFER.** He released `CHANGELOG.md` specifically at S152; the S129 deprioritisation still covers this file and is unrepealed. Now **235,610 B, 26 receipts, cap 4**, and `python3 starter-kit/methodology_trim.py --file HANDOFFS.md --check` **exits 1 (fires)**. On release: `--check`, `--write`, then `bash docs/archive/<shard>.verify.sh` **after committing**. Three gates unchanged: Test 34 needs **three** receipts to survive; the front matter has **46 B** of headroom against `bin/tests.sh:3047` `fmbytes39` — cut the edit, never raise the reserve; re-derive the dry run **after** the claim commit. **(5) FIX `starter-kit/methodology_trim.py:1014` `choose_cut`** so it adds the pointer block and regenerated-field growth to `resulting(k)` before testing `stops(b)`. Distributed, so it wants upstream consideration, not a quiet fork fix. **(6) DELETE THE FIVE MERGED BRANCHES ON `origin`** — `git push origin --delete docs/learning-13-handoff-predictions pr1/framework-learnings-extraction pr2/ledger-trimmer pr3/apparatus-extraction pr4/context-budget-gate`. Outward-facing on the fork: confirm first.
key_files: `docs/planning/upstream-read-set-pr-plan.md:483` (§11 — **read before quoting §2–§5**; §11.1 at `:505` is what makes *freeze* mean "keep Phase 2's output", and it is why this session did not mis-frame the options), `:555` (§11.3, the eight remaining Phase 5 owings — item 1 is now next). `docs/planning/port-branch-identity-adjudication.md:159` (§5, the five costed options — **option (v) at `:211` names `ed22ace` by SHA and that payload ships 110/4**), `:233` (§6, the two tiers). `CHANGELOG.md` `7d5b186:85-95` on the branch (the rewritten bullet). `bin/check-learnings:104` (`ROW_BUDGET_BYTES = 1500`, the arm the branch adds and option (v) breaches). `30ddf26:bin/sync:191` (`--source choices=("local","github")` — the line that refutes the byte-identity claim; **sha-qualified because the same line is `:245` on `main`** — a bare line number into a growing file drifts), `bin/sync:136` on `main` (the `sys.exit()` that keeps Test 9 red — **S152's handoff and this session's claim entry both say `:134`; grep says `:136`). `starter-kit/FRAMEWORK_LEARNINGS.md:89` (Learning #57).
gotchas: **(1) `30ddf26` IS CITED 41 TIMES ACROSS 8 FILES, THREE OF THEM FROZEN ARCHIVES — never amend or rebase it.** `grep -rl '30ddf26' --include='*.md' .` before touching that branch. This session appended `7d5b186` instead for exactly this reason; the count was measured before the choice, not after. **(2) A BARE-BASENAME PATH CHECK AGAINST FULL TREE PATHS OVER-REPORTS BY 55%** — `SESSION_NOTES.md` and `RECOMMENDED_SKILLS.md` exist upstream under `starter-kit/`, so comparing the bare token to `git ls-tree` paths called them absent. 14 vs the true 9. Resolve by basename when the mention has no slash, and **eyeball the raw matched items**. **(3) A COUNT IS ONLY TRUE OF ITS STATED RANGE.** *"32 of 46 rows cite session numbers S35–S119"* is exactly right; **35** rows cite *some* fork session once S1/S7/S9/S16 are counted. Neither number is wrong — the range is part of the claim. **(4) CHECKING OUT THE PORT BRANCH IN THE PRIMARY WORKING TREE REPLACES `CLAUDE.md` AND `SESSION_RUNNER.md` WITH UPSTREAM'S OLDER COPIES** and fires a file-changed notice. Harmless with a clean tree, but **use `git worktree add --detach` instead** — this session did for every test run and only used the primary tree for the commit itself. **(5) `git worktree prune` FIRST** — a dead session's scratchpad worktree blocks `add`. **(6) `bin/tests.sh` MUTATES AND RESTORES BOTH LIVE LEDGERS.** Back both up **outside** the repo and `shasum -c` after; never `cd` into the backup dir to check (that compares copies to themselves). **(7) READ EXIT CODES BARE.** `python3 starter-kit/context_budget.py | tail` reported `0`; run bare it is **2**. I made this mistake once this session and caught it in the same phase.
runtime_smoke: **THIS REPO SHIPS NO APPLICATION; THE BUILD-EQUIVALENT IS `bash bin/tests.sh`.** **Six full suite runs this session, every exit code read bare.** On `main`: Phase 0 baseline **304 passed / 1 failed / 0 skipped, exit 1**, and the close-out re-run after Learning #57 (below). In a detached worktree at `30ddf26`: control **113/1**; option (v) as written **110/4**; option (v) repaired **113/1**; option (iv) refresh **113/1**. Pristine `upstream/main` **114 passed / 0 failed, exit 0**; the branch `7d5b186` **113/1**. **ROW-FOR-ROW, TWICE, by normalised assertion label:** `30ddf26` → `7d5b186` = **114 rows both sides, 114 shared, 0 status flips, 0 added, 0 removed**; `upstream/main` → `7d5b186` = **113 shared, 0 flips**, one row differing because the test names its own outcome (`github source dry-run works` → `failed`). **THAT ONE FAILURE'S CAUSE WAS MEASURED, NOT ASSUMED:** of the branch's **25** manifest sources exactly **1** is absent from `KJ5HST/methodology` — `starter-kit/FRAMEWORK_LEARNINGS.md`, the file this PR adds — so **merging the PR cures its own only failure**. That is a *different* fact from the fork's Test 9, where 2 of 27 remain absent. Checkers on `main`, each bare: `check-learnings` **0** (56 rows, contiguous 1..56, 0 over 1,500 B), `check-links` **0**, `check-handoff` **0**, `--all` **0**. Freeze verified as an identity: `git hash-object starter-kit/FRAMEWORK_LEARNINGS.md` on the branch = `b21854cc2fc6a736801a5e87ae43b4407872eeef`, and `git diff --name-only` for `7d5b186` lists **`CHANGELOG.md` only**. Both live ledgers backed up outside the repo and `shasum`-verified byte-identical after every suite run. **WHAT THIS DOES NOT EXERCISE:** any adopter tree, upstream CI (neither side has `.github/workflows`), the maintainer's machine, the pushed-branch path (nothing was pushed), and the eight frozen archive proofs.
changelog_ref: CHANGELOG.md "2026-09-08 · [ad hoc] S153 close-out — Tier-2 (a) settled FREEZE; the port branch is `upstream/main` + 2 commits and passes row-for-row with zero status flips", plus "2026-09-08 · [ad hoc] S153 — the `:92-94` defect rewritten on the port branch" below it, and the 2026-09-07 S153 claim entry
commit: 35d9eeb (claim) + 231bdbd (chore/history) + 7d5b186 (the branch rewrite, on `port/framework-learnings-extraction`, NOT on `main`) + this close-out
```

