# HANDOFFS.md — archive: 2026-09-21 → 2026-09-21

Retired records from [`HANDOFFS.md`](../../HANDOFFS.md), moved here so the live ledger stays small enough to read
in one pass. Same format, same newest-on-top order — this is the same ledger, continued.

Holds **1 record(s), 2026-09-21 → 2026-09-21**. Cut key: `2026-09-21`. Counts here are computed from the file
itself, never carried forward. This shard is frozen: it states no forward-looking rule,
because the live file owns those and a copy of one was wrong a day after it was written.

---

```handoff
session: S211
date: 2026-09-21
status: complete
self_score: 8
predecessor_score: 9
active_task: **P2 OF THE `context_budget.py` PLAN IS BUILT AND VERIFIED: ONE COMMIT, `c299c30`, ON THE LOCAL BRANCH `fix/context-budget-status`, AFTER P1'S `e859196`. IT IS NOT PUSHED, AND NOTHING IS UPSTREAM-FACING. NEXT: THE PLAN'S P3 (vet and package), WHICH ALSO OWES ONE OPERATOR DECISION ON A STATUS-PRECEDENCE EDGE FOUND HERE.** The plan is [`docs/planning/context-budget-status-plan.md`](docs/planning/context-budget-status-plan.md). What changed: when the growth run fires, the advisory's second sentence is chosen by `worst`, the headline's variable. Unchanged word for word when nothing is over; *"A ceiling has fired as well — see the rows marked over."* when something is (§3 D3's own example). Tool blob `131158cb` → `dd4803bf`. **Found and NOT fixed:** `measure_file()` gives a row the status of whichever check wrote last, so a byte-ceiling `over` is overwritten by a failed structure pattern's `instrument-failed`. On the fixed tool the advisory then still says *"Nothing is over a ceiling yet"* above *"1,500 B exceeds the 1,000 B ceiling by 500"*. It was run, not predicted. It is outside D3, it is recorded in the plan's *P2 outcome* (`:388`), and the upstream entry names it under *Not changed here*. BL-75 and BL-80 stay open until the PR merges. **#83, #84, #85: still no maintainer response; nothing owed upstream.** Side actions, approved at the Phase 0 picker: the `HANDOFFS.md` retention trim and its fold, and the fork push after this commit, with its record following.
what_was_done: **Phase 0:** `CHANGELOG.md` frontier = HEAD `36044bb`, gap empty; `HANDOFFS.md` frontier `1e1cd18`, the one commit after it being S210's announced push record. No pending stub, 2 receipts; nothing backfilled. Gate citation **re-run** in a `--no-local` clone at `36044bb`, HEAD asserted: S210's exactly. Dashboard 76/100, medium, 0 high+. Upstream: 0 issues; #83/#84/#85 heads `219fb9d`/`77afc12`/`e2501c5`, 0 reviews, only our comments (#83's newer `updatedAt` is our fork pushes referencing it); `upstream/main` still `6b29d3d`. **`520f972`** the claim. **`03194df`** the retention trim (`--cut 2 --force`, dry run first, 25,742 → 15,895 B, shard **-12**; SRF, SHARD_NAME_DISAMBIGUATED and FRONTMATTER_FIELD_ABSENT, all expected, and **no CUT_STRADDLES_DAY this time**: the cut fell between the 2026-09-20 and 2026-09-21 receipts). Its `.verify.sh` gave **L1/L2/L3 OK, exit 0 read bare** in a clone at `03194df`. **`4542465`** the fold; `bash bin/tests.sh` in a clone at `4542465`: **343 / 0 / 6 skipped**. **P2, in a `--no-local` clone of the local branch with `core.hooksPath` set, HEAD asserted `e859196`:** read `render()`, `main()`, the history code and `measure_file()`'s status writes. `git grep` found the sentence in one place only (the tool; no test or doc quotes it). 3 tests in `TestGrowthRunAdvisory` written first: (1) a matrix over all five statuses `render()` ranks × `run_hit`, in process, each cell proving its headline and advisory first; (2) the presence control; (3) the real `main()` → `render()` path on an over project, `growth_run` 2, seeded history. **RED on `131158cb`: 2 of 3 fail**, at the assertion under test (only the `over` × run-hit cell). Then the fix (`:661-669`). **The fixed baseline through the mutant harness first, then four mutants, each test run by its own name:** literal restored (T1, T3), sentence deleted (all), condition inverted (all), condition widened to `instrument-failed` (T2). All killed; `--selftest` catches none. A docstring claiming the original sentence is *"true of the table"* under `instrument-failed` was corrected before commit, once the edge was run. The upstream `CHANGELOG.md` entry went above `:38`; jargon scan: 0 hits. **`c299c30`** committed, fetched back as a fast-forward `e859196..c299c30`, no remote tracking. **Verified in a fresh `--no-local` clone at `c299c30`, HEAD asserted:** 129 unit tests OK (126 + 3, 2 skipped); `--selftest` 52 PASS, exit 0; the `--force` grep empty; `bin/tests.sh` **141 / 0**; upstream's ratchet `10/10 pass · results 43e25ddcd120 · manifest 97a7aab85b9a`. **`8a0455c`** the plan's status line and *P2 outcome* block (`:372`, the edge `:388`, P3's lines `:403`), BL-80's S211 paragraph (`BACKLOG-DETAIL.md:3074`) and the BL-75/BL-80 index rows; `BACKLOG-DETAIL.md.verify.sh` C1–C5 OK. **Phase 3C:** no fork-learnings row. The lesson (an output keyed on an aggregate inherits how that aggregate's inputs are WRITTEN; grep every assignment, not only the reducer) is code-specific and its defect is P3's decision. It went to agent memory as a second instance of the existing inherited-guards entry. D3's retirement obligation does not arise.
next_steps: **(1) THE PLAN'S P3 — VET AND PACKAGE, ONE SESSION** (`docs/planning/context-budget-status-plan.md:407`). Clone from THIS REPOSITORY'S LOCAL BRANCH: `git clone --no-local --branch fix/context-budget-status /Users/rmsharp/Development/methodology <scratch>`, assert HEAD = `c299c30`, then `git config core.hooksPath .githooks`. Measure on the tip: `python3 starter-kit/quality_ratchet.py --run` (measured here: `tests-sh-passed` 141, `context-budget-unit-tests` 129). **D5:** tighten `.quality-gates.json:10` (139) and `:34` (118) to the measured values, in their own commit, with one upstream `CHANGELOG.md` entry above `:38`. **Overlap, computed, not estimated:** re-fetch the PR refs with `git fetch upstream refs/pull/<n>/head:refs/remotes/upstream/pr<n>` for 83/84/85, then `git merge-tree --write-tree --name-only fix/context-budget-status <head>` for `77afc12`, `e2501c5`, `219fb9d`. On each trial merge that is clean apart from `CHANGELOG.md`, run the unit suite and `bin/tests.sh` on the merge result, **serially**. Draft the PR body to `docs/planning/context-budget-status-pr-body.md` (fork), recognized terms only (no session numbers, BL codes or D-numbers). **Put TWO decisions to the operator in ONE picker:** the body itself, and the status-precedence edge (plan `:388`): list it under the PR's *not changed* as the upstream entry already does, and/or make it its own backlog item. Recommend listing it and opening an item; do not fix it inside this PR. **(2) THEN P4** (outward: a go-ahead on the exact body AND on pushing the branch to `origin`) and **P5**, each its own session. **(3) AT PHASE 0, CHECK #83/#84/#85:** #83 1 comment (ours, `5755256040`), head `219fb9d`; #84 1 comment (ours), head `77afc12`; #85 0 comments, head `e2501c5`. A reply is new work with its own go-ahead. **(4) THE NEXT CLAIM MAKES THREE `HANDOFFS.md` RECEIPTS**: trim (`--cut 2 --force`) and fold right after its Phase 0 report; this has held eleven times. **(5) NO `CHANGELOG.md` TRIM OWED:** 109,060 B before this close-out's entry; about 115 KB after it and the push record (an estimate), roughly 80 KB under the 196,608 B trigger. **(6) STILL OWED, UNCHANGED:** BL-78's closing edits as their own session (`docs/planning/BACKLOG.md:166`, the archive move, `.context-budget.json:101` `files[4]._`); BL-81, BL-79, BL-77 and BL-74 undecided. **CARRIED:** S206's list.
key_files: **Branch `fix/context-budget-status` @ `c299c30` (local only):** `starter-kit/context_budget.py` `measure_file` `:339` (status writes `:366` over, `:427` instrument-failed, `:441`/`:448` over), `render` `:591`, `worst` `:594-605` (`order` `:595`), the advisory `:661-669`, `main`'s ranking comment `:1417-1419`; `tools/test_context_budget.py` `NOT_OVER_YET` `:625`, `TestGrowthRunAdvisory` `:634-705` (tests `:656`, `:671`, `:682`), `TestTokenCeiling` `:706`, witness `:1151`; `CHANGELOG.md:38` (P2's upstream entry, its *Not changed here* bullet names the edge) and `:67` (P1's); `.quality-gates.json:10` and `:34` (the D5 floors). **Fork `main`:** `docs/planning/context-budget-status-plan.md` status `:4-9`, P2 `:359`, P2 outcome `:372`, the edge `:388`, P3's lines `:403`, P3 `:407`, P4 `:425`; `docs/planning/BACKLOG-DETAIL.md:3074` (BL-80's S211 paragraph); `docs/planning/BACKLOG.md:164` (BL-75), `:168` (BL-80); `docs/HANDOFFS_ARCHIVE_INDEX.md:75` (last row = shard **-12**).
gotchas: **(1) THE BRANCH EXISTS ONLY HERE:** `fix/context-budget-status` is a local branch in `/Users/rmsharp/Development/methodology`, at `c299c30`, with no remote. Do not delete it or `git gc --prune`; clone P3 from it. **(2) DO NOT FIX THE STATUS-PRECEDENCE EDGE INSIDE P3 WITHOUT THE OPERATOR.** It is outside D3 and outside the PR's two defects; the fix belongs in `measure_file()`, not the advisory, and widening the advisory's condition to `instrument-failed` is WRONG: mutant 4 shows it would claim a ceiling fired on a pure instrument failure. **(3) `bin/tests.sh`'S COUNT DOES NOT MOVE WITH THE UNIT TESTS:** the unit suite is ONE row there. S210's *"`bin/tests.sh` 141 + k"* was wrong for P2 (unit +3, shell +0). Take both D5 floors from P3's measured table, not from arithmetic. **(4) `render()` ALWAYS EMITS ANSI** (no isatty check), and the advisory wraps mid-sentence: strip the escapes and collapse whitespace before asserting on a sentence. **(5) `unittest -v` WITH SUBTESTS GARBLES PER-TEST PARSING:** the first mutant table here was unreadable; run each test by its dotted name per mutant. **(6) The branch's `:1417-1419` comment says an instrument failure ranks ABOVE `over`; `render()`'s `order` (`:595`) ranks it BELOW.** Pre-existing, part of the edge, unchanged. **(7) A LINE CITED FROM DIFF ARITHMETIC WAS WRONG BY ONE** (`:1418` for `:1417`), caught by a grep before commit; grep every cited line on the tree it names. **(8) `bin/check-handoff` READS EACH FIELD AS ONE LINE.** **(9) The trim's notice set is not fixed:** no CUT_STRADDLES_DAY this time. Read the notices; don't match a remembered list. **(10) `HANDOFFS.md`'s front matter still says `--cut 1 --force`**, wrong through eleven trims now. **`CHANGELOG.md` HAS NO FOLD.**
runtime_smoke: **THE APPLICATION IS THE TOOL, AND IT WAS RUN:** on the branch, in fresh `--no-local` clones with HEAD asserted by sha, through 3 new tests (one of them a subprocess run of `main()`) and three hand-run `--status` fixtures: over with the run fired (new sentence, exit 2), not-over with the run fired (original sentence, exit 1), and the edge (the original sentence above an exceeded-ceiling finding, exit 2). `git status --porcelain --ignored` showed no write from any of them. **Citation, branch clone of `c299c30` (upstream's gates):** `quality_ratchet: 10/10 pass · 0 fail · 0 unmeasured · results 43e25ddcd120 · manifest 97a7aab85b9a`, with `tests-sh-passed` 141 (floor 139) and `context-budget-unit-tests` 129 (floor 118). **Citation, fork `main` clone of `8a0455c`:** `quality_ratchet: 11/11 pass · 0 fail · 0 unmeasured · results 10575dac7361 · manifest 01a4ae7aa511`, identical to Phase 0 since no fork gate moved. Also green: `bash bin/tests.sh` **343 / 0 / 6 skipped** in a clone at `4542465`, after the trim; the trim's `.verify.sh` (L1/L2/L3, exit 0); `BACKLOG-DETAIL.md.verify.sh` C1–C5; `bin/check-links` 111 links / 23 files. **NOT EXERCISED:** trial merges against #83/#84/#85 (P3); a real adopter (P5); how a reader takes the new sentence (the operator, at P3); this close-out commit itself (BL-64).
changelog_ref: CHANGELOG.md "2026-09-21 · [BL-80] S211 claim", the `HANDOFFS.md` trim (written by the trimmer), "[ad hoc] S211 — `HANDOFFS.md`: the trim's pointer block folded into the shard index", "[BL-80] S211 — P2 built: `c299c30` on the LOCAL branch", "[BL-80] S211 — the plan's P2 outcome recorded", and this close-out
commit: 520f972 (claim) + 03194df (HANDOFFS trim) + 4542465 (fold) + c299c30 (P2, local branch fix/context-budget-status) + 8a0455c (plan + backlog) + this close-out
```

