# HANDOFFS.md — archive: 2026-09-15 → 2026-09-16

Retired records from [`HANDOFFS.md`](../../HANDOFFS.md), moved here so the live ledger stays small enough to read
in one pass. Same format, same newest-on-top order — this is the same ledger, continued.

Holds **4 record(s), 2026-09-15 → 2026-09-16**. Cut key: `2026-09-16`. Counts here are computed from the file
itself, never carried forward. This shard is frozen: it states no forward-looking rule,
because the live file owns those and a copy of one was wrong a day after it was written.

---

```handoff
session: S175
date: 2026-09-16
status: complete
self_score: 7
predecessor_score: 8
active_task: **THE FORK RESYNC IS PLANNED, NOT MERGED: `docs/planning/upstream-resync-2026-09-plan.md` (DRAFT) MERGES `upstream/main` `6b29d3d` INTO FORK `main` IN FOUR STAGES OVER TWO SESSIONS, R1 AND R2, AND WAITS ON THE OPERATOR'S D1–D4.** The operator chose "Trim, then resync" after Phase 0, then re-scoped to "Write the merge plan" (picker) once `merge-tree` showed about two dozen conflict hunks in 13 files, with resolutions only the operator can decide. The `HANDOFFS.md` trim and fold are done. Fork-local: nothing pushed, nothing sent upstream.
what_was_done: **`88e40f5`** -- claim, with the two Phase 0 instrument snapshots. **`4679cca`** -- `HANDOFFS.md` trimmed, S173 and S172 to `docs/archive/HANDOFFS-through-2026-09-16-2.md`, 40,816 -> 15,874 B, `--cut 2 --force`: the claim stub was a fourth record, so `--cut 2` removed the same 24,942 B a Phase 0 `--cut 1` would have. Proof exit 0 before and after the commit. **`1f34e75`** -- the pointer block folded into `docs/HANDOFFS_ARCHIVE_INDEX.md` as one 124 B row; front matter 4,475 B inside the trim commit, back to 4,019 B. **`e3bc12c`** -- the plan, with the operator's re-scope decision recorded. It rests on: per-commit `merge-tree` conflict sets that cut four stages at `0fd003a` / `cca7941` / `64f23bf` / `6b29d3d` (4 / 4 / 5 / 0 newly conflicting files); a resolution and a verification for each of the 13 files; the 33 upstream-changed paths split 13 / 6 / 14; upstream's 11 receipts passing the fork's `check-handoff` (`--all`, `--archived`); the merge taking `CHANGELOG.md` over the trimmer's 196,608 B trigger; the ten arriving gates against the fork's measured values; and **a trial of D1's recommendation** (stage 1 merged in a throwaway clone): 297 / 3 / 6 against 300 / 0 / 6, exactly three flips. Two are D1's cost. **The third is not: upstream's `commit-msg` hook refuses Test 27.N1b's trailer-less fixture commit under an agent harness, whatever D1 decides.** A read-only claims review (an opus subagent, ~150 claims) found 15 wrong claims in the draft; each was re-run and corrected before `e3bc12c`. Also found at Phase 0: upstream PR #83 (the maintainer's parallel-sessions plan) conflicts with fork `main` in the same 13 files, so the resync gains no conflicts from it.
next_steps: **DECIDED BY THE OPERATOR AFTER THIS CLOSE-OUT (picker): D1 (A), D2 (a), D3 (a), D4 (a) — every recommendation; the plan is RATIFIED, so R1 is next.** **(1) THE OPERATOR RATIFIES OR REVERSES D1–D4** (`docs/planning/upstream-resync-2026-09-plan.md:187`). One picker, recommendations first: D1 (A) the distributed learnings file becomes upstream's and the fork's rows #15–#66 move verbatim to `docs/FORK_LEARNINGS.md`; D2 (a) adopt the ratchet and tighten it to the fork's values after stage 4; D3 (a) `DASHBOARD_VERSION` 2.18.0; D4 (a) R1 = stage 1, R2 = stages 2–4. If the operator answers after this close-out, record it in a follow-up entry, as S173/S174 did. **(2) R1** (`:285`): pre-flight re-derives §2.1 (`git fetch upstream`; if `upstream/main` moved or #83 merged, add a stage to R2); `git merge --no-ff --no-commit 0fd003a`; resolve rows 1, 3, 5, 10; commit; then Test 27.N1b's one-line override, then D1's follow-through in commits of at most 5 files. **R1's own claim makes 3 receipts; this close-out leaves 2** (`grep -c '^```handoff$' HANDOFFS.md`), so R1's Phase 0 does not trim. The merge brings 8 upstream receipts, so the retention trim is due at R1's end: `--cut 2` keeps R1's stub and the newest fork receipt. **(3) R2** (`:310`), **(4) BL-57's P5** (`changelog-rules-contradictions-plan.md:659`) -- after stage 3 its port may reduce to merging `bl57/changelog-rules`, a prediction for P5. **CARRIED:** BL-61 (header reserve), BL-60 (folds BL-36), BL-54 after P5, `choose_cut`. **No `FRAMEWORK_LEARNINGS.md` row was written:** that file's fate is D1, and it is 2,437 B under its growth warning. The session's learning (an arriving enforcement hook binds the test suite's own fixture commits, not only the session's) goes to whichever file D1 names.
key_files: `docs/planning/upstream-resync-2026-09-plan.md:41` (§2.1 stages and the re-conflict caveat), `:76` (§2.2, the arriving hooks and lint), `:120` (§2.3 the 13 rows), `:141` (§2.4 ledgers), `:166` (§2.5 gates), `:187` (D1–D4), `:216` (D1's trial, the three failures), `:285` (R1), `:310` (R2), `:335` (measured vs predicted); `bin/tests.sh:1281` (Test 27.N1b's commit), `:1211`, `:1692` (tests pointing `core.hooksPath` at the live hooks), `:2561` (`add_row37`'s `max + 1`); `tools/test_methodology_dashboard.py:5359` (the Learning #26 pin); `bin/check-learnings:36` (NOT a fork citation: another project's numbers); `docs/HANDOFFS_ARCHIVE_INDEX.md:51` (the fold rule, moved from `:50` by this session's row); `docs/planning/BACKLOG.md:148` (BL-53), `:152` (BL-57)
gotchas: **(1) A POSITIONAL `--cut N` DEPENDS ON WHEN IT RUNS.** S174 wrote `--cut 1` for a Phase 0 trim; after the claim stub, `--cut 1` would have archived S174's own receipt. Compare the dry run's removed bytes against the intended records (24,942 B both ways here) before `--write`. **(2) A CONFLICT SET COMPUTED AGAINST `main` IS NOT THE SET AGAINST THE PREVIOUS STAGE.** The draft guessed M3 for `.context-budget.json`'s re-conflict; `merge-tree` against the trial's M1 result showed M2. Compute against a committed stage result in a clone. **(3) A `--no-local` CLONE SEES ONLY COMMITS:** commit a resolution before testing it there (the trial did; the draft's R2 order did not). **(4) THE LIVE CHECKOUT FAILS ONE CONTEXT-BUDGET UNIT TEST THAT A CLONE PASSES** (`TestFitGateEndToEnd`, via `bin/tests.sh:267`; it reads this path's session transcripts), and Test 9 failed once on GitHub, then passed standalone and on re-run. Run gates in clones and re-run a lone Test 9 failure; never waive it. **(5) BL-57's P5 REMOVES TEST 27** with the hook's claim carve-out (`changelog-rules-contradictions-plan.md:278`, `:683`), so R1's fix to it should stay one line. **(6) ZSH AGAIN:** `$c:starter-kit/...` is a history modifier (use `${c}:`), `echo =====` is `=cmd` expansion, and `git rev-parse --short` takes one ref -- all three hit this session. A stray `cd docs/planning` also moved the session's working directory.
runtime_smoke: **NO APPLICATION; THE BUILD-EQUIVALENT IS `bash bin/tests.sh`,** run in `--no-local` clones with HEAD asserted equal to the source commit. **`1f34e75` (after trim and fold):** first run exit 1, 299 passed / 1 failed / 6 skipped, the failure Test 9 (`bin/sync --source=github --dry-run`); the same command standalone exit 0; **re-run exit 0, 300 passed / 0 failed / 6 skipped**, matching S174's prediction -- the 6 skips are Test 34's stated rows at 2 receipts. **`e3bc12c` (the plan):** exit 0, **300 passed / 0 failed / 6 skipped**, and a digit-masked diff of its assertion rows against `1f34e75`'s re-run is empty. **Trial (D1 (A), stage 1, throwaway clone `e856970`):** 297 / 3 / 6, and a digit-masked diff against `1f34e75`'s run shows exactly three pass->fail flips. **Other gates on fork `main`:** the shard's `.verify.sh` exit 0 at `4679cca` and after `1f34e75`; `bin/check-handoff --all --allow-pending` exit 0; `bin/check-links` exit 0 (105 links); `bin/check-learnings` exit 0 (65 rows); `methodology_trim.py --file CHANGELOG.md --check`: 165,082 B, does not fire; `context_budget.py --status` exit 2 as at Phase 0, and the one row flip is `HANDOFFS.md` warn -> ok from the trim. No `.quality-gates.json` on fork `main` yet, so no ratchet run is owed. **NOT EXERCISED:** any merge on fork `main`, the ratchet, any push or outward action, CI (none).
changelog_ref: CHANGELOG.md "2026-09-16 · [BL-57] S175 close-out", plus the claim, the trim, the fold, the re-scope decision and the plan entries
commit: 88e40f5 (claim) + 4679cca (trim) + 1f34e75 (fold) + e3bc12c (plan) + this close-out
```

**Self-assessment: 7/10.** Plus: Phase 0 computed where it could have predicted. PR #83's conflict set
and the manifest subset answered the operator's question with commands, not reasoning. I noticed the
claim stub shifts the trim's cut before running it. A failed Test 9 got a standalone reproduction and a
re-run instead of a waiver. When `merge-tree` showed a resync bigger than one session, I put that to
the operator with the precedent (`213f841` carried out a ratified plan) rather than starting to merge.
The trial of D1's recommendation found a failure no decision avoids (the arriving `commit-msg` hook
against Test 27), which R1 would otherwise have met cold. Every reviewer finding was re-run before it was
fixed. **Minus:** the draft carried 15 wrong claims to review. The worst were stage re-conflicts I
guessed at M3 when the trial clone could have computed them, a `--no-renames` count stated without a
grep, merge-result line numbers passed off as fork line numbers, and two DONE criteria that could not
pass as written. My first answer to the operator ("the planned order stays") was right about PR #83 but
did not look at the resync's own size, which changed the plan within the hour. Three zsh traps already in
memory recurred, and a stray `cd` moved the session's directory. **Reduction:** `HANDOFFS.md` 40,816 ->
15,874 B at the trim (warn -> ok). Growth: this receipt, about 9 KB of ledger entries, two backlog-row
additions, and a 31 KB plan outside any mandated read.

**Predecessor (S174): 8/10.** Its item (1) was exact: the command, the fold rule's location, and a
prediction of 300 / 0 / 6 that the re-run matched. Its 13-conflict count held, and its gotchas (2) and
(3), the anchored fence and the `docs/archive/` glob hazard, were both used here. **Not 9:** it wrote
`--cut 1` for a "Phase 0" trim without saying which receipts the cut keeps. Phase 0 is read-only apart
from the ledger backfill, and after the claim the same command would have archived S174's own receipt. It
also framed the resync as one merge session, with no sign of what `213f841`'s precedent and a look at
the hunks showed. The Learning #15/#16 collision (`pr82-review.md:369`) and the arriving hooks were
already on record, and neither reached its gotchas. Its P5 citation (`:655`) is now `:659`. **ROI:
positive.**

```handoff
session: S23
date: 2026-09-16
status: complete
self_score: 8
predecessor_score: 8
active_task: Post-merge verification of main after the operator merged PR #82 at 64f23bf (2026-09-16T18:05:47Z), and the first tightening the S22 receipt records as owed: tests-sh-failed max 1 -> 0 and tests-sh-passed min 138 -> 139 (Test 9 turns green on main). ONE DELIVERABLE (the verified main + one tightening commit). Operator trigger: "merged". COMPLETE.
what_was_done: Pulled main ff-only to 64f23bf (parents 0fd003a + 5c9d3b4; the remote branch was already deleted, the local one deleted here) → claim b4c04af → full matrix on the merged tree AS-IS: bin/tests.sh 139/0 (Test 9 green — the first time since the branch was cut), dashboard unit 226 OK, budget 118 OK, trimmer 123 OK, ratchet 45 OK, check-links OK, check-learnings OK, check-handoff --all OK, twins byte-identical at 2.11.1, context_budget --status exit 0, ratchet --selftest OK (22), commit-msg --selftest OK → tightening fb81c4b (tests-sh-failed 1 -> 0, tests-sh-passed 138 -> 139; the live hook's verdict exit 0, two tightenings; `_first_tightening_owed` rewritten as the discharge record and the standing rule) → --run at the tightened manifest 10/10 → this close-out. Nothing else touched. The claim commit preceded the tightening, so the tightening is the SECOND commit on main after the merge, not the first the S22 receipt predicted — the claim is mandatory (1B); the prediction should have said "first non-claim commit".
next_steps: (a) v3.8 RELEASE PR, S12's procedure — now the only thing between main and a release: README §What's New entry (fold `### Since v3.6 (unreleased)` in), CLAUDE.md §Versioning v3.8 entry (bin/_manifest.py 24 -> 29 rows; DASHBOARD_VERSION 2.10.6 -> 2.11.1; FM count 28 unchanged; Learnings 16 rows with #14 reserved; new distributed tools methodology_trim.py, context_budget.py, quality_ratchet.py; FRAMEWORK_APPARATUS.md + FRAMEWORK_LEARNINGS.md siblings; §Mechanical Gates Bind Every Actor; SAFEGUARDS ratchet row; anti-pattern #10), annotated tag + GitHub Release at the merge commit. CLAUDE.md's token ceiling is a no-growth pin (23,482 of 23,483): the v3.8 §Versioning entry must be PAID FOR BY REDUCTION in CLAUDE.md or the pin re-split by decision — decide before writing it. (b) Refresh the oversight-root scanner /Users/terrell/code/methodology_dashboard.py to 2.11.1 by ONE-FILE copy of starter-kit/methodology_dashboard.py (never --sync; it is at 2.6.1 and prints stale-scale numbers every Orient). (c) Operator rulings still open: .context-budget-history.jsonl tracked-or-ignored (gates the context-budget gate); the "11 reconciled receipts" instruction (no such block has ever been written — drop it or rule that a merging session's receipt is the receipt); the one-time ledger reorder from S19 (four 2026-09-15 entries under six 2026-09-14 ones, now deeper in the file). (d) Follow-ups not built: `measured_blob` self-check in context_budget.py; CI running --run + --precommit against the base branch; rmsharp may still reply on #82's thread. DERIVED: `git rev-list --count origin/main..main` = 0 after this close-out's push (read the command, not this sentence, if in doubt).
key_files: .quality-gates.json (10 gates at main's measured values; `_first_tightening_owed` is now a discharge record); CHANGELOG.md:38 (the S23 entry); HANDOFFS.md:10 (this receipt); README.md §What's New (`### Since v3.6 (unreleased)` still to be folded); CLAUDE.md §Versioning (v3.8 entry owed; 1 token of headroom); .context-budget.json (CLAUDE.md entry: max_tokens 23483 at 2.519 — the pin the release entry must respect); /Users/terrell/code/methodology_dashboard.py (the 2.6.1 oversight copy)
gotchas: (1) The ratchet is live on main with the tightened manifest: any commit that lowers tests-sh-passed below 139, raises tests-sh-failed above 0, or removes/empties the manifest is refused; a red bin/tests.sh on main is now a red GATE, not a note. (2) CLAUDE.md has ONE token of headroom (23,482 / 23,483) — the release entry cannot be appended without a reduction or a re-pin; `context_budget.py --precommit` is run by hand here (not in the hook), so a session that skips it will not be refused and will silently break the pin. (3) `--run` takes ~2 m 50 s; `context_budget.py --status` writes .context-budget-history.jsonl (not ignored) — remove before commit. (4) Test 9 (`--source=github`) reads GitHub's main: it is green now and will go red again on any branch that adds a new distributed file until that branch merges — by construction, not a defect. (5) Two-table trap (S12 gotcha 2) unchanged. (6) Learnings: none appended this session — nothing framework-level was learned that #16 does not already say; stated rather than left unsaid (Degradation row, FM #28).
runtime_smoke: quality_ratchet: 10/10 pass · 0 fail · 0 unmeasured · results 6542e640a956 · manifest 97a7aab85b9a — run at fb81c4b's tree, after the tightening (2 m 50 s). The merged tree as-is verified separately before the edit: bin/tests.sh 139/0 and every suite/check itemized under what_was_done. The merge itself read back from the GitHub API (state MERGED, 2026-09-16T18:05:47Z, merge commit 64f23bf), not inferred from the pull.
changelog_ref: CHANGELOG.md "2026-09-16 · [ad hoc] PR #82 merged — post-merge verification on main and the first tightening" — written at claim, completed with the verification results and the run
commit: b4c04af (claim) + fb81c4b (tightening) + this commit (close-out)
```

```handoff
session: S22
date: 2026-09-16
status: complete
self_score: 8
predecessor_score: 8
active_task: Build the PR #82 review-response fix set on feat/quality-ratchet, in the order posted in the maintainer's confirmation comment (2026-09-16): (1) the deletion hole — find_root via git, precommit refuses removal and compares against the newest PARSEABLE committed manifest, the hook fires when HEAD has a manifest, install-hook writes the real path; (2) the dashboard — history walk when the file is absent, deleted/unparseable as an empty gate list, direction flips and command changes reported, the name-keyed coverage bonus dropped; (3) tests-sh-failed max 1; (4) docs — Phase 0 step 6 gate-citation sub-step paid by reduction, the merge/rebase clause, the enforcement-point sentence; (5) both read-set densities re-measured and CLAUDE.md's token ceiling; (6) tighten to measured, --run, the PR description on rmsharp's rewording. OPERATOR-DIRECTED BUNDLING across subsystems (the S8 shape, not a slice) — recorded as such; one independently verified checkpoint commit per fix, RED-first for every refusal. OUT: the context-budget gate (the history-file decision is the operator's) and the merge. Operator trigger: "go". COMPLETE — all six built, pushed, PR body rewritten; NOT merged.
what_was_done: Merge b2aea23 (origin/main → branch, both ledgers a union: S21 above S20 above S19) → claim 563a6e2 → F1 b742344 (quality_ratchet.py 1.0.0 → 1.1.0: find_root falls back to the git toplevel; newest_committed() = the newest parseable committed manifest THAT DECLARES A GATE is the comparison base; precommit refuses "manifest removed"; install_hook writes the relpath of the running copy; .githooks/pre-commit fires when HEAD has a manifest; unit 33 → 43 with 8 RED first, selftest 17 → 22, tests.sh +4 through real git) → F1b e5a5dda (the base note fired on every ordinary commit — `git log -- file` names the newest commit that touched it, not HEAD; seen in F1's own hook output; unit 44) → F2 ddcbf29 (dashboard 2.11.1 both twins: history walk while the manifest stays deleted, deleted/unparseable versions as flagged empty gate lists, each version compared to the nearest older version with gates so 80 → deleted → 1 reads 80 → 1, direction flips as loosenings, command/extract edits as LOW advisories, coverage-name +2 dropped; unit 222 → 226, 6 RED first; fleet exposure 0 — no sibling has a manifest history) → F3 ee73bbb (tests-sh-failed max 1, manifest 9 → 10; run_gates memoizes a command two gates share; unit 45) → F4 df926b6 (runner Phase 0 step 6 gains the receipt-citation check, PAID BY REDUCTION: 18,897.5 → 18,865.5 tokens by the doubled-file Read, 53,328 → 53,252 B; SAFEGUARDS ratchet row: manifest removal refused, merge/rebase skip, 6,029.7 of 6,100; ITERATIVE_METHODOLOGY §Mechanical Gates states the enforcement point; BOOTSTRAP Step 10 the two limits) → F5 5f3f874 (.context-budget.json: runner 2.8227 on blob 2a3e410d, SAFEGUARDS 2.8234 on 933816b4, CLAUDE.md max_tokens 23,483 at 2.519 on 1244e95b; tool now within one token of every measurement) → F6 b5dda4e (tests-sh-passed 138, dashboard 226, ratchet 45 — three tightenings; --run 10/10) → pushed → PR #82 body replaced with rmsharp's §1 rewording plus the caveats, decisions in plain words, and a "Review findings and what changed" section (read back: 9,876 chars, head b5dda4e) → Learning #16 appended (1,137 B) → this close-out. THE TWO DEFECTS THE REVIEW DID NOT HAVE: a bypassed EMPTY manifest as a comparison base (found by the tests.sh flow, whose pre-existing `git checkout -- file` had restored a still-staged refused edit), and F1b's spurious note.
next_steps: (a) WAIT FOR rmsharp on the thread (the PR body now says what changed); if they push, RE-CHECK THE HEAD SHA (S524 Learning #67) — head is b5dda4e plus this close-out commit. (b) MERGE when the operator says so: `git merge-tree --write-tree --name-only origin/main origin/feat/quality-ratchet` at this close-out — DERIVED, exit code read, not the output: see the ledger entry for the result. After merge, FIRST commit on main: tests-sh-failed max 1 → 0 and tests-sh-passed 138 → 139 (Test 9 turns green) — tightenings, no approval. (c) v3.8 release PR (S12's procedure): manifest 24 → 29, DASHBOARD_VERSION 2.10.6 → 2.11.1, FM 28, Learnings 16 with #14 reserved, two new distributed tools + the apparatus/learnings siblings. (d) Follow-ups NOT built: a `measured_blob` key context_budget.py checks itself (F5 records blobs in prose only); the context-budget gate after the .context-budget-history.jsonl tracked-or-ignored decision; CI running --run + --precommit against the base branch (the only thing that makes "every actor" literal); the one-time ledger reorder from S19; the "11 reconciled receipts" instruction still needs the operator's ruling (no such block has ever been written). (e) Oversight-root scanner refresh to 2.11.1 by ONE-FILE copy after merge.
key_files: starter-kit/quality_ratchet.py:61 (find_root), :83 (newest_committed — the base rule), :199 (precommit's removal branch), :233 (measure_gate memo), :345 (hook_text / install_hook relpath); .githooks/pre-commit:35 (the HEAD-or-worktree condition); tools/methodology_dashboard.py:1938 (_gate_loosenings incl. flips), :1965 (_gate_command_changes), :1980 (_gate_manifest_history: deleted/unreadable as empty), :2010 (collect_gate_metrics: walk when history exists), :2040 (_fold_history: nearest older version with gates); tools/test_quality_ratchet.py TestPrecommitThroughGit + TestFindRootAndHookPath; tools/test_methodology_dashboard.py TestQualityGateSignals (deletion, net loosening, flip, command, no-bonus); .quality-gates.json (10 gates; tests-sh-failed); .context-budget.json (three measured densities with their blobs); starter-kit/SESSION_RUNNER.md:18 (Phase 0 step 6, the citation check); starter-kit/SAFEGUARDS.md:54 (the ratchet row); ITERATIVE_METHODOLOGY.md:412 (the enforcement-point paragraph); starter-kit/FRAMEWORK_LEARNINGS.md row 16; PR #82 body (rewritten) — line numbers at b5dda4e
gotchas: (1) THE RATCHET IS LIVE HERE with the new base rule: a commit that removes or empties .quality-gates.json is refused; tightening/adding pass; the hook runs the WORKING-TREE tool, so a broken edit to quality_ratchet.py breaks your own commit — test tool edits in a scratch repo first. (2) `git checkout -- <file>` restores from the INDEX; after a refused staged edit use `git checkout HEAD -- <file>` (this bit bin/tests.sh for one release). (3) `--run` is ~2 m 46 s and writes .quality-gates-results.json (gitignored on this branch, not on main); `context_budget.py --status` writes .context-budget-history.jsonl (not ignored) — remove before committing. (4) Densities are tied to blobs: re-measure SESSION_RUNNER.md (2a3e410d), SAFEGUARDS.md (933816b4), CLAUDE.md (1244e95b) when `git rev-parse HEAD:<path>` changes — the tool does not check this itself yet. (5) The runner has 34.5 real tokens of headroom, SAFEGUARDS 70; the pair is at 99.58 % of the read cap. (6) zsh: `${r}:path` in `git cat-file` loops. (7) Test 9 fails on the branch by construction until merge.
runtime_smoke: quality_ratchet: 10/10 pass · 0 fail · 0 unmeasured · results efccbc7f2195 · manifest 08423c179055 — run at b5dda4e (2 m 46 s, bin/tests.sh executed once for its two gates); `--selftest` OK (22 checks); the live hook observed refusing nothing this session (every staged manifest edit was a tightening or an add) and the removal refusal observed in scratch repos through real git (tests.sh +4, selftest +5, unit +12). Build-equivalent at EVERY checkpoint: bin/tests.sh 138/1 (Test 9 by construction), dashboard unit 226 OK, budget 118 OK (2 skipped), trimmer 123 OK, ratchet 45 OK, check-links OK, check-learnings OK (15 rows, #14 reserved, after row 16), check-handoff --all OK, twins byte-identical at 2.11.1, context_budget --status and --precommit exit 0. Token measurements by the Read-tool refusal: runner 37,731/2, SAFEGUARDS 42,208/7, pair 49,789/2. The PR body edit read back through the API.
changelog_ref: CHANGELOG.md "2026-09-16 · [ad hoc] PR #82 review-response fixes — the six corrections and the review's accepted findings, one checkpoint per fix" — written at claim, one bullet per checkpoint, completed here with the PR-body edit and the merge-tree result
commit: b2aea23 (merge) + 563a6e2 (claim) + b742344 e5a5dda ddcbf29 ee73bbb df926b6 5f3f874 b5dda4e (checkpoints) + this commit (close-out)
```

```handoff
session: S20
date: 2026-09-15
status: complete
self_score: 8
predecessor_score: 8
active_task: Make the quality-ratchet plan real (docs/planning/quality-ratchet-plan.md, PR #81) and open a PR for review. Operator trigger: "back to 81. Make it a reality and post a PR for review." ONE DELIVERABLE as a PRE-DECLARED VERTICAL SLICE — one capability (declared thresholds that bind every actor and only tighten) through the plan's four buildable layers, 11 checkpoint commits, the full matrix at each boundary. Plan Phases 5 (adopter dogfood, N sessions) and 6 (release) are OUT by the plan's own text. COMPLETE — branch feat/quality-ratchet pushed and the PR opened; NOT merged (the operator reviews).
what_was_done: Claim 5cd300e → P0 008d656 (calibrate tests skip below the fit minimum; read-set partition re-split 18,900 + 6,100 and held by a test — G1) → P1 628d218 (D3 SAFEGUARDS row; D4 §Mechanical Gates Bind Every Actor; D5 Phase 3C mechanical branch; D8 FM #17 clause + Degradation row, FMs 1–28 byte-unchanged; Learning #15) → P2a 9d34485 (starter-kit/quality_ratchet.py, 494 lines, --precommit/--run/--status/--selftest/install-hook, no --force; empty seed; 33 unit tests) → P2b 727d9ff (manifest 27 → 29; both scanner twins exclude the installed files — 6 guard tests went RED on the rows alone) → P2c d433739 (tests.sh 116 → 128 incl. the hook proven through real git commits, RED-first; BOOTSTRAP Step 10 Recommended + per-stack table; results file gitignored) → P3 cca7941 (dashboard 2.11.0: gate metrics, advisory risks, git-only loosening history, measured-coverage points; 11 tests RED against 2.10.7; 222 OK; 27-repo fleet delta 0) → P4a bae6b05 (DEVELOPMENT + AUDIT #10; HANDOFFS spec citation form; check-handoff D9 lint, newest-only) → P4b 58babe6 (this repo's 9 gates at measured values, 9/9 pass; ratchet chained into .githooks/pre-commit and observed refusing a live 127 → 100) → P4c 04044f1 (completeness sweep: README/CLAUDE/T1/HOW_TO_USE) → self-review b4226d3 + d24fb2c (gate run named in 3E/Phase 6/HOW_TO_USE; CLAUDE.md back under its pin by reduction) → this close-out. Every checkpoint: bin/tests.sh, check-links, check-learnings, unit suites; the Phase 0 pair re-measured by the doubled-file Read at P1 (24,822) and after the trims (24,842 tokens, 99.4 % of the cap).
next_steps: (1) OPERATOR REVIEW of the PR — the §8 decisions were taken at the plan's own recommendations and are listed in the PR body to reverse: 1 principle-level section YES; 2 results file gitignored; 3 amend FM #17, no FM #29; 4 empty seed; 5 separate .quality-gates.json; 7 delivery/acceptance stays a separate plan; 6 (ask CampusIQ) not a PR matter. (2) After merge, FIRST commit on main: add the owed `tests-sh-failed` (max 0) gate to .quality-gates.json (Test 9 turns green on main) and raise `tests-sh-passed` to 135 — a tightening, no approval needed; then re-run and cite. (3) Plan Phase 5: pick ONE adopter with a real suite (naudio 98 / rad-con 90 / wsjtx-arm 89 on the fleet scan), declare 3–5 gates at measured values, record the three counts for five sessions. (4) Plan Phase 6: v3.8 release PR — this slice + PR #80 together (manifest 24 → 29, DASHBOARD_VERSION 2.10.6 → 2.11.0, FM count 28, Learnings 15 with #14 reserved, two new distributed tools, the apparatus/learnings siblings); follow S12's procedure. (5) Still owed from S19: the 11 reconciled receipts for #80's commits, the one-time ledger reorder, `.context-budget-history.jsonl` tracked-or-ignored, the one-file oversight scanner refresh (now to 2.11.0). DERIVED: `git merge-tree --write-tree --name-only origin/main HEAD` at close-out returned a tree id with no conflict block (main has not moved since the branch point 8b4dc2c).
key_files: starter-kit/quality_ratchet.py:134 (compare — the ratchet's rules), :175 (precommit), :218 (measure_gate: no extract → exit code; no command → unmeasured), :425 (selftest); starter-kit/quality-gates.json (seed, empty); .quality-gates.json:1 (this repo's 9 gates; `_first_tightening_owed`); .githooks/pre-commit:26 (the chain); tools/methodology_dashboard.py:1893 (collect_gate_metrics) and the risks block in assess_risks; tools/test_methodology_dashboard.py TestQualityGateSignals; tools/test_quality_ratchet.py; bin/check-handoff:97 (GATE_CITATION_RE), validate_gate_citation; ITERATIVE_METHODOLOGY.md:401 (§Mechanical Gates Bind Every Actor); starter-kit/SESSION_RUNNER.md:319 (FM #17 clause), the Degradation row after FM #28's, 3C mechanical bullet, 3E line; starter-kit/SAFEGUARDS.md:54 (the D3 row); starter-kit/FRAMEWORK_LEARNINGS.md row 15; starter-kit/BOOTSTRAP.md Step 10; .context-budget.json (re-split 18,900 + 6,100; TestThisRepoReadSetPartition holds the sum); docs/planning/quality-ratchet-plan.md (the contract)
gotchas: (1) The ratchet is LIVE in this repo's hook: any commit that lowers a `min`, raises a `max`, or removes a gate in .quality-gates.json is refused; `--no-verify` is recorded, not exempt. Tightening/adding pass. (2) bin/check-handoff now demands the `quality_ratchet: N/M pass …` token in the NEWEST complete receipt (this one carries it); run `python3 starter-kit/quality_ratchet.py --run` (~3 min, it runs bin/tests.sh) before writing a receipt, and do NOT put --run inside bin/tests.sh (recursion). (3) The Phase 0 pair is at 99.4 % of the read cap and the runner has 22 tokens of headroom: the next runner byte must be paid for by a reduction — that is the rule, not a bug. CLAUDE.md is 15 B under its pin for the same reason. (4) Test 9 fails on this branch by construction (`--source=github` reads main, which lacks starter-kit/quality_ratchet.py) — the #80 shape; it passes after merge, which is why `tests-sh-failed` is not yet declared. (5) `--status` on context_budget.py writes an untracked .context-budget-history.jsonl (S19 gotcha) — removed before every commit here. (6) The dashboard's "N declared gates, never run here" advisory will show on the canonical repo in any fresh clone (results are gitignored) — by design (D6); the portfolio scan excludes methodology/ anyway.
runtime_smoke: quality_ratchet: 9/9 pass · 0 fail · 0 unmeasured · results 74c773523dab · manifest 2424c429b2c6 — run at d24fb2c; plus `--selftest` OK (17 checks) and the hook observed refusing a live loosening in this repo (P4b). Build-equivalent at every checkpoint: bin/tests.sh 134/1 (Test 9 by construction), dashboard unit 222 OK, budget 118 OK (2 skipped, honestly), trimmer 123 OK, ratchet 33 OK, check-links 107 OK, check-learnings OK (14 rows, #14 reserved), commit-msg --selftest OK, twins byte-identical at 2.11.0, context_budget --status OK.
changelog_ref: CHANGELOG.md "2026-09-15 · [ad hoc] Quality ratchet — the plan's Phases 1–4 built as one pre-declared vertical slice" — written at claim, one bullet per checkpoint, completed here with the PR number
commit: 5cd300e (claim) + 11 checkpoints 008d656..d24fb2c + e13958d (close-out) + this commit (PR #82 recorded — https://github.com/KJ5HST/methodology/pull/82)
```

