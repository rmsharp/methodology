# Handoff Receipts — durable close-out proof

This repository dogfoods its own methodology: every session records a durable, machine-checkable
`handoff` receipt here at close-out (Phase 3D), and Phase 0 reconciles it against `git log`. See
[`starter-kit/HANDOFFS.md`](starter-kit/HANDOFFS.md) for the block format and the write points, and
`bin/check-handoff` for the checker. Newest on top; prepend-only.

**Older receipts are archived.** This file currently holds **3** (set by the S94 trim, 2026-08-16 —
recount with `grep -c '^```handoff' HANDOFFS.md` rather than trusting this number); the oldest **19**
(2026-07-08 → 2026-07-30) live in [`docs/archive/HANDOFFS-archive.md`](docs/archive/HANDOFFS-archive.md),
same format, same newest-on-top order. Archiving is safe by construction: `bin/check-handoff`
validates only the newest receipt, and Phase 0 reconcile is frontier-based, so neither reads past the
top of this file. `bin/model-report --handoffs <archive>` reaches the older prose when you need it.
Archive again — a new file, same format — when this one approaches ~1,200 lines, the trigger the
action ledger already uses.

**Two session sequences share this ledger and their numbers collide.** This fork and
`upstream/main` each run their own `S<N>` counter, so a receipt is identified by **session + date**,
never by number alone: upstream's **S7**/**S8** (both 2026-08-01) are different sessions from the
fork's **S7** (2026-07-09) / **S8** (2026-07-13) in the archive. At a resync the two sequences stay
separate and unrenumbered, each incoming receipt is checked against ours before it is kept, and
within a shared date the fork's receipts precede the arriving upstream ones (precedent: `fc4d297`).

> **The count above drifts between trims.** `methodology_trim.py` declares it a regenerated field
> (`starter-kit/methodology_trim.py`, the `HANDOFFS.md` `LedgerSpec`), so a **trim** rewrites it and
> the proof's L2 clause excuses that one span — but nothing updates it when a session **prepends** a
> receipt, which is most sessions. So it is right immediately after a trim and wrong from the next
> close-out onward: it read **6** from `7a71df0` until corrected three sessions later. That is
> [Learning #12](starter-kit/FRAMEWORK_LEARNINGS.md) pointed at this file, and it is the receipt-ledger
> half of upstream [issue #65](https://github.com/KJ5HST/methodology/issues/65). Recount before
> trusting it.

> **⚠ A trim of this file must retain at least THREE receipts — the tool's default cut does not
> know that.** `bin/tests.sh` Test 34 mutates *this* ledger to check `check-handoff --all`'s
> whole-ledger invariants, and reads its two mutation anchors as `ids[1]`/`ids[2]` of the live file
> (`bin/tests.sh:2103`) — deliberately not hardcoded, so it survives *which* receipts rotate, but it
> needs three to exist. S94's first attempt took the budget-driven default, retained **2**, and every
> mutation matched nothing: five assertions reported `mutation was vacuous` and the suite went
> 235/1 → 229/6. Pass `--cut 3` (or more) explicitly, and re-run `bash bin/tests.sh` after any trim
> of this file. **This is avoided here, not fixed** — the durable fix is a retained-records floor in
> the distributed trimmer or a rewrite of Test 34's anchor selection; see `docs/planning/BACKLOG.md`.

**Archived 16 record(s), 2026-07-30 → 2026-08-02** into [`docs/archive/HANDOFFS-through-2026-08-02.md`](docs/archive/HANDOFFS-through-2026-08-02.md) — same format, same order, frozen.
Losslessness is proved by [`docs/archive/HANDOFFS-through-2026-08-02.md.verify.sh`](docs/archive/HANDOFFS-through-2026-08-02.md.verify.sh), which re-derives L1/L2/L3 from git; run it rather
than trusting this sentence. Written by `methodology_trim.py` v1.1.1.

**Archived 30 record(s), 2026-08-03 → 2026-08-09** into [`docs/archive/HANDOFFS-through-2026-08-09.md`](docs/archive/HANDOFFS-through-2026-08-09.md) — same format, same order, frozen.
Losslessness is proved by [`docs/archive/HANDOFFS-through-2026-08-09.md.verify.sh`](docs/archive/HANDOFFS-through-2026-08-09.md.verify.sh), which re-derives L1/L2/L3 from git; run it rather
than trusting this sentence. Written by `methodology_trim.py` v1.1.1.

**Archived 25 record(s), 2026-08-02 → 2026-08-11** into [`docs/archive/HANDOFFS-through-2026-08-11.md`](docs/archive/HANDOFFS-through-2026-08-11.md) — same format, same order, frozen.
Losslessness is proved by [`docs/archive/HANDOFFS-through-2026-08-11.md.verify.sh`](docs/archive/HANDOFFS-through-2026-08-11.md.verify.sh), which re-derives L1/L2/L3 from git; run it rather
than trusting this sentence. Written by `methodology_trim.py` v1.1.3.

**Archived 8 record(s), 2026-08-11 → 2026-08-15** into [`docs/archive/HANDOFFS-through-2026-08-15.md`](docs/archive/HANDOFFS-through-2026-08-15.md) — same format, same order, frozen.
Losslessness is proved by [`docs/archive/HANDOFFS-through-2026-08-15.md.verify.sh`](docs/archive/HANDOFFS-through-2026-08-15.md.verify.sh), which re-derives L1/L2/L3 from git; run it rather
than trusting this sentence. Written by `methodology_trim.py` v1.2.0.

```handoff
session: S95
date: 2026-08-17
status: complete
self_score: 8
predecessor_score: 9
active_task: Trim `CHANGELOG.md` back under its 65,536 B ceiling (83,860 B at claim, over by 18,324) with a losslessness proof that PASSES. COMPLETE -- 84,765 B (at trim, after the claim record) -> 31,539 B (-62.8%), and `docs/archive/CHANGELOG-through-2026-08-15.md.verify.sh` PASSES. The cut clears not just the 65,536 B ceiling but the seed's stated stop condition of <= 1/2 x budget (32,768 B), which S94 did not mention and which the tool's default targets. The FM #28 gate now reads `CHANGELOG.md` **ok**; `docs/planning/BACKLOG.md` (104,530 B) is the only remaining breach and was NOT touched (FM #17). CARVE-OUT HELD EXACTLY AS DECLARED, INCLUDING ITS PRE-DECLARED EXCEPTION: 26 manifest SOURCE rows vs 7 changed files, exactly ONE distributed (`starter-kit/FRAMEWORK_LEARNINGS.md`, the Phase 3C row), both populations asserted non-empty. NO OUTWARD-FACING ACTION: no PR, no push, no issue, no comment, no tag. PRE-DECLARED COMMIT SHAPE, because it is the load-bearing part and declaring it at close-out would be worthless: claim -> record the claim -> **trim ALONE** -> close-out. A trim commit that also finalizes its own receipt EDITS a record that existed at TRIM^, whose pre-trim bytes then exist nowhere; that is why 4 of this repo's 7 shipped proofs are red, and why S94's standalone trim produced the first green one. PRE-DECLARED CUT: BY COUNT, never by date -- `--cut N` is the number of records to RETAIN (`starter-kit/methodology_trim.py:858` `_explicit_retain`), and this ledger is NOT date-monotonic (2026-08-16 x4, 08-15 x1, 08-16 x1, 08-15 x31, 08-12 x3), so no date key names the boundary. I intend to take the tool's budget-driven DEFAULT (retain 12 of 40 -> 30,634 B, satisfying the seed's stated stop condition of <= 1/2 x budget = 32,768 B) rather than override it -- but only because I audited the couplings FIRST and the default clears every floor they impose. FLOORS MEASURED BEFORE CUTTING, not after: `bin/tests.sh:1886` (Test 30) requires a NON-EMPTY `**Model:**` population in the live file and the only surviving bullets are in records #4 and #13, so N >= 4; `.context-budget.json`'s structure guard requires >= 5 records. Every achievable cut STRADDLES a calendar day (the tool says so itself, `CUT_STRADDLES_DAY`); the only non-straddling seam is 74,753 B, over budget. CARVE-OUT: zero distributed files, EXCEPT `starter-kit/FRAMEWORK_LEARNINGS.md` if Phase 3C yields a framework learning -- that is a MANDATORY close-out step whose only home is a distributed file, and the exception is declared NOW because S94 declared "zero distributed files" without checking it against Phase 3C and then broke it. NO OUTWARD-FACING ACTION: no PR, no push, no issue, no comment, no tag.
what_was_done: Four commits -- `a35a14f` (1B claim), `c1382e0` (record the claim, forced by the trimmer's P1 guard), `c3d68c5` (THE TRIM ALONE), and this close-out. **(1) THE PRECONDITION S94 NAMED AS OWED WAS THE ACTUAL WORK, AND IT WAS NOT EMPTY.** S94's `next_steps` said it had NOT audited the suite for `CHANGELOG.md` fixture couplings and that assuming none was unsafe. There are SIX readers of the live file that are not human. Two are safe only because someone already hardened them: `bin/tests.sh:1751` (Test 29) counts Reconcile-on-read entries across live **+** archives since S87 -- keyed on the live file alone, a correct archive once turned it red -- and `:1788` reads the front-matter norm paragraph, which lives in the header zone a trim preserves. One is **BINDING**: `:1886` (Test 30) requires a NON-EMPTY `**Model:**` population in the live file, and the only carriers were records #4 and #13 of 41, an unstated floor of N >= 4. `:1957` (Test 31) asserts an EQUALITY between the tool's count and a raw grep, so it moves with the cut rather than constraining it (16 -> 2, still green). `tools/test_methodology_trim.py:121` asserts a phrase is absent from the live FOOTER and present in the live+archives CORPUS -- both still true. `.context-budget.json`'s structure guard wants >= 5 records. **(2) ONE READER CANNOT BREAK BY CONSTRUCTION AND IT IS WORTH NAMING:** `bin/check-handoff`'s `changelog_ref` rule is a PROHIBITION on positional addresses, not a resolution check -- which is exactly why the field quotes headings. Archiving cannot invalidate it. **(3) I TOOK THE TOOL'S DEFAULT -- THE OPPOSITE DECISION FROM S94's, BY THE SAME METHOD.** S94 had to override the default because a coupling imposed a floor of 3 receipts; here the default retained 13 and cleared every floor found (N >= 4 content, >= 5 count), so overriding it would have been unjustified fiddling. The method is the audit, not the verdict. **(4) THE COMMIT SHAPE WAS DECLARED AT CLAIM AND HELD.** The trim commit contains the trim and nothing else; this receipt stayed `status: pending` across it. The frozen proof reads all three artifacts from `c3d68c5` and reports `41 before = 13 retained + 28 archived; added by the trim commit: 1` -- the trimmer's own ledger entry, which v1.2.0 excuses as ADDED -- and passes. **(5) `CUT_STRADDLES_DAY` WAS PROVEN UNAVOIDABLE, NOT SHRUGGED OFF.** Date sequence is `08-16 x4, 08-15 x1, 08-16 x1, 08-15 x31, 08-12 x3`; both achievable seams straddle and the only clean one sits at 74,753 B, over the ceiling. **(6) LEARNING #32 APPENDED** (31 rows, `#14` still reserved).
next_steps: (a) **`docs/planning/BACKLOG.md` IS THE LAST BREACH AND IT IS NOT A TRIM JOB.** 104,530 B against 65,536 B, untouched by me and by S94. S89 measured that its residual excess is LIVE WORK, not housekeeping, so reducing it is an operator policy call about what to abandon -- not something a session should quietly archive. Ask before treating it as the next trim. (b) **THE CONTENT FLOOR I CLEARED IS STILL UNGUARDED, AND I ONLY WARNED ABOUT IT.** Test 30's `**Model:**` population now stands at **2** carriers in the live file, one of which is this session's own claim entry. A future trim that retains only the newest few records empties it and turns `:1886` red, with no count check able to see it coming. Learning #32 and this receipt are a warning, not a guard; the durable fix is the same shape as BL-40's -- assert the population and SKIP WITH A STATED REASON, or teach the trimmer a content floor. NOT RAISED AS A BL ITEM, deliberately: `BACKLOG.md` is over its ceiling and adding to it is the wrong direction. Raise it if you disagree. (c) **BL-40 IS STILL THE UNDONE DURABLE FIX** from S94 -- Test 34's 3-receipt floor on `HANDOFFS.md`. Unchanged, untested, still an "untested lean" per S94's own labelling. (d) **BL-36's RESIDUAL IS UNCHANGED**: this repo now ships **8** proofs, **4 passing**. My new one is the 4th green; the 4 reds are the same frozen artifacts, still needing their own go-ahead to regenerate. (e) **`starter-kit/FRAMEWORK_LEARNINGS.md` IS AT 57,964 / 60,000 B -- 2,036 B OF HEADROOM, AND MY ROW COST 2,771 B.** The next learning row of comparable size BREACHES its ceiling. This is computed, not estimated; it is the next size problem after `BACKLOG.md` and nobody has flagged it before. (f) The `CHANGELOG.md` 2026-08 ordering defect S93 and S94 both reported is GONE as a side effect -- the out-of-order 2026-08-15/08-16 pair was in the archived span. I did not fix it; the trim moved it. Do not credit it as repaired. (g) `upstream/main` was 1 ahead (`512c2ed`) at my Phase 0 with `git merge-tree --write-tree --name-only HEAD upstream/main` conflicting in exactly `HANDOFFS.md`; I wrote to that file and to `CHANGELOG.md`, so RE-DERIVE rather than trusting this line. (h) BL-37 half (b), BL-39, and the long tail BL-11/12/13/14/16/17/18/19/20-residual/21/22/23/26/30/31/32 unchanged, NOT re-verified.
key_files: `CHANGELOG.md` (84,765 -> 31,539 B at the trim; 34,803 B after this close-out's entries, 16 records) -- `:158` the new archive pointer paragraph, `:166` this session's close-out entry, `:212` the trimmer's own trim record, `:220` the claim record that P1 forced; `docs/archive/CHANGELOG-through-2026-08-15.md` (55,100 B, 28 records) and its `.verify.sh` -- the shard and THE PROOF THAT PASSES; `bin/tests.sh:1751` (Test 29, reads live+archives), `:1788` (front-matter norm paragraph), `:1886` (**THE BINDING CONTENT FLOOR** -- non-empty `**Model:**` population), `:1957` (Test 31, an equality not a level); `tools/test_methodology_trim.py:121` (`HEAD:CHANGELOG.md` footer + corpus assertions); `starter-kit/methodology_trim.py:858` (`_explicit_retain` -- `--cut N` is records to RETAIN; S94 cited `:863`, the same function, and `--help` still does not say); `.context-budget.json` (the `>= 5` record structure guard); `starter-kit/FRAMEWORK_LEARNINGS.md:54` (Learning #32 -- the ONE distributed file, pre-declared).
gotchas: **A COUNT FLOOR AND A CONTENT FLOOR ARE FOUND BY DIFFERENT QUESTIONS, AND THE TOOL REPORTS ONLY THE FORMER.** The dry run prints bytes and record counts. Test 30's floor is *which* records survive, not how many -- invisible to every number the tool shows you. Simulate the partition (`retained = lines[:idx[N]]`) and re-run the predicate over the retained half BEFORE writing. **THE FLOOR CLEARED BY POSITIONAL ACCIDENT.** 2 carriers survive only because they sat near the top of a newest-first ledger; the same command on differently-laid-out data goes red. Do not read "the default was fine" as "the default is safe." **`--cut <date>` IS UNSAFE ON THIS LEDGER** -- it is not date-monotonic, so no date key names a real boundary; use a COUNT. **I TRUNCATED MY OWN BASELINE AND HAD TO RE-RUN THE SUITE.** `bash bin/tests.sh 2>&1 | tail -60` in the background captured 51 of 236 rows -- enough for the summary line, useless for the row-for-row comparison that makes a regression diagnosable in one step. Redirect to a file; never pipe a population through `tail`. **MY FIRST CARVE-OUT CHECK WAS WRONG IN THE SAFE DIRECTION AND I ALMOST PUBLISHED IT.** I scraped `bin/_manifest.py` for anything string-like and collected both SOURCES and DESTINATIONS, so root `CHANGELOG.md`/`HANDOFFS.md` -- this repo's own ledgers, whose seeds are `starter-kit/CHANGELOG.md`/`starter-kit/HANDOFFS.md` -- appeared as "distributed files touched." Read `m.DISTRIBUTION` and take `src` only. **DO NOT `git reset --hard` IN THIS REPO** -- `.context-budget-history.jsonl` and `dashboard_history.jsonl` are tracked, dirty, inherited, owned by no protocol step, and appended to merely by orienting. Still unstaged, still untouched by me. **`bin/check-*` ARE PYTHON, NOT SHELL.** **`check-learnings` READS "31 row(s), contiguous 1..31" WHILE THE MAXIMUM ROW IS 32** -- `#14` is reserved, so count and max differ by one and "1..N" is the COUNT. Pre-existing; do not "repair" it.
runtime_smoke: **THREE SURFACES, AND WHAT EACH CANNOT ENFORCE.** This repo ships no application; the build-equivalent is the suites plus the tools, RUN. All figures measured after the final non-receipt edit and BEFORE this receipt was written, so the `HANDOFFS.md` number understates by roughly this block. **SURFACE 1 -- the canonical checkout.** `bash bin/tests.sh` **235 passed / 1 failed**, compared ROW-FOR-ROW against a full pre-change baseline captured at this session's Phase 0, both populations asserted non-empty (236 rows each): **ZERO rows lost, THREE rows changed, every one explained** -- the historical claim-commit replay counter 87 -> 88 (two rows; my own 1B claim joined the population, exactly as S93 saw) and Test 31's real-file count 16 -> 2 (the audited coupling, moving as predicted, tool count still equal to the raw grep). Sole failure is Test 9's pre-existing `--source=github` 404, confirmed BY NAME (`github source dry-run failed`), not by count. `tools.test_methodology_trim` 103/103, `tools.test_methodology_dashboard` 300/300 (4 skipped), `tools.test_context_budget` 41/41. `bin/check-handoff --all --allow-pending` OK (4 receipts), `bin/check-links` OK (88/22, unchanged), `bin/check-learnings` OK (31 rows). *Cannot enforce:* the suite would also pass with the `**Model:**` population at 1 -- nothing asserts the margin, only the non-emptiness. **SURFACE 2 -- the artifact itself, tampered BEFORE commit,** the only window in which the proof can see a working-tree change (S93's gotcha; it held). Every restore `cmp`-verified against backups held OUTSIDE the repo. Clean -> PASS. One record deleted -> FAIL naming `MISSING: ### 2026-08-15 · [ad hoc] S91 close-out`, with L1 and L3 both firing. Pure reorder -> whole BLOCKS swapped with the record multiset asserted identical and the byte count asserted unchanged first, both victims pre-existing rather than added -> FAIL via `L3 record(s) out of order across the move: [17, 18]` **and no MISSING line**, which is what proves the ordering clause actually ran rather than the red arriving through the MISSING path (S94's invalid first fixture). *Cannot enforce:* nothing here tests the shard against post-commit tampering, invisible by design. **SURFACE 3 -- the frozen proof, post-commit.** `bash docs/archive/CHANGELOG-through-2026-08-15.md.verify.sh` reads all three artifacts from `c3d68c5`: `41 before = 13 retained + 28 archived; added by the trim commit: 1` -> **OK**. All eight shipped proofs re-run: **4 PASS, 4 FAIL**, the four being BL-36's untouched residual (S94 measured 3/4 of seven; I added the 8th and it is green). *Cannot enforce:* this proves the MOVE was lossless, not that the archived records remain reachable prose -- `bin/model-report --changelog <shard>` was not exercised against the new shard. **FM #28 GATE:** `CHANGELOG.md` **ok** (420 ln / 2,000; 31,539 B at the trim), `HANDOFFS.md` ok, `CLAUDE.md` ok, `FRAMEWORK_LEARNINGS.md` ok but at 57,964 / 60,000; `docs/planning/BACKLOG.md` 104,530 B still OVER. `growth run` reads 14/10 and is NOT evidence about this trim -- it is computed on `resident_bytes`, which in this repo is `CLAUDE.md` alone, so it is structurally incapable of responding to a ledger reduction (S94's correction of its own Phase 0 report, re-verified here).
changelog_ref: CHANGELOG.md "2026-08-17 · [ad hoc] Ledger trim: `CHANGELOG.md` → `docs/archive/CHANGELOG-through-2026-08-15.md` (28 record(s), 84,765 B → 31,539 B)"
commit: a35a14f (1B claim) + c1382e0 (claim recorded, P1) + c3d68c5 (the trim alone) + this close-out
```
<!-- claim stub written at session start; completed at close-out -->
Model: Claude Opus 5 (1M context).

**Predecessor S94 scored 9/10.** **+** One sentence in its `next_steps` was the whole session: *"I
did NOT audit the suite for `CHANGELOG.md` fixture couplings and it is not safe to assume there are
none."* A negative claim, honestly labelled as unfinished work rather than quietly omitted — and it
was right to warn. There are six non-human readers of that file and one imposes a floor the tool
cannot see. Without that sentence I would have run the default cut and discovered Test 30 the way
S94 discovered Test 34: from a red suite. **+** Its `--cut N` semantics note — *"the number to
RETAIN, not to archive, and `--help` does not say"* — is the difference between a 13-record retention
and a 13-record archive. I verified it at `_explicit_retain` before using it; it cited `:863` and the
function is at `:858`, a drift too small to deduct for. **+** Its published baseline of **235 passed /
1 failed with the sole failure named** let me confirm my own 236-row capture matched the row before I
changed anything. **+** Its P1-guard finding predicted the exact refusal I hit, and its
`.jsonl`/`--hard` gotcha kept two inherited dirty files untouched through a session that rewrote the
ledger. **−** **Its `changelog_ref` does not resolve.** It quotes *"[ad hoc] `HANDOFFS.md` trimmed
141,085 → 38,071 B — the first `HANDOFFS` losslessness proof in this repo that passes"*; the actual
heading is *"Ledger trim: `HANDOFFS.md` → `docs/archive/HANDOFFS-through-2026-08-15.md` (8 record(s),
141,085 B → 38,071 B)"*. `grep -F` finds the quoted string in neither the live ledger nor any archive.
The locator-form rule exists precisely so this field survives a trim; quoting a *paraphrase* of the
heading defeats it as surely as a line number would. **−** It chose retention explicitly against the
ceiling and Test 34 and never mentions the seed's own stop condition of ≤ ½ × budget, which its 38,071 B
does not meet — a successor cannot tell whether that rule was weighed and overridden or simply missed.
Not a 10 because of the dead pointer; not an 8 because the one sentence it wrote about its own
unfinished audit was worth more than most receipts' entire `next_steps`.

**Self-assessment: 8/10.** **+** I did the audit before cutting rather than after, classified each
reader as a count floor or a content floor, and proved the binding one by simulating the actual
partition and re-measuring the predicate on the retained half — not by reading the tool's numbers,
which cannot express it. **+** I took the tool's default and said *why it was safe here*, which is a
different claim from "the default is safe": 2 carriers survive by positional accident, and I wrote
that down as the warning rather than as reassurance. **+** Both tamper controls were valid, and the
reorder one avoided the trap that invalidated S94's first attempt — whole blocks, multiset and byte
count asserted unchanged first, victims pre-existing — so it failed through L3's ordering clause with
no MISSING line, which is the only way that red answers the question asked. **+** I proved
`CUT_STRADDLES_DAY` unavoidable by enumerating the seams instead of accepting or dismissing the
advisory. **−** **I truncated my own baseline.** Piping the suite through `tail -60` captured 51 of
236 rows; I caught it, but only after the run, and re-running cost ~8 minutes. Capturing a population
through a tail filter is the denominator mistake wearing a different hat, and I made it while
explicitly setting out to capture a population. **−** **My first carve-out check was wrong and I came
close to publishing it.** Scraping the manifest for string-like values collected destinations as well
as sources, so it reported three distributed files touched when the answer is one. It failed safe —
over-reporting — but a check I cannot state the semantics of is not evidence, and I had to read
`DISTRIBUTION` to find out what my own numbers meant. **−** I wrote a garbled clause into
`CHANGELOG.md` and caught it on re-read rather than before the write. Not a 9 because two of my own
verification artifacts were wrong on first construction; not a 7 because both were caught in-session
by re-reading rather than by luck, and the session's central claim — the content floor — was measured
three separate ways before it was acted on.


```handoff
session: S94
date: 2026-08-16
status: complete
self_score: 8
predecessor_score: 9
active_task: Trim `HANDOFFS.md` back under its 65,536 B ceiling with a losslessness proof that actually PASSES. COMPLETE -- 141,085 B -> 38,071 B (-73.0%), and `docs/archive/HANDOFFS-through-2026-08-15.md.verify.sh` is THIS REPO'S FIRST GREEN `HANDOFFS` PROOF. The FM #28 gate now reads `HANDOFFS.md` **ok**; `CHANGELOG.md` and `docs/planning/BACKLOG.md` are still over and were NOT touched (separate deliverables, FM #17). CARVE-OUT DECLARED AT CLAIM AS ZERO DISTRIBUTED FILES, AND I EXCEEDED IT BY ONE -- see gotchas; recorded, not dressed up. NO OUTWARD-FACING ACTION: no PR, no push, no issue, no comment, no tag.
what_was_done: Four commits -- `1ac90ac` (1B claim), `cd83833` (record the claim, forced by the trimmer's own P1 guard), `17753d9` (THE TRIM ALONE), and this close-out. **(1) THE COMMIT SHAPE WAS THE DELIVERABLE'S LOAD-BEARING PART AND IT HELD.** Declared at claim, not discovered at close-out: both previously-shipped `HANDOFFS` proofs are red because each of those trim commits finalized its own frontier receipt inside the trim, EDITING a record that existed at TRIM^, whose pre-trim bytes then exist nowhere. S93's v1.2.0 excused a commit's ADDED records and deliberately not an EDITED one. So the receipt stayed `status: pending` across `17753d9` and is finalized only here. The frozen proof reports `added by the trim commit: 0` and PASSES. **(2) THE TRIMMER'S OWN P1 GUARD RE-ORDERED THE SESSION, CORRECTLY.** The first dry run refused: my claim commit sat above the `CHANGELOG.md` frontier, and a trim rewrites that file, so `git log -1 -- CHANGELOG.md` would advance past it and Phase 0's `frontier..HEAD` set would lose it permanently. Hence `cd83833`. The order trimming forces is **claim -> record -> trim -> close-out**. **(3) MY FIRST TRIM WAS WRONG AND THE SUITE CAUGHT IT.** Taken at the tool's budget-driven default, it retained 2 receipts and made five of `bin/tests.sh` Test 34's assertions VACUOUS -- 235 passed/1 failed -> 229/6. Test 34 reads its mutation anchors as `ids[1]`/`ids[2]` of the live ledger (`bin/tests.sh:2103`), deliberately not hardcoded so it survives WHICH receipts rotate, and silent about HOW MANY must survive. I unwound that commit with `git reset --soft` + targeted `restore`/`checkout` -- NEVER `--hard`, because two inherited `.jsonl` telemetry files were dirty and SAFEGUARDS forbids touching a predecessor's uncommitted work -- and re-trimmed at `--cut 3`. **(4) RETENTION WAS CHOSEN AGAINST BOTH CONSTRAINTS WITH NUMBERS, NOT BY PICKING THE MINIMUM.** Receipts here run 10-13.5 KB (S91 13,553; S92 13,091; S93 11,917), so: 2 -> 21,231 B, Test 34 vacuous; 3 -> 38,071 B, 27,465 B headroom (~2 sessions), Test 34 satisfied exactly; 4 -> 56,222 B, 9,314 B headroom, under the ceiling by less than one receipt. **(5) BL-40 RAISED AND LEARNING #31 APPENDED** (30 rows, `#14` still reserved). **(6) I CORRECTED MY OWN PHASE 0 REPORT** -- I cited `growth run 10/10` as evidence of ledger escalation; it is computed on `resident_bytes`, which in this repo is CLAUDE.md ALONE (`.context-budget.json` declares a one-file resident class), so it is structurally incapable of responding to a `HANDOFFS.md` reduction. It read 11/10 after a 103 KB cut.
next_steps: (a) **`CHANGELOG.md` IS THE NEXT TRIM AND IT IS NOW UNBLOCKED IN THE SAME WAY** -- 79,587 B against 65,536 B, and this session grew it by ~2 KB. The identical commit shape applies: claim -> record -> trim ALONE -> close-out. **Check what reads that file before cutting** (Learning #31); `bin/tests.sh` Test 34's ledger dependence is on `HANDOFFS.md`, but I did NOT audit the suite for `CHANGELOG.md` fixture couplings and it is not safe to assume there are none. (b) **BL-40 IS THE DURABLE FIX AND IT IS NOT DONE.** The next default-cut trim of `HANDOFFS.md` re-breaks Test 34; I avoided the trap, I did not remove it. Option (b) in that item -- make Test 34 skip with a stated reason below three receipts -- is canonical-only, no adopter impact, and my recommendation, but I did NOT implement or test it, so treat "looks right" as an untested lean. (c) `docs/planning/BACKLOG.md` 104,530 B, still over, untouched, and it GREW by this session's own BL-40 entry. S89 measured that its residual excess is live work, not housekeeping, so trimming it is an operator policy call, not a session's. (d) **BL-36's RESIDUAL IS UNCHANGED**: the four frozen proofs still read 4 red / 2 green under the old logic and still need their own go-ahead; my new proof is a SEVENTH artifact and does not touch them. Current state across all seven: 3 PASS, 4 FAIL. (e) **AUDIT REC 3 IS STILL UNWRITTEN.** This session FOLLOWED the discipline ("a trim commit touches nothing but the trim") and proved it works -- that is now evidence for the rule, not a substitute for it. (f) The `CHANGELOG.md` 2026-08 ordering defect S93 reported at `:162`/`:172` is STILL THERE and I did not fix it (FM #17); my two entries went to the top of the section. (g) `upstream/main` is 1 ahead (`512c2ed`); `git merge-tree --write-tree --name-only HEAD upstream/main` still conflicts in exactly one path, `HANDOFFS.md` -- and this session rewrote that file wholesale, so RE-DERIVE, do not trust this line. (h) BL-37 half (b), BL-39, and the long tail BL-11/12/13/14/16/17/18/19/20-residual/21/22/23/26/30/31/32 unchanged, NOT re-verified.
key_files: `HANDOFFS.md:8` (the retained-count line, a declared regenerated field of the trimmer -- its stale `(S92, 2026-08-15` attribution repaired here, since the trim reset the number and left the credit), `:24` (the drift blockquote, corrected: a trim DOES regenerate the count, a prepend does not), `:31` (**the new `--cut 3` floor warning** -- the thing a future trimming session must read), `:60` (this receipt); `docs/archive/HANDOFFS-through-2026-08-15.md` + `.verify.sh` (the shard and the first green `HANDOFFS` proof); `bin/tests.sh:2082` (Test 34) and `:2103` (**the `ids[1]`/`ids[2]` line that is the whole coupling**); `starter-kit/methodology_trim.py:863` (`--cut N` = records to RETAIN, not to archive -- I had to read the source to learn this; `--help` does not say), `:1194` (the frozen proof reads all three artifacts from the trim commit), `:1251` (the measured added/absent sets), `:1340` (L3's ordering clause); `docs/planning/BACKLOG.md` (BL-40, and the header open-list line); `starter-kit/FRAMEWORK_LEARNINGS.md` (Learning #31 -- **the distributed file I said I would not touch**); `CHANGELOG.md` (this session's three entries).
gotchas: **I DECLARED A CARVE-OUT I HAD NOT CHECKED AGAINST THE PROTOCOL, AND I BROKE IT.** The claim said ZERO DISTRIBUTED FILES; Phase 3C is a MANDATORY close-out step and its only home is `starter-kit/FRAMEWORK_LEARNINGS.md`, which `bin/_manifest.py` distributes. S93's claim pre-declared exactly this contingency and I had read it. I kept the learning -- skipping a mandatory step to protect a self-imposed note is the wrong trade, and the edit is append-only and purely local (no push, no PR, so no operator gate is involved) -- but the carve-out was MINE and I broke it. Final count: 26 manifest rows, 6 files changed, ONE distributed. **A SELF-DECLARED SCOPE NOTE IS NOT AN OPERATOR PROHIBITION, AND IT IS ALSO NOT NOTHING** -- do not inflate it into a blocker, do not quietly exceed it. **`--cut N` IS THE NUMBER TO RETAIN, NOT TO ARCHIVE**, and `--help` says only "override the computed cut point"; the semantics are at `methodology_trim.py:863`. Guessing wrong here is a silently over-aggressive trim. **THE FIRST REORDER FIXTURE I BUILT PROVED NOTHING AND LOOKED LIKE IT WORKED.** Swapping record BODIES while leaving the inter-record prose in place perturbs both records' bytes, so the proof failed through the MISSING path and L3's ordering clause never ran -- a red that answers a different question than the one asked. A pure reorder must swap WHOLE BLOCKS (record + its trailing prose) and assert the record multiset is unchanged before running; then it fails as `out of order across the move: [3, 4]` with `added by the trim commit: 0`. **READ THE FAILING CLAUSE, NOT THE EXIT CODE.** **`core.hooksPath` IS NOW SET TO `.githooks` IN THIS CLONE** -- three consecutive earlier receipts (S11 among them) record it as unset and say the ledger gate never runs for anyone here. That is NO LONGER TRUE: the pre-commit hook fires and refuses a tracked-content commit without `CHANGELOG.md` co-staged. The 1B claim legitimately needs `--no-verify`; the trim does not, because the tool co-stages the ledger entry itself. **DO NOT `git reset --hard` IN THIS REPO** -- `.context-budget-history.jsonl` and `dashboard_history.jsonl` are tracked, dirty, inherited, and owned by no protocol step, and merely running `context_budget.py` or the dashboard at Phase 0 appends to them. Unwinding a commit needs `--soft` plus targeted `restore`/`checkout`, with `git status --porcelain` checked before and after. **THE LIVE LEDGER IS A TEST FIXTURE.** Nothing in the file said so before this session; `HANDOFFS.md:31` says so now.
runtime_smoke: **THREE SURFACES, AND WHAT EACH CANNOT ENFORCE.** This repo ships no application; the build-equivalent is the suites plus the tools, RUN. All figures measured after the final non-receipt edit and BEFORE this receipt was written, so the `HANDOFFS.md` number understates by roughly this block. **SURFACE 1 -- the canonical checkout.** `bash bin/tests.sh` **235 passed / 1 failed**, IDENTICAL to S93's published baseline; the sole failure is Test 9's pre-existing `--source=github` 404 (2 of 26 distributed files absent upstream), confirmed by name, not by count alone. The intermediate **229 / 6** was MY regression and is the reason this session has a BL item. `tools.test_methodology_trim` 103/103, `tools.test_methodology_dashboard` 300/300, `tools.test_context_budget` 41/41. `bin/check-handoff --all --allow-pending` OK (3 receipts), `bin/check-links` OK (88/22), `bin/check-learnings` OK (30 rows). *Cannot enforce:* the suite passes at 2 retained receipts too -- it reported five VACUOUS assertions and still summarised them as failures only because the harness separates did-not-apply from survived. **SURFACE 2 -- the artifact itself, tampered before commit.** Three controls against the real shard, every restore `cmp`-verified against backups held OUTSIDE the repo: clean -> PASS; one record deleted -> FAIL naming `MISSING: session: S89` / `S88`; pure reorder (record multiset asserted identical first) -> FAIL via `L3 record(s) out of order across the move: [3, 4]`. Done BEFORE committing, the only window in which the proof can see a working-tree change -- S93's gotcha, and it held. *Cannot enforce:* nothing here tests the shard against post-commit tampering, which is invisible by design. **SURFACE 3 -- the frozen proof, post-commit.** `bash docs/archive/HANDOFFS-through-2026-08-15.md.verify.sh` reads all three artifacts from `17753d9` and reports `11 before = 3 retained + 8 archived; added by the trim commit: 0` -> **OK**. All seven shipped proofs re-run: 3 PASS, 4 FAIL, the four being BL-36's untouched residual. *Cannot enforce:* this proves the move was lossless, NOT that the archived receipts remain reachable prose -- `bin/model-report --handoffs` was not exercised. **FM #28 GATE:** `HANDOFFS.md` **ok** (176 ln / 1,200; 39,289 B / 65,536); `CHANGELOG.md` 79,587 B and `docs/planning/BACKLOG.md` 104,530 B still OVER; `CLAUDE.md` and `FRAMEWORK_LEARNINGS.md` ok. **CARVE-OUT VERIFIED MECHANICALLY:** `bin/_manifest.py` imported and set-compared against `git diff --name-only`, both populations asserted non-empty -- 26 manifest rows, 6 changed files, ONE distributed (`starter-kit/FRAMEWORK_LEARNINGS.md`, the breach above).
changelog_ref: CHANGELOG.md "2026-08-16 · [ad hoc] `HANDOFFS.md` trimmed 141,085 → 38,071 B — the first `HANDOFFS` losslessness proof in this repo that passes"
commit: 1ac90ac (1B claim) + cd83833 (claim recorded, P1) + 17753d9 (the trim alone) + this close-out
```
<!-- claim stub written at session start; reconciled at close-out -->
Model: Claude Opus 5 (1M context).

**Predecessor S93 scored 9/10.** **+** One sentence in its `next_steps` shaped this entire session's
structure: *"a trim done today writes a proof that holds — but only if the trim commit is standalone
or add-only."* Without it I would have bundled close-out into the trim, as this repo's own practice
had done twice before, and produced a third permanently-red proof instead of its first green one.
That is the highest-leverage thing a handoff can do — it changed the shape of the work, not just its
speed. **+** Its gotcha *"DO NOT TEST THE FROZEN PROOF BY TAMPERING AFTER THE TRIM IS COMMITTED …
your 'RED check' passes green. I wasted a cycle on exactly this"* saved me that cycle exactly as
advertised; I tampered pre-commit and got real RED with named victims. **+** Its published baseline
of **235 passed / 1 failed**, given as a row-for-row figure with the sole failure named, is the only
reason my own regression to **229 / 6** was diagnosable in one step as *mine* rather than
pre-existing. A bare "suite passes" would have cost an hour. **+** Its `.jsonl`-telemetry gotcha
meant I knew before Phase 0 that those two files were inherited, unowned, and would be dirtied
further just by orienting — which is what made me reach for `git reset --soft` instead of `--hard`
when I had to unwind a commit. **−** The one deduction, and it is narrow: **its `next_steps`
actively recommended trimming and did not carry the precondition that makes trimming safe.** Nothing
in its receipt says the live ledger is a test fixture with a floor of three receipts, and S93 was the
session best positioned to notice — it had just run the full suite against that very file while
working on that very tool. A recommendation to act carries the known preconditions of acting. That
constraint now lives at `HANDOFFS.md:33` and in BL-40, which is where it should have been before I
needed it.

**Self-assessment: 8/10.** **+** The commit shape was declared at claim time and held exactly,
producing the deliverable's actual point: `added by the trim commit: 0` and this repo's first
passing `HANDOFFS` proof. **+** I proved the proof non-vacuous rather than trusting a green exit,
and caught that my *first* reorder fixture was invalid — it perturbed record bytes and failed
through the MISSING path, so L3's ordering clause never ran and the red answered a different
question than the one I asked. I found that by reading the failing clause instead of the exit code.
**+** When I broke the suite I compared against the predecessor's published baseline, traced it to a
single line (`bin/tests.sh:2103`), and then chose retention against *both* constraints with the
numbers tabulated — rather than taking the tool's default, the bare minimum, or the largest that fit.
**+** The unwind was done with `--soft` plus targeted `restore`/`checkout` and `git status` checked
on both sides, so two inherited dirty files were provably untouched. **+** I corrected my own Phase 0
report rather than letting it stand: I had cited `growth run 10/10` as evidence of ledger escalation,
and it is computed on `resident_bytes` — CLAUDE.md alone here — so it cannot respond to a
`HANDOFFS.md` reduction at all. **−** **I declared a carve-out I had not checked against my own
protocol and then broke it.** "ZERO DISTRIBUTED FILES" was written without asking what Phase 3C
mandates; its only home is a distributed file. S93's claim pre-declared exactly this contingency and
I had read that claim minutes earlier. **−** My first trim went in at the tool's default without
asking what else reads the file — precisely the question Learning #31 now instructs everyone else to
ask. The suite caught it; I did not. That is one wasted commit cycle and the difference between a
lesson learned and a lesson enforced. **−** I recorded BL-40's option (b) as "looks right" without
attempting it, so a future session inherits a lean with no evidence under it; I labelled it as such,
which is the minimum, not a fix.


```handoff
session: S93
date: 2026-08-16
status: complete
self_score: 8
predecessor_score: 9
active_task: BL-36 -- repair the GENERATED `.verify.sh` losslessness proof so it stops reporting FAIL over archives S88 measured intact. COMPLETE for the code half; BL-36 is narrowed to its open residual (the four already-frozen artifacts), whose disposition the operator scheduled as a SEPARATE session. S88's audit (`docs/audits/2026-08-15-bl36-archive-losslessness.md`) had answered the question -- 0 records missing at every trim, 0 of 228 identities unreachable at HEAD -- and located the fault entirely in the proof, but deliberately made no repair because `starter-kit/methodology_trim.py` is DISTRIBUTED (`bin/_manifest.py:50`) and needed an operator-gated, RED-first session. CARVE-OUT HELD AS DECLARED: exactly ONE distributed file edited, verified by importing `bin/_manifest.py` and set-comparing against `git diff --name-only`, BOTH populations asserted non-empty (26 manifest rows, 4 changed files -- the other two are the inherited untracked-by-any-protocol-step `.jsonl` telemetry). NO OUTWARD-FACING ACTION: no PR, no push, no issue, no comment, no tag.
what_was_done: Two commits -- `4047a61` (the Phase 1B claim) and this close-out, which carries the fix, its tests, the learning, the ledger entry and the backlog narrowing. **MY OWN CLAIM STUB'S DESIGN WAS WRONG AND THE PROTOTYPE CAUGHT IT BEFORE ANY CODE WAS WRITTEN -- that is the single most useful thing that happened this session.** The stub asserted that replacing the positional frame with MULTISET matching would make bundled trims pass. A read-only prototype replaying all six real shards showed that holds only for records the commit ADDED; a record the commit EDITED is genuinely absent afterwards, so BL-27's "stays a loud FAIL" was right and 2 of the 4 failures must stay red. **(1) THE REPAIR IS AUDIT REC 2 DONE BY CONTENT, NOT BY POSITION.** Rec 2 said "make `injected` a measured count"; measuring it positionally derives the operand from the very difference L1/L3 assert on, an identity that cannot fail (Learning #16). The added set is now `after + shard` minus `before` by record text, removed occurrence-wise and in order before the byte comparison. `INJECTED` is gone from the template; L1/L2/L3 keep their names and semantics. **(2) THE IN-MEMORY TWIN AT `:1838` IS DELIBERATELY UNCHANGED**, with a comment saying why -- there the count is a fact the tool owns (it performs the injection), not an inference about a commit. **(3) SIX RED-FIRST TESTS, ALL OBSERVED FAILING FIRST** against the unmodified tool (7 failures incl. subTests); trim suite 97 -> 103/103. **(4) TWO NARROWED CONTROLS EARNED THEIR PLACE BY FAILING AFTER THE FIX.** The loss control caught that my new global `missing` silently REBOUND L2's same-named front-matter variable three clauses above it -- a generated script is one flat namespace -- so L3 read L2's usually-empty list and reported a downstream symptom instead of its own finding; renamed to `absent_records`/`added_records` with the hazard pinned in a comment. The reorder control caught that my first fixture swapped two records the commit had ADDED, which is correctly invisible and proved nothing. **(5) LEARNING #30 APPENDED** (29 rows, `#14` still reserved). **(6) BL-36 NARROWED, NOT CLOSED**, with the residual's disposition recorded and now backed by measured numbers.
next_steps: (a) **THE FOUR FROZEN PROOFS ARE THE NAMED NEXT SESSION, AND IT NOW HAS NUMBERS IT DID NOT HAVE.** Regenerating under v1.2.0 takes them from 4 red to 2 red -- re-derive with the replay method rather than trusting that. Recommended: regenerate all four and state in each shard's front matter that the proof was regenerated on that date and is no longer the artifact originally shipped. That is a deliberate exception to the archive freeze rule (`starter-kit/HANDOFFS.md` §Size, and when to archive) and NEEDS ITS OWN GO-AHEAD. (b) **THE TWO THAT STAY RED WANT THE PROTOCOL RULE, NOT MORE CODE** -- audit rec 3, "a trim commit touches nothing but the trim", is the only thing that removes their cause, and it is untaken. It is also cheap and touches a distributed doc. (c) **`CHANGELOG.md` IS STILL OVER AND `HANDOFFS.md` IS FURTHER OVER THAN WHEN S92 MEASURED IT** -- 77,562 B and 126,464 B against 65,536 B, `docs/planning/BACKLOG.md` 101,685 B, all measured AFTER this session's last ledger write and BEFORE this receipt was appended, so the `HANDOFFS.md` figure understates by roughly this block. Re-derive with `python3 starter-kit/context_budget.py`. **The BL-36 blocker on trimming is now HALF lifted, not lifted**: a proof written by v1.2.0 no longer fails on a bundled commit, so a trim done today writes a proof that holds -- but only if the trim commit is standalone or add-only. (d) `growth_run` is **9/10**, up from 8 at my Phase 0 and 7 at S91. (e) **A DURABLE-RECORD DEFECT I FOUND AND DID NOT REPAIR, because repairing a predecessor's receipt is not my call:** S92's receipt `active_task` says "NO OUTWARD-FACING ACTION TAKEN: no PR, no push, no issue comment, no tag", and a comment WAS posted afterwards (`143ff2b`, operator-authorized, correctly ledgered). Two later commits (`e61739c`, `0b4aa3d`) touched that very receipt without fixing it. (f) **`CHANGELOG.md`'s 2026-08 section is not newest-on-top**: the 2026-08-16 entry at `:172` sits BELOW a 2026-08-15 one at `:162`. Pre-existing, not mine, not fixed (FM #17). (g) BL-37 half (b), BL-39, and the long tail BL-11/12/13/14/16/17/18/19/20-residual/21/22/23/26/30/31/32 are unchanged and NOT re-verified this session. (h) **UPSTREAM RE-DERIVED AT MY PHASE 0**: `upstream/main` exactly 1 ahead (`512c2ed`), `git merge-tree --write-tree --name-only HEAD upstream/main` conflicts in exactly ONE path, `HANDOFFS.md`. RE-DERIVE -- this session writes to that file.
key_files: `starter-kit/methodology_trim.py` (DISTRIBUTED; v1.1.3 -> v1.2.0) -- `:50` the version block and why this is MINOR not patch, `:1032` `VERIFY_TEMPLATE` begins, `:1046` the shell header with `INJECTED` removed, `:1192` `TRIM = os.environ["TRIM_SHA"]` (no more `INJ`), `:1244` THE NAMESPACE WARNING and the measured `added_records`/`absent_records` sets, `:1256` `drop_added`, `:1270` L1 over `rebuilt`, `:1331` L3's MISSING clause and why no count clause can ever run, `:1351` the BL-27 note re-expressed in the new vocabulary, `:1372` the report lines, `:1392` `build_verify` (parameter dropped), `:1838` the in-memory `injected` left alone ON PURPOSE with the reason beside it; `tools/test_methodology_trim.py` (CANONICAL-ONLY) -- `:939` the BL-36 header argument, `:962` `_insert_records_at_top_of_zone`, `:971` `_trim_then_bundle`, `:1027` the namespace-collision control (LOSS), `:1042` the reorder control with its added-records trap documented, `:1068` the template coupling guard; `docs/planning/BACKLOG.md:890` (BL-36 narrowed to its residual); `starter-kit/FRAMEWORK_LEARNINGS.md:52` (Learning #30); `CHANGELOG.md:162` (the `[BL-36]` entry).
gotchas: **A GENERATED SCRIPT IS ONE FLAT NAMESPACE AND IT WILL NOT WARN YOU.** `missing` was already bound by L2's front-matter clause; my identically-named record-level list rebound it, and because L2 usually finds nothing the rebind was to `[]`, so L3 silently took its `else` branch and reported "out of order" for what was actually a dropped record. Grep the GENERATED artifact for every name you add -- `python3 -c "...build_verify(...)"` into a scratch file, then `grep -nE '\bname\b'`. **DO NOT TEST THE FROZEN PROOF BY TAMPERING AFTER THE TRIM IS COMMITTED.** Once `TRIM_SHA` is non-empty the script reads all three artifacts from the trim commit (`show(TRIM^, LIVE)`, `show(TRIM, LIVE)`, `show(TRIM, SHARD)`), so a working-tree tamper is invisible BY DESIGN and your "RED check" passes green. I wasted a cycle on exactly this. Tamper BEFORE committing, when the script compares HEAD against the working tree. **THE TRIM'S OWN LEDGER ENTRY IS AN ADDED RECORD LIKE ANY OTHER** -- a bundle of N extras reports `added by the trim commit: N+1` on `CHANGELOG.md`. My first test asserted N and was wrong; the tool was right. **A REORDER FIXTURE MUST SWAP RECORDS THE COMMIT DID NOT ADD** -- added records are dropped before the ordering comparison, so swapping two of them is correctly invisible and the test passes proving nothing. The control at `:1069` asserts both victims are pre-existing. **`bin/check-learnings` READS "29 row(s), contiguous 1..29" WHILE THE MAXIMUM ROW IS 30** -- `#14` is reserved, so count and max differ by one and "1..N" is the COUNT. Pre-existing; do not "repair" it. **`bin/check-handoff`, `bin/check-links`, `bin/check-learnings` ARE PYTHON, NOT SHELL** -- `bash bin/check-handoff` dies with a syntax error that looks like a broken tool. Use `python3`. **THE TWO `.jsonl` TELEMETRY FILES ARE STILL UNSTAGED AND STILL INHERITED**; running `context_budget.py` at Phase 0 appends another row to `.context-budget-history.jsonl`, so the diff grows just by orienting. Nothing stages them and no protocol step owns them.
runtime_smoke: **THREE SURFACES NAMED, AND WHAT EACH CANNOT ENFORCE STATED.** This repo ships no application; the build-equivalent is the suites plus the tools themselves, RUN. All figures measured after the final ledger write and BEFORE this receipt was appended. **SURFACE 1 -- the canonical checkout.** `bash bin/tests.sh` **235 passed / 1 failed**, compared ROW-FOR-ROW against the pre-change baseline captured at this session's own Phase 0, both populations asserted non-empty (236 rows each): **zero rows lost, one row changed** -- a historical-claim-commit replay counter 85 -> 86, which now includes my own 1B claim commit. Sole failure is Test 9's pre-existing github-source 404 (2 of 26 distributed files absent upstream). `tools.test_methodology_trim` **103/103** (97 before), `tools.test_methodology_dashboard` **300/300**, `tools.test_context_budget` **41/41**. `bin/check-links` OK (88/22), `bin/check-learnings` OK (29 rows), `bin/check-handoff --all --allow-pending` OK. *Cannot enforce:* nothing here exercises a real archive. **SURFACE 2 -- THE SIX REAL SHIPPED SHARDS, replayed read-only.** Proofs generated by the FIXED tool for each shard's own spec, run from the repo root, written to the scratchpad and never to `docs/archive/`; `git status --porcelain docs/archive` empty afterwards. `CHANGELOG-through-2026-08-02` and `-08-09` **FAIL -> PASS**; the two already-green stay green; the two `HANDOFFS` proofs **stay red, correctly**, now naming `MISSING: session: S61` / `S64`. *Cannot enforce:* this replays the LOGIC against real data -- the four artifacts on disk are frozen and still say what they said, which is precisely BL-36's residual. **SURFACE 3 -- A FRESH `bin/sync` ADOPTER TREE, the only surface that speaks for a DISTRIBUTED file.** Delivered `methodology_trim.py` `diff -q` byte-identical to canonical, `TRIM_VERSION = "1.2.0"`, zero occurrences of `@@INJECTED@@` or `ar[INJ:]`. In that tree, using the adopter's OWN copy: a trim bundled with a second ledger entry proves lossless end to end (exit 0, both added records named); and in a second adopter tree a pre-commit shard tamper goes RED naming its victim (`MISSING: ### 2026-01-21 · [ad hoc] adopter entry 20`, exit 1) with a control proving the same proof was green immediately before. *Cannot enforce:* `--source=local` only -- the `--source=github` path is Test 9 and stays red until these files exist upstream, so NOTHING here verifies delivery over the network. **FM #28 GATE:** `CHANGELOG.md` 77,562 B, `HANDOFFS.md` 126,464 B, `docs/planning/BACKLOG.md` 101,685 B, all OVER 65,536 B; `CLAUDE.md` and `FRAMEWORK_LEARNINGS.md` ok; growth run **9/10**.
changelog_ref: CHANGELOG.md "2026-08-16 · [BL-36] The archive losslessness proof stopped inferring what the trim commit added — `methodology_trim.py` v1.2.0"
commit: 4047a61 (1B claim) + this close-out commit
```
<!-- claim stub written at session start; reconciled at close-out -->
Model: Claude Opus 5 (1M context).

**Predecessor S92 scored 9/10.** **+** Every figure it published and I independently re-derived
held exactly: `bin/tests.sh` 235/1 to the row, `check-links` 88/22, 28 learning rows, 9 receipts,
`BACKLOG.md` at 98,342 B, and `upstream/main` exactly 1 ahead with `merge-tree` conflicting in
exactly `HANDOFFS.md`. Its two ledger byte-figures read low against mine only because two later
commits grew those files — and it had *said* its own `HANDOFFS.md` number understated by roughly
its own block. **+** Its 1B-stub gotcha — *"the stub takes ONLY `session`, `date`, `status`,
`active_task`; `key_files: pending` fails the `path:line` lint even in stub mode"* — is why my
claim passed `check-handoff` first try. Traceable ROI inside ten minutes. **+** Its `next_steps`
(d) named BL-36 as the thing that should precede any trim, with the reason, and pointed at S88's
audit. That is the whole reason this session could start at the code instead of re-deriving the
diagnosis. **−** The one real defect, and it is in the durable record rather than the prose: its
`active_task` asserts *"no issue comment"* and a comment was posted afterwards — with two later
commits touching that receipt and neither repairing it. **−** Its `next_steps` ran to eight
lettered items; (c) and (d) were correctly prioritized, but the tail is closer to "here is the
backlog" than to a handoff. Not a 10 because of the stale claim; not an 8 because the verification
density is genuinely high and it was load-bearing input to everything I did.

**Self-score 8/10.** **+** I ran a throwaway prototype against the six real shards *before*
writing a line of production code, and it refuted my own ratified claim-stub design — the
multiset framing I had committed to in the 1B claim was right for additions and wrong for edits.
Cheap, and it meant the tests I then wrote encoded the true boundary instead of my assumption.
**+** The two narrowed controls both failed after the fix and both were finding real defects in my
work, not in the tool — the namespace rebind especially, which was silent, self-consistent, and
would have shipped a proof that reported the wrong clause on a real loss. **+** Three surfaces,
with what each cannot enforce stated, and `docs/archive/` provably untouched. **−** I wasted a
cycle "proving" the adopter proof goes red by tampering after the trim commit, which is invisible
by design — I should have read the `if TRIM:` branch before designing the check rather than after
it passed green. **−** My first two test fixtures were both wrong (the added-count off by the
trim's own entry; the reorder swapping added records). They were caught, but by the controls
rather than by me. **−** I did not repair S92's stale outward-action claim, and I think that was
right — it is a predecessor's receipt — but it means the defect is now two sessions old and
recorded in a handoff rather than fixed. Not a 9 because three of my own artifacts were wrong on
first write; not a 7 because every one of them was caught in-session by a control I had built for
that purpose, which is the discipline working rather than luck.

```handoff
session: S92
date: 2026-08-15
status: complete
self_score: 8
predecessor_score: 9
active_task: Answer upstream [issue #75](https://github.com/KJ5HST/methodology/issues/75) fork-side. COMPLETE. The Planning Session Checklist required "explicit completion criteria and verification commands" per phase and did NOT require the criterion be demonstrable on a surface the executor would actually have -- verified present in THIS tree at Phase 0, not accepted from the issue text. Filed 2026-08-14 by the maintainer against his own adopter project: a 3-phase plan passed the checklist; Phase 1 closed reporting "verified live against prod -- cold run downloaded=4," every word true OF THE SIMULATOR, while on real hardware the code wrote nothing at all (the platform forbids creating directories at an App Group container root; the simulator permits it because its container is an ordinary host directory). 413 unit tests stayed green and were RIGHT TO -- no failure was a logic bug. CARVE-OUT HELD AS DECLARED: exactly ONE distributed file edited (`starter-kit/SESSION_RUNNER.md`, the item) plus the Phase 3C `FRAMEWORK_LEARNINGS.md` row, verified by importing `bin/_manifest.py` and set-comparing against `git diff --name-only` with BOTH populations asserted non-empty (26 manifest rows, 5 changed files). NO OUTWARD-FACING ACTION TAKEN: no PR, no push, no issue comment, no tag. The answer is prepared and vetted fork-side and STOPS there.
what_was_done: Three commits -- `a744218` (1B claim), `b1b7eaf` (the fix + Test 36), this close-out. **(1) THE GAP WAS RE-VERIFIED IN THIS TREE BEFORE IT WAS ANSWERED**, not inherited from the issue: the checklist's six items were read and counted mechanically (6, now 7). **(2) THE RESEARCH FINDING THAT RESHAPED THE ANSWER -- THE FRAMEWORK ALREADY OWNED THE DOCTRINE, AS AN ORPHAN.** §Vertical Slice Sessions gate (d), *"Faithful verification, per surface"* (`:163`), already says faithfulness is established and never assumed. A corpus-wide grep for that phrase returns **ONE line -- its own definition**; gate (d) is named once more INSIDE its own section (`:176`) and cited exactly once outside it in the whole fork (`docs/planning/b1-sync-coverage-expansion-plan.md:189`, a plan that applied it as a phase criterion). **The accurate claim is that no GENERAL PROCEDURE routes to it** -- not the checklist, not Phase 3E, not the flight manual's Phase 6 or Quality Gates. My first draft said "zero inbound citations," which was wrong: I grepped `gate (d)` and missed `gate d`. So it bound only sessions that opted into a vertical slice, and only once a layer was already built: after the last moment naming the surface could have changed the plan. #75's failing project ran an ordinary 3-phase plan, so gate (d) never reached it. **(3) THE FIX PROJECTS GATE (d) TO PLAN TIME BY QUOTING IT** rather than authoring a rival rule that could drift from it. **(4) TWO SITES, ONE REQUIREMENT -- a deliberate departure from #75's own one-line draft, and flagged as one:** all six pre-existing checklist items were checked and each mirrors a requirement stated in a section above it, so a checklist line with nothing behind it would have been the list's only orphan. §Per-Phase Completion Criteria gains a **The surface** bullet (`:133`); the checklist gains the verification line (`:150`). **(5) MECHANIZED AS A COUPLING GUARD, NOT A PRESENCE GREP.** Quoting gate (d) creates a citation that can dangle -- rename the gate and the new prose quotes a rule gone under that name, which is BL-10's exact class and which a grep for the added lines survives. `bin/tests.sh` Test 36 (canonical-only, deliberately NOT added to `bin/_manifest.py`): 6 assertions, 5 mutants, all killed, every mutant non-vacuity-checked by the existing `mutate` helper. Mutant 3 renames gate (d) and leaves the citation untouched -- the direction a presence-only guard cannot see. **(6) THE POPULATION GUARD EARNED ITS PLACE DURING AUTHORING, NOT IN THEORY:** my section extractor compiled `re.S` without `re.M`, so every line-anchored section extracted to `""` and all four absence checks would have passed vacuously. `EMPTY-CRITERIA`/`EMPTY-CHECKLIST`/`EMPTY-SLICE` failed loudly on the very first run instead; mutant 5 now pins that behaviour by renaming a heading. **(7) BL-39 RAISED** for #75's two *Related* items, split deliberately and recorded with the reason. **(8) LEARNING #29 APPENDED** (28 rows, `#14` still reserved).
next_steps: (a) **THE OUTWARD-FACING DECISION IS YOURS AND THIS IS THE STRONGEST CANDIDATE YET TO SEND** -- it answers an open issue the MAINTAINER HIMSELF filed on 2026-08-14, one day before this session, which is an invitation to his repo but still NOT authorization (the standing rule, and PR #64's precedent). Everything a PR needs exists: the gap re-verified in-tree, the orphan finding with its one-line grep, 6 assertions / 5 killed mutants, a row-for-row harness diff, and a real adopter-tree sync smoke. **Two things in it a maintainer might push back on, both deliberate and both defensible:** the change is in TWO sites, not the one line he drafted (justified by all six existing items mirroring a stated requirement -- re-run that check before arguing it), and it QUOTES gate (d) rather than restating it. (b) **STILL WORTH BATCHING** -- BL-13/14/17/20-residual/21/31/36/38 remain fork-side-ready and DISTRIBUTED-file-touching; the batching rule exists to protect the maintainer's review time. (c) **`CHANGELOG.md` IS NOW OVER ITS BYTE CEILING AND THE TRIM IS OWED, NOT OPTIONAL** -- **69,557 B against 65,536 B, over by 4,021**, measured AFTER the last ledger entry this session wrote (an earlier draft of this receipt quoted 66,009 and 68,731 B, both taken mid-change and both stale before close-out -- re-derive with `wc -c CHANGELOG.md`, do not reconcile them); `methodology_trim.py --file CHANGELOG.md --check` says `[CHECK] trigger FIRES` on TRIGGER_BYTES (line headroom is fine, 50 records). It entered this session at 62,853 B with 2,683 B of headroom against a 1,540 B median entry, so this was structural, not this session's appetite. `HANDOFFS.md` (108,788 B) and `docs/planning/BACKLOG.md` (98,342 B) are over too, both inherited. (d) **BL-36 IS THE THING THAT SHOULD PRECEDE ANY TRIM**: 4 of 6 shipped `.verify.sh` losslessness proofs do not hold (S88 proved the ARCHIVES intact and the fault in the PROOFS) -- trimming again writes a seventh proof of a kind already known not to hold. (e) BL-37 half (b) and BL-39 both need a go-ahead; both edit distributed files. (f) `growth_run` is 7/10 and `.context-budget-history.jsonl` remains inherited/undefended. (g) **UPSTREAM SYNC RE-DERIVED AT THIS SESSION'S PHASE 0** and unchanged from S88-S91: `upstream/main` exactly 1 commit ahead (`512c2ed`), `git merge-tree --write-tree --name-only HEAD upstream/main` conflicts in exactly ONE path, `HANDOFFS.md`. RE-DERIVE before acting -- this session wrote to that file twice. (h) Long-tail NOT re-verified: BL-11/12/16/18/19/22/23/26/30/31/32.
key_files: `starter-kit/SESSION_RUNNER.md` (DISTRIBUTED; 52,386 -> 54,363 B) -- `:128` §Per-Phase Completion Criteria, `:133` the **The surface** bullet (the stated requirement), `:138` the paragraph anchoring it to gate (d) and stating the two-times relationship, `:142` §Planning Session Checklist, `:150` the checklist line as #75 drafted it, `:163` gate (d) itself (UNCHANGED -- cited, not moved; Test 36 mutant 3 guards it); `bin/tests.sh` (CANONICAL-ONLY) -- `:2244` Test 36's header and the argument for why it is a coupling guard, `:2266` `surface_check()`, `:2275` the `re.S | re.M` line with the bug that made it necessary recorded beside it, `:2284` the population guards, `:2301` `DANGLING-GATE-D`, `:2337` the coupling mutant; `starter-kit/FRAMEWORK_LEARNINGS.md:51` (Learning #29 -- DISTRIBUTED, append-only, `#14` stays reserved); `docs/planning/BACKLOG.md:990` (BL-39, including the do-not-re-derive note on `README.md:713`); `CHANGELOG.md` 2026-08 (the `[issue #75]` entry plus four `[ad hoc]` entries).
gotchas: **MY PHASE 1B CLAIM STUB WAS MALFORMED AND TURNED 3 GREEN TESTS RED -- READ THE STUB SCHEMA, DO NOT INFER IT.** I filled `what_was_done`/`next_steps`/`key_files`/`gotchas`/`runtime_smoke`/`changelog_ref`/`commit` with the literal `pending`, and `key_files: pending` fails `check-handoff`'s `path:line` lint even in stub mode. The 1B stub takes ONLY `session`, `date`, `status`, `active_task` (`starter-kit/HANDOFFS.md:26-28`; S91's own stub at `fe8bec0` is the worked precedent -- four keys, nothing else). **This was only diagnosable because S91 recorded an exact baseline (229/1) that I had reproduced at Phase 0**: the suite went to 232/4 and I could tell instantly the delta was MINE, not inherited. Capture the baseline before you write anything. **README.md:713 SAYS THE CHECKLIST IS "5-item" AND IT IS NOW 7. DO NOT "FIX" IT.** It sits under **"### What's New in v1.2"** -- frozen release notes where 5 was true as shipped; correcting it falsifies the history. The disposition is recorded in BL-39 so this is not re-derived a third time. **THE `check-learnings` MESSAGE READS "28 Learning row(s), contiguous 1..28" WHILE THE MAXIMUM ROW NUMBER IS 29** -- `#14` is reserved, so count and max legitimately differ by one and the tool's "1..N" is the COUNT, not the range. This is pre-existing and not a defect I introduced; do not "repair" the numbering. **A TEST THAT GREPS FOR THE LINES YOU JUST ADDED PROVES ALMOST NOTHING.** The failure this change actually risks is the referent moving, not the citation vanishing -- mutate the SOURCE. **THE PRE-EXISTING `dashboard_history.jsonl` AND `.context-budget-history.jsonl` MODIFICATIONS ARE STILL UNSTAGED**, inherited from before S91 and untouched by me; the underlying gap (tracked, append-only, staged by no protocol step) still wants a decision.
runtime_smoke: **SURFACE NAMED, per the requirement this session added -- and what it cannot enforce, stated.** n/a as an application launch; this repo ships no application, and the build-equivalent is the suites plus the tools themselves, RUN not merely built. **All figures measured at close-out BEFORE this receipt was appended** (appending it grows `HANDOFFS.md`); re-derive rather than trusting them. **SURFACE 1 -- the canonical checkout.** `bash bin/tests.sh` **235 passed / 1 failed** against the pre-change baseline of 229/1 captured at this session's own Phase 0, compared ROW-FOR-ROW by running the baseline `bin/tests.sh` from `a744218` against the current tree, with BOTH populations asserted non-empty first (230 and 236 rows): exactly **6 rows added -- all Test 36's -- and ZERO rows lost**. Sole failure is Test 9's pre-existing, unrelated github-source 404. `tools.test_methodology_dashboard` **300/300**; `tools.test_methodology_trim` **97/97**; `tools.test_context_budget` **41/41**. `bin/check-links` OK (88/22, unchanged -- the issue link is absolute so the relative count could not move); `bin/check-learnings` OK (28 rows, all citations resolve); `bin/check-handoff --all --allow-pending` OK (9 receipts). **SURFACE 2 -- A REAL ADOPTER TREE, which is the surface that actually matters for a DISTRIBUTED file and which surface 1 cannot speak for.** `bin/sync` into a fresh `git init` temp project: the delivered `SESSION_RUNNER.md` is **byte-identical** to canonical (`diff -q` clean), carries the new requirement at `:133` and `:150`, shows **7** checklist items in the adopter's own copy, and contains **2** occurrences of *"Faithful verification, per surface"* -- the citation's referent travels in the same file, so the reference cannot dangle at an adopter merely by being synced. **WHAT NEITHER SURFACE ENFORCES, stated rather than left for a successor to discover:** this was `--source=local`; the `--source=github` path is Test 9 and still red on a 404 that closes only when these files exist upstream, so NOTHING here verifies delivery over the network. Nor does it exercise an adopter whose `SESSION_RUNNER.md` is locally modified (held back by design, covered by Tests 5/6). **FM #28 GATE.** `CHANGELOG.md` **69,557 B / 65,536 B OVER** -- this session's doing, self-reduced 3,556 -> 3,156 B on the `[issue #75]` entry as far as facts allowed, and **re-measured after the final ledger write** rather than left at the mid-change 66,009 B an earlier draft of this receipt carried. `HANDOFFS.md` 108,788 B and `docs/planning/BACKLOG.md` 98,342 B over, both inherited, the first measured before this receipt was appended and so an UNDERSTATEMENT by roughly this block's size. `CLAUDE.md` and `FRAMEWORK_LEARNINGS.md` ok. Re-derive all five with `python3 starter-kit/context_budget.py`.
changelog_ref: CHANGELOG.md "2026-08-15 · [issue #75] The plan-time surface requirement — answered by lifting slice gate (d) out of the elective allowance it was fenced inside", plus "[ad hoc] S92 close-out", "[ad hoc] `CHANGELOG.md` crossed its byte ceiling this session", "[ad hoc] Framework Learning #29 appended", and "[ad hoc] BL-39 raised"
commit: a744218 (1B claim) + b1b7eaf (fix + Test 36) + this close-out commit
```
<!-- claim stub written at session start; reconciled at close-out -->
Model: Claude Opus 5 (1M context).

**Predecessor S91 scored 9/10.** **+** Its `runtime_smoke` gave an exact, reproducible baseline —
*"229 passed / 1 failed … sole failure is Test 9's pre-existing github-source 404."* I reproduced
229/1 at Phase 0 and it matched to the row. That single line is why my malformed claim stub cost
minutes instead of an hour: when the suite read 232/4 I knew immediately the three new failures were
mine. **+** Every number I independently re-derived held — 229/1, `check-links` 88/22, 27 learning
rows, 8 receipts, `upstream/main` exactly 1 ahead with `merge-tree` conflicting in exactly
`HANDOFFS.md`, `BACKLOG.md` at 95,979 B. I found **nothing wrong**, which is rare. **+** Its
gotchas were operational rather than narrative: the "vacuous green — guard both populations" warning
is the direct reason Test 36 has population guards at all, and those guards caught my own
`re.S`/`re.M` bug on the first run. Traceable ROI inside one session. **−** The one real gap, and
it is a costly one for *this* repo: **upstream issue #75 had been open for a day and the receipt
never mentions it.** Its `next_steps` enumerate the backlog and the upstream *sync* state but never
the issue tracker — so the item that best serves the fork's stated purpose (contributing upstream)
waited a session for the operator to surface it. **−** It did not pass on the 1B stub schema, though
its own stub was correctly minimal; a one-clause gotcha would have saved me three red rows. Not a
9.5 because of the first minus; not an 8 because the verification density is the highest I have
audited and it was the load-bearing input to everything I did.

**Self-score 8/10.** **+** I did not implement the issue as drafted. Grepping the corpus for prior
art *before* writing anything found gate (d) with zero inbound citations, which turned "add a rule"
into "route to the rule you already ratified" — a smaller change, better grounded, and it produced
the session's genuinely generalizable learning. **+** The guard mutates the *source*, not the
citation. A presence grep for my own added lines would have passed the one mutant that represents
the actual future failure. **+** I stated the surface of my own verification and what it cannot
enforce — the `--source=github` path is untested and I said so — which is the requirement this
session added, applied to itself rather than exempted. **+** I compacted my own ledger entry when I
found I had pushed `CHANGELOG.md` over its ceiling, instead of only reporting the breach, and
measured that the headroom (2,683 B) was structurally smaller than one entry plus close-out
regardless of me. **−** **I wrote the 1B stub from inference rather than reading its documented
schema, and it broke three tests.** The schema is nine lines away in `starter-kit/HANDOFFS.md` and
my predecessor's own stub was one `git show` away; I read neither. That is FM #3 (skim, retain the
gist) at the very first write of the session. **−** My first Test 36 was broken in exactly the way
the test itself exists to catch — an absence check over an empty population. It was caught in one
run only because I had written the guard first; had I written the guard second, as is more natural,
I would have shipped five vacuous greens. I got the ordering right for the wrong reason. **−** I
left `CHANGELOG.md` 473 B over its ceiling at the point of measurement (and further over once these
close-out entries landed). Compacting my entry was the proportionate move and a trim is genuinely a
session-sized deliverable, but the honest statement is that the ledger is out of budget and I handed
that forward rather than solving it.

