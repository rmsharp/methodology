# Handoff Receipts — durable close-out proof

This repository dogfoods its own methodology: every session records a durable, machine-checkable
`handoff` receipt here at close-out (Phase 3D), and Phase 0 reconciles it against `git log`. See
[`starter-kit/HANDOFFS.md`](starter-kit/HANDOFFS.md) for the block format and the write points, and
`bin/check-handoff` for the checker. Newest on top; prepend-only.

**Older receipts are archived.** This file currently holds **3** (recounted at S95's close-out,
2026-08-17 — recount with `grep -c '^```handoff' HANDOFFS.md` rather than trusting this number: it
read **3** from the S94 trim until this line was corrected, one close-out later, which is exactly the
span the note below predicts); the oldest **19**
(2026-07-08 → 2026-07-30) live in [`docs/archive/HANDOFFS-archive.md`](docs/archive/HANDOFFS-archive.md),
same format, same newest-on-top order. Archiving is safe by construction: `bin/check-handoff`
validates only the newest receipt, and Phase 0 reconcile is frontier-based, so neither reads past the
top of this file. `bin/model-report --handoffs <archive>` reaches the older prose when you need it.
Archive again — a new file, same format — when this one approaches ~1,200 lines, the trigger the
action ledger already uses.

**Two session sequences share this ledger and their numbers collide.** This fork and
`upstream/main` each run their own `S<N>` counter, so a receipt is identified by **session + date**,
never by number alone: upstream's **S7**/**S8** (both 2026-08-01) are different sessions from the
fork's **S7** (2026-07-09) / **S8** (2026-07-13) in the archive. At a resync the two sequences stay
separate and unrenumbered, each incoming receipt is checked against ours before it is kept, and
within a shared date the fork's receipts precede the arriving upstream ones (precedent: `fc4d297`).

> **The count above drifts between trims.** `methodology_trim.py` declares it a regenerated field
> (`starter-kit/methodology_trim.py`, the `HANDOFFS.md` `LedgerSpec`), so a **trim** rewrites it and
> the proof's L2 clause excuses that one span — but nothing updates it when a session **prepends** a
> receipt, which is most sessions. So it is right immediately after a trim and wrong from the next
> close-out onward: it read **6** from `7a71df0` until corrected three sessions later. That is
> [Learning #12](starter-kit/FRAMEWORK_LEARNINGS.md) pointed at this file, and it is the receipt-ledger
> half of upstream [issue #65](https://github.com/KJ5HST/methodology/issues/65). Recount before
> trusting it.

> **⚠ A trim of this file should retain at least THREE receipts — the tool's default cut does not
> know that.** `bin/tests.sh` Test 34 mutates *this* ledger to check `check-handoff --all`'s
> whole-ledger invariants, reading two mutation anchors from the live file: not hardcoded, so it
> survives *which* receipts rotate, but it needs three to exist. S94's first attempt took the
> budget-driven default, retained **2**, and the suite went 235/1 → **229/6**. Pass `--cut 3` (or
> more) explicitly, and re-run `bash bin/tests.sh` after any trim of this file.
>
> **Since S96 (2026-08-17) a short ledger is STATED rather than silent — which is not the same as
> fixed** (BL-40 (b)). The anchors are an asserted population: below the floor those six assertions
> print as `SKIP` rows naming themselves and the reason, the summary line carries a skip count, and
> a ledger with *zero* receipts still FAILS, because corruption is not rotation. A cut to two no
> longer reads as a checker regression, and it still leaves six invariants unexercised. **Nothing
> prevents that cut** — judging it worth the coverage is the reader's call, which is why the count
> is on the summary line.
**Archived 16 record(s), 2026-07-30 → 2026-08-02** into [`docs/archive/HANDOFFS-through-2026-08-02.md`](docs/archive/HANDOFFS-through-2026-08-02.md) — same format, same order, frozen.
Losslessness is proved by [`docs/archive/HANDOFFS-through-2026-08-02.md.verify.sh`](docs/archive/HANDOFFS-through-2026-08-02.md.verify.sh), which re-derives L1/L2/L3 from git; run it rather
than trusting this sentence. Written by `methodology_trim.py` v1.1.1.

**Archived 30 record(s), 2026-08-03 → 2026-08-09** into [`docs/archive/HANDOFFS-through-2026-08-09.md`](docs/archive/HANDOFFS-through-2026-08-09.md) — same format, same order, frozen.
Losslessness is proved by [`docs/archive/HANDOFFS-through-2026-08-09.md.verify.sh`](docs/archive/HANDOFFS-through-2026-08-09.md.verify.sh), which re-derives L1/L2/L3 from git; run it rather
than trusting this sentence. Written by `methodology_trim.py` v1.1.1.

**Archived 25 record(s), 2026-08-02 → 2026-08-11** into [`docs/archive/HANDOFFS-through-2026-08-11.md`](docs/archive/HANDOFFS-through-2026-08-11.md) — same format, same order, frozen.
Losslessness is proved by [`docs/archive/HANDOFFS-through-2026-08-11.md.verify.sh`](docs/archive/HANDOFFS-through-2026-08-11.md.verify.sh), which re-derives L1/L2/L3 from git; run it rather
than trusting this sentence. Written by `methodology_trim.py` v1.1.3.

**Archived 8 record(s), 2026-08-11 → 2026-08-15** into [`docs/archive/HANDOFFS-through-2026-08-15.md`](docs/archive/HANDOFFS-through-2026-08-15.md) — same format, same order, frozen.
Losslessness is proved by [`docs/archive/HANDOFFS-through-2026-08-15.md.verify.sh`](docs/archive/HANDOFFS-through-2026-08-15.md.verify.sh), which re-derives L1/L2/L3 from git; run it rather
than trusting this sentence. Written by `methodology_trim.py` v1.2.0.

**Archived 4 record(s), 2026-08-15 → 2026-08-17** into [`docs/archive/HANDOFFS-through-2026-08-17.md`](docs/archive/HANDOFFS-through-2026-08-17.md) — same format, same order, frozen.
Losslessness is proved by [`docs/archive/HANDOFFS-through-2026-08-17.md.verify.sh`](docs/archive/HANDOFFS-through-2026-08-17.md.verify.sh), which re-derives L1/L2/L3 from git; run it rather
than trusting this sentence. Written by `methodology_trim.py` v1.2.0.

```handoff
session: S101
date: 2026-08-23
status: pending
active_task: **Run the `HANDOFFS.md` trim** — S100's `next_steps` (a), assigned by the operator. Breach RE-DERIVED at Orient, not inherited: **98,284 B against 65,536 B, over by 32,748.** S100's own close-out added 15,318 B *after* it measured 82,966, so every `HANDOFFS.md` byte figure in that receipt — including its `--cut 3` estimate of 43,928 B — understates. Header 6,016 B; the five receipts run S100 18,430 / S99 17,179 / S98 17,161 / S97 20,085 / S96 19,408 B. Both ledgers reconciled with a **zero-commit gap**, so the P1 record-then-trim guard is satisfied and no backfill was written. `--check` FIRES on both triggers. **ONE NUMBER WORTH FIXING BEFORE THE TRIM MOVES IT:** `bin/check-handoff`'s per-record budget is 18,432 B and Test 34's retention floor is 3, so a post-trim ledger whose receipts sit at that budget measures 6,016 + 3 x 18,432 = **61,312 B — 4,224 B of headroom, under a quarter of one receipt.** The ceiling holds only while the mean receipt stays under **19,840 B** AND every session trims. S98's collision is therefore not repealed, merely not binding today; S100 skipped one trim and this file reached 98 KB.
next_steps: SIX DECLARATIONS, to be judged at close-out. **(1) THE TRIM IS THE ONE DELIVERABLE.** No tool edit, no `CHANGELOG.md` trim, no BL item raised — each is a second capability (FM #26). **(2) `--cut 3`, Test 34's floor**, confirmed by DRY RUN on the live file before any `--write`; the floor is read off this file's own front-matter warning, not assumed. **(3) THE TRIM COMMITS ALONE**, this receipt `status: pending` across it — S98's commit shape, restated by S100. **(4) LOSSLESSNESS IS PROVED BY RUNNING THE EMITTED `.verify.sh`, NOT BY TRUSTING THE TOOL'S SENTENCE.** The eleven proofs that already ship were run BEFORE any write: **7 green / 4 red**, the reds being the `*-2026-08-02` and `*-2026-08-09` pairs on both ledgers — that is the control, and the trim must redden none of the seven. Suite baseline captured pre-change too: **280 rows, 279 passed / 1 failed**, the sole failure Test 9's pre-existing `--source=github` 404, confirmed BY NAME. **(5) CARVE-OUT, DECLARED AT CLAIM AND DELIBERATELY NOT NARROWED THE WAY S100'S WAS.** The trim touches NO distributed file: it writes `HANDOFFS.md`, one new `docs/archive/` shard and its proof, all canonical-only. `starter-kit/FRAMEWORK_LEARNINGS.md` IS distributed and Phase 3C may mandate a row there — declared here rather than discovered at 3C, with its **2,410 B** of headroom budgeted before anything is written. **(6) NO OUTWARD-FACING ACTION** — no PR, no push, no issue, no comment, no tag. Upstream issue #75's prepared answer stays unsent; this makes eleven sessions.
```

```handoff
session: S100
date: 2026-08-23
status: complete
self_score: 8
predecessor_score: 8
active_task: **BL-41 — `methodology_trim.py` could not trim `HANDOFFS.md` at all. COMPLETE**, by the operator's choice among **four** costed levers put to him at Phase 2 (BL-41 listed three; the fourth was created by my own claim commit). `TRIM_VERSION` 1.2.0 -> **1.3.0**. **The defect, stated more precisely than BL-41 stated it:** the shard name is a function of a record DATE while the cut is POSITIONAL, so the naming function is **NOT INJECTIVE** — distinct cuts derive one name whenever two records share a date — yet it was used as a unique key behind a write-once refusal. The advertised escape, *"Disambiguate with `--cut`"*, **could not work by construction**: a date cut key both SELECTS the records and BECOMES the name. **VERIFIED ON THE LIVE CASE AS A DRY RUN:** `--cut 3` — the cut BL-41 recorded as impossible — now archives to `docs/archive/HANDOFFS-through-2026-08-17-2.md`, retaining Test 34's floor of three, **82,966 B -> 43,928 B, 21,608 B under the ceiling** (more than a full receipt of headroom, which is the durable answer S96 and S98 were both reaching for). **NO TRIM WAS RUN** — a second capability (FM #26), scoped out by the operator at Phase 2. ALL SEVEN CLAIM DECLARATIONS HELD EXCEPT (6), WHICH I BROKE AND SAY SO BELOW. NO OUTWARD-FACING ACTION: no PR, no push, no issue, no comment, no tag. Upstream issue #75's prepared answer remains unsent — now ten sessions.
what_was_done: Three commits — `f4d0399` (1B claim), `9ca9e0f` (THE DELIVERABLE), this close-out. **(1) RESEARCH KILLED ONE OF BL-41'S THREE OPTIONS BEFORE THE OPERATOR COULD PICK IT, AND DEMOTED A SECOND.** Option (2), naming the shard by record range (`HANDOFFS-S96-S97.md`), is **unsound for this ledger**, and the disproof is in its own front matter ~30 lines above where S99 was working: *"Two session sequences share this ledger and their numbers collide"* — upstream's S7/S8 are different sessions from the fork's. It reintroduces the same non-injectivity on another axis, needs a per-spec identity extractor (`LedgerSpec` carries only `date_of_record`), and has **no answer for `CHANGELOG.md`**, whose records have no identity but their date. Option (3), dropping Test 34's floor, **does not fix the defect** — it removes the constraint that made *this instance* unsatisfiable while the non-injective naming survives and recurs at the next shared date — and pays six whole-ledger invariants for that. Both arguments are recorded in the BL-41 completed row so they are not re-litigated. **(2) THE FOURTH OPTION, WHICH MY OWN CLAIM CREATED.** Adding the 5th receipt moved the arithmetic: `--cut 2` began deriving `HANDOFFS-through-2026-08-18.md`, a FREE name (82,966 -> 26,758 B) — clearing the ceiling with **no code change**, costing six invariants only TEMPORARILY (Test 34 skips them aloud; they return at three receipts). Offered as the stopgap it is, correctly refused as a fix. **(3) THE FIX, AND THE HALF OF IT NOBODY ASKED FOR.** A taken name resolves to `-2`, `-3`, … and the rename is REPORTED (`SHARD_NAME_DISAMBIGUATED`) — a shard whose name no longer uniquely says *through this date* must not arrive silently. Write-once is UNCHANGED: nothing is overwritten, and past `SHARD_SUFFIX_MAX` (99) it still refuses. **`shard_name_taken()` checks the `.verify.sh` sibling too**: the pair is written together, an interrupted run can leave the proof without its shard, and a shard-only check would overwrite a frozen proof — the same corruption, one file over. M1/M2 narrow it both ways and both die. **(4) A SECOND DEFECT FOUND, NOT FIXED** — next_steps (b).
next_steps: (a) **`HANDOFFS.md` IS STILL OVER (82,966 B) AND THE TRIM IS NOW AVAILABLE — RUN IT, AND RE-DERIVE EVERY NUMBER HERE INCLUDING MINE.** `python3 starter-kit/methodology_trim.py --file HANDOFFS.md --cut 3` archives 2 records to `HANDOFFS-through-2026-08-17-2.md`. My 43,928 B was measured BEFORE this receipt existed, so it understates by roughly this block; re-derive with the method, not the number: `idx=[i for i,l in enumerate(lines) if l.startswith('```' 'handoff')]`, then `len('\n'.join(lines[:idx[N]]).encode())` is the live size retaining N. **The P1 guard blocks a trim until the ledger is reconciled — record first, then trim** — and a trim must be its OWN commit with the receipt left `status: pending` across it. Re-run `bash bin/tests.sh` after. (b) **A SECOND DEFECT, FOUND AND NOT FIXED — in a DISTRIBUTED file, so it needs a go-ahead.** `starter-kit/context_budget.py:371` `ledger_dimension()` reports a read-mandated ledger in LINES whenever nothing has fired, by explicit design (*"with nothing fired, the file's class decides"*). The fired case is right; the **not-yet-fired case is the blind spot, and it is the one a session planning headroom reads.** `CHANGELOG.md` entered this session showing `894 ln / 2,000 ln — ok` (45%) while its BYTE axis stood at **65,139 / 65,536 — 99.4%, 397 B**. My Phase 0 report published that harmless-looking row. **Not raised as a BL item** — I would have had to re-open the file S99 just brought under its ceiling. Disagree and raise it. (c) **`CHANGELOG.md` IS NOW OVER BY 1,254 B (66,790 / 65,536), KNOWINGLY.** I compacted my claim entry to 378 B to fit the 397 B; the deliverable entry spent the rest. FM #27 requires the entry, so the breach is structural — but I spent the last of that runway. **Its trim has NO name collision** and the trigger fires, so the tool repaired here is its remedy too. (d) **`starter-kit/FRAMEWORK_LEARNINGS.md` IS AT 63,126 / 65,536 — 2,410 B, ONE ORDINARY ROW.** Learning #36 cost 1,436 B. Most imminent DISTRIBUTED breach here; S96 flagged it at 604 B against a 60,000 ceiling, which was then raised. Budget the row BEFORE writing it. (e) **S99'S OWED LEARNING IS STILL OWED AND ITS NUMBER HAS MOVED** — *a losslessness proof only proves losslessness of the population it enumerates*. S99 predicted #36; #36 is now mine, so it is **#37**. Captured in code (`BACKLOG-DETAIL.md.verify.sh` C5's comment), not in the table. Two rows would have left the next session ~900 B, so I left it. Two sessions old. (f) **BL-36 IS 4 OF 9, NOT THE INHERITED 4 OF 6** — re-measured: 11 proofs exist (9 archive, 2 planning), 7 green / 4 red, the reds being the `*-2026-08-02` and `*-2026-08-09` pairs on both ledgers. `HANDOFFS-through-2026-08-17.md.verify.sh` — the blocker's own proof — is GREEN, so the collision was never entangled with BL-36. (g) `upstream/main` still 1 ahead (`512c2ed`), fork 370 ahead; `git merge-tree` at Orient conflicted in exactly `CHANGELOG.md` and `HANDOFFS.md`, and I wrote to both — RE-DERIVE. (h) Upstream [issue #75](https://github.com/KJ5HST/methodology/issues/75) is the only open upstream issue; S92's answer prepared, vetted, unsent, ten sessions. (i) BL-37 (b), BL-39 and the long tail unchanged, NOT re-verified. **BL-16 has no heading of its own by design**: open list 19, index 19 rows, detail file 18 headings — that arithmetic is correct, and a naive re-derivation calls it a phantom mismatch.
key_files: **Every line number below was re-derived at close-out against the committed tree.** `starter-kit/methodology_trim.py` (**the one DISTRIBUTED file**) — `:50` `TRIM_VERSION = "1.3.0"` and the rationale chain (grep `TRIM_VERSION = `); `:100` `SHARD_SUFFIX_MAX = 99`, the bound (grep `SHARD_SUFFIX_MAX = `); `:394` `shard_name_taken()`, the pair-aware predicate, placed beside `_safe_cut_key` because both guard the same name (grep `def shard_name_taken`); `:1755` the disambiguation loop and the retained `SHARD_EXISTS` refusal at the bound (grep `while shard_name_taken`); `:1779` the `SHARD_NAME_DISAMBIGUATED` advisory (grep `SHARD_NAME_DISAMBIGUATED`). `tools/test_methodology_trim.py` (**canonical-only**) — `:708` the BL-41 block header stating what must NOT change (grep `BL-41: the shard name is not injective`); `:719` the fixture control asserting the collision EXISTS; `:806` the NARROWED shard-only check; `:846` `test_the_bound_refusal_is_FATAL_and_not_merely_reported`, **the one test there that asserts on exit status, the departure labelled in its own docstring** (grep `THE ONE PLACE THIS FILE ASSERTS`). `starter-kit/FRAMEWORK_LEARNINGS.md:64` — Learning **#36**, 1,436 B, four columns. `docs/planning/BACKLOG.md:9` the open list, now 19 items with BL-41 removed; `:165` BL-40's completed row and `:166` BL-41's, which carries the arguments against options (2) and (3) so they are not re-litigated. `CHANGELOG.md` — the claim entry and the deliverable entry.
gotchas: **A `check-*` TOOL CAUGHT TWO DEFECTS IN MY OWN LEARNING ROW THAT I DID NOT.** My first draft was a **2-column row** — the malformation BL-35 existed to fix, which lived undetected 40+ sessions — my second was **237 B over budget**. The table is FOUR columns (`# | Learning | Source | When to Apply`) and the budget covers the WHOLE row. Read a neighbouring row before appending; do not infer the schema from a truncated grep. **PER-ROW 1,500 B; PER-RECEIPT 18,432 B (`bin/check-handoff:606`), measured FENCE-TO-NEXT-FENCE WITH THE PROSE INCLUDED** — I budgeted against the fence alone and was 2,561 B out. **DO NOT SPLICE BY LINE INDEX WITHOUT AN ASSERT.** One of mine was off by one; the assert fired instead of the edit landing. Anchor on CONTENT and assert the match count is exactly 1. **DO NOT PUT A ```handoff FENCE INSIDE A PYTHON TRIPLE-QUOTED STRING** — my first claim-stub script died on it (S96 hit the heredoc-in-heredoc twin). Write the payload to a file and splice it. **`BACKLOG-DETAIL.md.verify.sh` C1/C2/C4 QUANTIFY ONLY OVER THE 18 ITEMS OPEN AT `384b17c`.** BL-41 was raised after the split, so it sits in `added` and its body is NOT byte-pinned — which is why closing it did not redden the proof. Do not generalise that to any item present at the split: editing one of those 18 WILL turn C2 red. I verified by re-running, not by reading. **`bin/check-*` ARE PYTHON. DO NOT `git reset --hard`** — two tracked `.jsonl` telemetry files go dirty from Phase 0 alone; I committed them with the claim rather than leaving them. **`check-learnings` READS "35 row(s), contiguous 1..35" WHILE THE MAXIMUM ROW IS 36** — `#14` is reserved, so count and max differ by one. Pre-existing; do NOT "repair".
runtime_smoke: **FOUR SURFACES.** This repo ships no application; the build-equivalent is the suites plus the tools, RUN. Figures measured after the last non-receipt edit and BEFORE this receipt, so every `HANDOFFS.md` byte number understates by roughly this block. **SURFACE 1 — the suites.** `bash bin/tests.sh` **279 / 1 / 0**, diffed ROW-FOR-ROW against a 280-row Orient baseline, both populations non-empty: **ZERO ROWS LOST**, re-run at the FINAL close-out state so this figure does not rot: **5** differing rows, every one carrying a count or id in its own name — receipts 4->5, `**Model:**` 20->23, and two fixture controls whose synthetic row/id moved (#36->#37, S100->S101) because my own row and receipt now exist. None is the fix's doing. Sole failure is Test 9's pre-existing `--source=github` 404, confirmed BY NAME. Python suites **451/451** (was 444; +7 net = 8 new tests less the refusal test they replace). `check-links` OK (88/22); `check-learnings` OK (35 rows); `check-handoff --all` OK (5 receipts). **SURFACE 2 — RED first, against COPIED fixtures in `tempfile` repos, never the live ledger.** Before the fix: **3 failures + 2 errors**. The **fixture control** (*the derived name really is taken*) and the **free-name negative control** (*an unforced suffix is a bug*) were GREEN on BOTH sides, as they must be — they assert what did not change. **SURFACE 3 — mutation, on my own new guard.** 9 mutants, each verified to APPLY before running (`DID-NOT-APPLY` kept distinct from `survived`, by `filecmp` against the pristine copy), **9/9 KILLED**, unmutated control green before and after, every restore `cmp`-verified. M1/M2 narrow `shard_name_taken` to each half; M3 `while`->`if`; M4/M5 delete then over-fire the advisory; M6 off-by-one at the bound; M7 writes the base name anyway; M9 removes the bound. **M8 — dropping `exit_code=2` from the bound refusal — SURVIVED THE FIRST ROUND** and is the one that mattered: it changes nothing a finding-only test can see, and a scripted caller would read a refusal as success. Killed by a new test, and the departure from the file's no-exit-code rule is labelled in that test's own docstring. **The round was re-run after the fix: 9/9.** **SURFACE 4 — the shipped proofs and the real case.** All 11 `.verify.sh` proofs run: **7 green / 4 red, identical to baseline** — the fix reddens nothing, and both backlog proofs stayed green across the BL-41 closure. `--cut 3` on the live ledger, DRY RUN: `SHARD_NAME_DISAMBIGUATED`, L1_OK, L2_OK, L3_OK, 82,966 -> 43,928 B; `git status --porcelain` confirmed it wrote nothing. **CARVE-OUT, verified mechanically:** 26 `bin/_manifest.py` DISTRIBUTION source rows vs 2 changed paths at the deliverable commit, both non-empty, **intersection exactly `starter-kit/methodology_trim.py`** — the one file declared at claim. **DECLARATION (6) WAS THEN BROKEN AT PHASE 3C:** Learning #36 makes `starter-kit/FRAMEWORK_LEARNINGS.md` a SECOND distributed file. Phase 3C mandates that row here, so the declaration was too narrow when written; recorded rather than quietly widened. **FM #28 GATE:** `CLAUDE.md` ok; `docs/planning/BACKLOG.md` ok (184 ln); `starter-kit/FRAMEWORK_LEARNINGS.md` ok at **63,126 / 65,536 — 2,410 B**; **`CHANGELOG.md` OVER by 1,254** (66,790); **`HANDOFFS.md` OVER by 17,430** (82,966), not trimmed, by the operator's scoping. `growth run 40/10` is NOT evidence here — it is computed on `resident_bytes`, which in this repo is `CLAUDE.md` alone. **WHAT NONE OF THIS ENFORCES:** no test quantifies over `LEDGERS`, so nothing checks that a FUTURE ledger added there gets shard-name disambiguation. The behaviour is in shared code and would apply; that is an inference, not a test.
changelog_ref: CHANGELOG.md "2026-08-23 · [BL-41] `methodology_trim.py` disambiguates a taken shard name instead of refusing"
commit: f4d0399 (1B claim) + 9ca9e0f (the deliverable) + this close-out
```
<!-- claim stub written at session start; completed at close-out -->
Model: Claude Opus 5 (1M context).

**Predecessor S99 scored 8/10.** **+** **BL-41 reproduced exactly, to the finding code** — I re-ran all
three cuts and every claim held. It also recorded that no `--write` had been issued, so I could reproduce its
evidence rather than rebuild it — which is why this session reached Phase 2 with the diagnosis
already half-made. **+** **Naming which of its three fixes touched a DISTRIBUTED file made the operator's choice a
real choice** rather than an implementation detail I would have settled alone. **+** Its
gotchas changed what I did — the P1 guard's record-then-trim ordering, and *do not `git reset
--hard`* — and its `key_files` line numbers held (`docs/planning/BACKLOG.md:9`, `:16`). **−** **Its option (2) is contradicted by the front matter of the very file it was writing about,
~30 lines above where it was working.** `HANDOFFS.md` states that two session sequences share the
ledger and their `S<N>` numbers collide, so naming shards by record range reintroduces the same
non-injectivity on another axis. One grep would have killed it before it reached me. **−** **It knew `CHANGELOG.md` was at the edge of its byte ceiling and did not hand that
forward.** Its own receipt records compacting its prose back under (66,990 -> ok); what it left was
**397 B** of runway, and none of its ten `next_steps` mentions it. Any entry the next session wrote
was guaranteed to breach — and mine did, at claim. That is precisely the miss S99 itself docked S98
for: a forward-looking number that was computable, was computed, and was not passed on. Not a 9 for
those two; not a 7, because everything it handed me was accurate, its warnings changed what I did,
and its central finding survived re-execution unchanged.

**Self-assessment: 8/10.** **+** **The research killed one of the three options before the operator
could choose it and demoted a second**, so the fork he got was four honest levers, not three plus a
trap. **+** I found a fourth option BL-41 does not list — a one-time `--cut 2`, no code
change — created by my own claim commit, and offered it as the stopgap it is. **+**
RED first against copied fixtures with both controls green on either side; 9/9 mutants killed with
apply-verification and `cmp`-verified restores; **zero rows lost** on a row-for-row suite diff.
**+** When M8 survived I killed it rather than booking it as an accepted survivor, labelling the
convention I broke. **+** I checked the namespace before adding two module globals, and answered
BL-36's count by measuring (4 of 9) rather than repeating the inherited 4 of 6. **−** **My claim's declaration (6) was wrong when I
wrote it, and I broke it.** I declared exactly one distributed file while Phase 3C mandates a
Learnings row in a second. I noticed at 3C, not at claim. **−** **Two defects in one 1,436 B table row, and a checker found both — I
found neither.** The first draft was a 2-column row, the exact malformation BL-35 existed to fix;
the second was 237 B over budget. I inferred the schema from a truncated grep instead of reading a
neighbouring row — this repo's own *read the fixture before the code* lesson, missed. **And a third:
I budgeted this receipt against the fence block alone (16,108 B) when a record is fence-to-next-fence
with the prose included; `check-handoff` corrected me by 2,561 B.** **−** **I left `CHANGELOG.md` 1,254 B over its ceiling** — structural (FM #27 required the entry
and 19 B remained after my own compaction), but I spent the last of that runway. **−** I did not discharge S99's owed Learning, now two sessions
old — defensible on scope, still a debt I chose not to pay. Not a 9 because four of my own
artifacts were malformed on first construction — a 2-column row, an over-budget row, an
off-by-one splice, and a receipt budgeted against the wrong unit — and my claim mis-scoped its own
blast radius; not a 7 because a control I ran caught every one, and the fix was mutation-proven
9/9 against a zero-loss row diff before I believed it.

```handoff
session: S99
date: 2026-08-18
status: complete
self_score: 8
predecessor_score: 8
active_task: Bring `docs/planning/BACKLOG.md` under its 65,536 B FM #28 ceiling. COMPLETE. **103,755 B -> 26,504 B, reports `ok`** -- the first time this file has been under its ceiling since the gate began measuring it. The remedy is a SPLIT, chosen by the operator from four costed levers I put to him at claim: item bodies moved VERBATIM to `docs/planning/BACKLOG-DETAIL.md` (read on demand, no ceiling), leaving a 19-row index. **NOTHING WAS COMPACTED, REWORDED OR DROPPED**, which is why this never reached the operator policy question `.context-budget.json:73` raises -- that question stands unanswered for whoever wants the DETAIL file smaller. ALL SIX CLAIM DECLARATIONS HELD: (1) no open-item content edited before the operator picked; (2) non-human readers enumerated FIRST, and one bit -- see gotchas; (3) instrument audited before any number was published; (4) commit shape claim -> deliverable -> [ledger, forced by the P1 guard] -> close-out; (5) carve-out verified mechanically, **intersection NONE**; (6) NO OUTWARD-FACING ACTION -- no PR, no push, no issue, no comment, no tag. Upstream issue #75's prepared answer remains unsent.
what_was_done: Four commits -- `384b17c` (1B claim), `8eb4f0e` (THE DELIVERABLE), `84b86b6` (the ledger entry the P1 guard demanded), this close-out. **(1) THE MEASUREMENT THAT CHOSE THE AXIS, AND ITS REFUSAL TO PATTERN-MATCH.** S97 and S98 each re-derived a sibling ledger OFF the whole-file axis after measuring 1 whole read in 80 and 81 transcripts. The obvious move was to do it a third time. Measured instead: across **82 transcripts (mine excluded)** this file was read **WHOLE 23 times in 22 of them -- 27%** -- against **584 partial reads**, most recently 2026-08-13. That is ~22x either sibling, because Phase 0 step 3 asks it for *current priorities*, which are not localised to one record. **So the whole-file ceiling is the RIGHT axis here and was RETAINED**, and the budget entry now says so in its own words so the next session does not copy the per-record remedy onto it. **MY INSTRUMENT WAS WRONG FOUR WAYS AND NO WRONG NUMBER LEFT THE SESSION:** `\bcat\b` matched `git cat-file -e` (an existence test, no content); a bare `BACKLOG` match swept `airqino/` and `model_project_constructor/` copies into "whole reads"; splitting on `;` cut `sed '1,5p;90,100p'` mid-quote and stranded real reads as UNCLASSIFIED; and two further "whole reads" were the word *cat* inside receipt PROSE, caught only by printing the raw pipeline text. **(2) NO HOUSEKEEPING CUT COULD HAVE WORKED, established at claim.** Open bodies were 81,340 B = 98% of the section and **1.24x the whole ceiling alone**; deleting the front matter, all 11 archive pointer rows and the historical section (20,716 B, everything not live work) still left it 17,503 B over. **(3) LOSSLESSNESS IS PROVED.** `docs/planning/BACKLOG-DETAIL.md.verify.sh` re-extracts each item from git **by its own `BL-N` identity, never by position** -- the flaw that makes 4 of 6 generated proofs read as data loss over intact archives (BL-36). C1 identity, C2 byte-exact bodies, C3 the move really happened, C4 reachability, C5 the section preamble. **7 mutants, each verified to APPLY, 7/7 KILLED** against a passing unmutated control, tree restored byte-identical after each. **(4) BL-41 RAISED** -- the trimmer cannot trim `HANDOFFS.md` at all; see next_steps (a).
next_steps: (a) **`HANDOFFS.md` IS OVER ITS CEILING AT 67,398 B AND I DID NOT TRIM IT, BECAUSE THE TOOL WILL NOT -- THIS IS BL-41, THE SESSION'S SECOND FINDING.** A shard is named `HANDOFFS-through-<date of the NEWEST ARCHIVED record>.md`; `docs/archive/HANDOFFS-through-2026-08-17.md` already exists from S98. Records are S96 (08-17), S97 (08-17), S98 (08-18), S99 (08-18). `--cut 3` (Test 34's floor) archives S96 -> that exact name -> **refused**; `--cut 2` -> same name -> refused; `--cut 1` is free but retains ONE, below the floor S98 explicitly warned to respect. **Every admissible cut yields the taken name, so the tool's own advice, "Disambiguate with `--cut`", has no solution.** All three verified as DRY RUNS against the live ledger; no `--write` issued. It unblocks itself only when six receipts accumulate (~2 sessions) and a floor-respecting cut spans past 2026-08-18 -- i.e. the ledger must overshoot further before the remedy for overshoot becomes available. Three candidate fixes are written up in BL-41; **one touches a DISTRIBUTED file and needs a go-ahead.** (b) **The breach is NOT the split's doing and not this session's deliverable slipping** -- it is my protocol-mandated claim receipt landing in a ledger S98 closed with 2,863 B of headroom against receipts that run 17-20 KB. Any session writing any receipt would have done it. (c) `docs/planning/BACKLOG.md` is now **ok at 26,504 B**; the gate's only red is `HANDOFFS.md`. (d) **A LEARNING IS OWED AND I DID NOT WRITE IT** -- *a losslessness proof only proves losslessness of the population it enumerates* (see gotchas). `starter-kit/FRAMEWORK_LEARNINGS.md` is DISTRIBUTED and my claim declared I expected to touch no distributed file; I held to that rather than quietly widening it. Learning #36 is the next session's, at 61,689 / 65,536 B with a 1,500 B row budget. (e) `upstream/main` still 1 ahead (`512c2ed`), fork now 368 ahead; conflicts remain exactly `CHANGELOG.md` and `HANDOFFS.md`, and I wrote to both -- RE-DERIVE. (f) **Upstream [issue #75](https://github.com/KJ5HST/methodology/issues/75) IS STILL OPEN**, S92's answer prepared, vetted, unsent -- eight sessions waiting on a go-ahead for the maintainer's own issue. (g) S98's (f) undercounted: **TWO** `.context-budget.json` entries cited `methodology_trim.py:69`, not one. I corrected `:73`'s copy because I was rewriting that sentence, and left `:45`'s (FM #17). (h) BL-36's four red proofs, BL-37 (b), BL-39 and the long tail unchanged, NOT re-verified.
key_files: `docs/planning/BACKLOG.md:9` (FORK-ONLY) -- the hand-maintained open list, now 19 items incl. BL-41; `docs/planning/BACKLOG.md:16` the re-derivation grep, **repointed at the DETAIL file** because run against this file it now matches nothing, a wrong answer rather than an obvious failure; `docs/planning/BACKLOG.md:84` the restored Routing block; `docs/planning/BACKLOG.md:117` the index table. `docs/planning/BACKLOG-DETAIL.md:3` (FORK-ONLY, new, 86,086 B) -- what the file is and why it has no ceiling; `docs/planning/BACKLOG-DETAIL.md:16` the editing rule, and that the `<a id="bl-N">` anchors are the proof's record separators. `docs/planning/BACKLOG-DETAIL.md.verify.sh:23` `BASE_SHA="384b17c"`; `docs/planning/BACKLOG-DETAIL.md.verify.sh:62` `detail_bodies()` splitting on anchors; `docs/planning/BACKLOG-DETAIL.md.verify.sh:83` C1 and **why growth is REPORTED and only loss FAILS**; `docs/planning/BACKLOG-DETAIL.md.verify.sh:130` C5 and the loss that caused it. `.context-budget.json:68` (FORK-ONLY) -- the structure pattern now anchored on the detail-file link so it matches the 19 index rows and NOT the 22 `**BL-N**` pointer rows beside them; `.context-budget.json:73` the read-pattern measurement and the RETAINED-ceiling argument. **EVERY LINE NUMBER ABOVE WAS RE-DERIVED AT CLOSE-OUT; my first draft had all seven wrong** (`:14`/`:76`/`:99`/`:29`/`:63`/`:86`/`:120`), and only `bin/check-handoff` refusing a receipt with no `path:line` token sent me back to look.
gotchas: **A LOSSLESSNESS PROOF ONLY PROVES LOSSLESSNESS OF THE POPULATION IT ENUMERATES.** C1-C4 were all green while my first cut **silently dropped 1,684 B** -- the `## Open items` preamble, *"Routing — what a session can actually run today"*, which is the standing record that the *"blocked on the paused channel"* disposition **WAS NEVER IMPOSED** (`CLAUDE.md`'s own rule about unattributed blockers; losing it is how that defect returns). It is not an item body, so it was outside the population the proof counted. I found it by noticing an unused variable in my own split script -- **no check told me**. Restored verbatim, and C5 now asserts it; M7 confirms C5 catches exactly that deletion. **A PROOF THAT FORBIDS CORRECT FUTURE USE IS A FALSE-POSITIVE GENERATOR.** My C1 first failed on any item NOT in the pre-split set -- so the next session to raise a backlog item would have turned it red, which is the BL-36 class of defect I wrote it to avoid. Fixed before shipping: **losses FAIL, growth is REPORTED**. Raising BL-41 immediately exercised it (`1 raised since (not a finding): BL-41`). **THE INDEX MUST NOT DECLARE A `Status` COLUMN.** `tools/methodology_dashboard.py:_scan_backlog_done` reads this file; one Status header flips its format from `unrecognized` to `table`, at which point `_count_table_done` starts counting the §Completed rows' `CLOSED`/`SHIPPED`/checkmark tokens as **unmigrated done-marks** on a file whose completed items are properly migrated. Baseline captured before the edit and asserted unchanged after. **THE ARCHIVE PROOF READS THE LIVE FILE.** `BACKLOG-archive-2026-08-15.md.verify.sh` C4 requires the shard be NAMED in `BACKLOG.md` and all 11 archived items keep a `**BL-N**` pointer row -- so deleting §Completed items, the most obvious dead weight, turns a green proof red. **THE P1 GUARD BLOCKS A TRIM UNTIL THE LEDGER IS RECONCILED**, by design: a trim advances the frontier and would hide the unrecorded commit permanently. Record, then trim. **`bin/check-*` ARE PYTHON. DO NOT `git reset --hard`** -- two tracked `.jsonl` telemetry files go dirty from Phase 0 alone.
runtime_smoke: **THREE SURFACES.** This repo ships no application; the build-equivalent is the suites plus the tools, RUN. **SURFACE 1 -- the suites.** `bash bin/tests.sh` **279 passed / 1 failed / 0 skipped**, and diffed ROW-FOR-ROW against the pre-change baseline captured at Orient, both populations asserted non-empty (280 and 280): **ZERO ROWS LOST**. The only 3 differing rows are assertions carrying counts in their own names -- receipts 3->4 and `**Model:**` entries 17->18 -- both my claim commit, not regressions. Sole failure is Test 9's pre-existing `--source=github` 404, confirmed BY NAME (`starter-kit/FRAMEWORK_LEARNINGS.md` and `starter-kit/methodology_trim.py` do not exist upstream yet). `tools.test_methodology_dashboard` + `_trim` + `_context_budget` **444/444**. `check-links` OK (88/22, unchanged); `check-learnings` OK (34 rows; note count 34 vs max row 35 -- `#14` is reserved, pre-existing, do NOT "repair"); `check-handoff` OK. **SURFACE 2 -- the new proof, proven RED before being trusted.** 7 mutants, each verified to APPLY (a no-op is reported as MUTATION DID NOT APPLY, never as "survived"), **7/7 KILLED** against a 5-check passing control: one byte inside a body (C2); an item deleted (C1); a COPY instead of a move (C3); an index row removed (C4); the detail file never named (C4); the anchors renamed so the extractor matched **zero** records -- killed by the empty-population control, which is what stops the proof passing vacuously; and the Routing block deleted (C5). Tree restored byte-identical after every one. **SURFACE 3 -- the budget gate.** Repaired its structure check and then MUTATED it: breaking the index links takes it from `ok` to `instrument-failed`, so the check is not a comment shaped like a guard. `docs/planning/BACKLOG.md` **ok, 26,504 B / 65,536**; `CLAUDE.md` ok; `CHANGELOG.md` ok; `starter-kit/FRAMEWORK_LEARNINGS.md` ok. **`HANDOFFS.md` OVER -- ~79.6 KB against 65,536, not trimmed, BL-41, see next_steps (a). RE-DERIVE with `python3 starter-kit/context_budget.py` rather than trusting that figure: it measures the file this sentence is inside, so every edit to the sentence moves it, and it moved twice while I wrote it.** It was +1,862 at claim; this receipt is the rest, and it is within the per-record budget, so the overage is the FOURTH RECORD existing at all rather than any record being too big. **`CHANGELOG.md` crossed its own ceiling from my two entries and I compacted MY OWN PROSE back under it** (66,990 -> ok) rather than trimming a ledger, which was neither declared nor needed -- the detail belongs in this receipt, not restated in the action ledger. **WHAT NONE OF THIS ENFORCES:** `check-links` covers only DISTRIBUTED files, so my 19 index links are checked by nothing in CI -- I verified all 19 resolve to real `<a id>` anchors, with both populations asserted non-empty, but that check lives in this receipt and not in a suite. **CARVE-OUT, verified mechanically:** 26 `DISTRIBUTION` source rows vs 6 changed paths, both non-empty, **intersection NONE** -- no distributed file touched, as declared.
changelog_ref: CHANGELOG.md "2026-08-18 · [ad hoc] `docs/planning/BACKLOG.md` comes under its ceiling by SPLIT — index here, bodies in a read-on-demand sibling"
commit: 384b17c (1B claim) + 8eb4f0e (the deliverable) + 84b86b6 (the ledger entry) + this close-out
```
<!-- claim stub written at session start; completed at close-out -->
Model: Claude Opus 5 (1M context).

**Predecessor S98 scored 8/10.** **+** Its `next_steps` (b) named this file as the strongest
candidate and, more usefully, handed me the constraint that shaped the whole session: S89's
measurement that the residual is *live work*, and the conclusion that compacting it is an operator
call. That is why I put a policy fork to the operator instead of deciding inside an implementation,
and why the split — which abandons nothing — was worth finding. **+** Its gotchas were load-bearing
and I followed them concretely: a trim must be its own commit with the receipt left `pending`; re-run
the suite after any trim; `check-learnings` reads 34 rows against a maximum of 35 and must not be
"repaired". **+** The per-record budget it built ran on me from my claim commit onward and did its
job — this receipt was measured against it twice. **−** **Its (b) framed the remedy as compaction
versus nothing, and the lever that actually worked was neither.** The split is this repository's own
ratified pattern — `CLAUDE.md` → `docs/RELEASE_HISTORY.md`, described in `CLAUDE.md` itself in the
words *"the always-loaded file stays scannable, the accumulated record moves to a sibling read on
demand"* — and `.context-budget.json`'s `_deliberate_exclusions` already recorded that sibling as
deliberately unbudgeted. Both were one grep from the file it was writing about. **−** **It left this
ledger 2,863 B from its ceiling while knowing receipts run 17–20 KB.** Its (a) reasoned carefully
about how many *trims* the header allowance covers, which is the right question about the guard's
derivation and the wrong one about the ledger: the binding runway was headroom for the next receipt,
and that was about one sixth of one. The very next session was guaranteed to breach, and did, at
claim time. That is the same class of miss — a forward-looking number that was computable and not
computed — that S98 itself docked S97 for. Not a 9 for those two; not a 7, because everything it
handed me was accurate, its warnings changed what I did, and the finding it *did* make is what let me
recognise the same collision shape in the trimmer within minutes rather than hours.

**Self-assessment: 8/10.** **+** I measured before choosing, and the measurement said *don't do what
the last two sessions did* — 27% whole reads against their 1-in-80, so the whole-file ceiling stayed.
Copying S97/S98's remedy would have been the confident wrong move, and it is now argued against in
the budget entry so the next session doesn't make it. **+** My instrument was wrong four ways and no
wrong number left the session, because I printed raw matches and eyeballed them every time. **+** I
enumerated the non-human readers before touching anything, which is the only reason I knew that
deleting §Completed items — the obvious dead weight — would turn a green proof red. **+** I put the
fork to the operator with costed options rather than picking inside an implementation, as declared.
**−** **I lost 1,684 B of live content and my own proof was green while it happened.** The Routing
block is the standing record that a constraint *was never imposed* — precisely the defect class
`CLAUDE.md` warns propagates because nobody re-derives it. Five checks passed over its deletion. I
caught it by noticing an unused variable in my own script; no check told me, and had I not looked it
would have shipped behind a proof that said the move was lossless. That is the worst thing in this
session and it is worth more than the recovery. **−** **My C1 would have failed on the next session
that raised a backlog item** — a false-positive generator, the exact BL-36 class I wrote the proof to
avoid — and I only found it because I raised BL-41 and watched it go red. **−** All seven `key_files`
line numbers in my first draft were wrong; `bin/check-handoff` refusing the receipt is what sent me
back. **−** I leave `HANDOFFS.md` over its ceiling. That one I do not count against the work: it is
BL-41, it is documented with the three cuts run as evidence, and the alternative was hand-editing
around a refusal that exists to protect the archive.

```handoff
session: S98
date: 2026-08-18
status: complete
self_score: 8
predecessor_score: 8
active_task: Bring `HANDOFFS.md` under its 65,536 B FM #28 ceiling DURABLY (S97's `next_steps` (b)). COMPLETE. The file was 114,466 B; it is now **49,579 B and reports `ok`**. **THE REMEDY THE HANDOFF NAMED DID NOT CLEAR IT, AND ESTABLISHING THAT WAS THE SESSION'S REAL WORK.** S97 recorded `--cut 3 = 49,724 B`; re-derived at Orient the same cut gave **66,778 B, 1,242 B OVER**. The cause is not untrimmed backlog but a COLLISION between two limits set independently: post-trim header 6,017 B leaves 59,519 B, `bin/tests.sh` Test 34 floors retention at THREE receipts, and the newest three totalled 60,761 B. Floor x mean already exceeded ceiling - header, so **no cut satisfied both** -- which is why the breach kept recurring while each session's trim looked correct. ALL SIX CLAIM DECLARATIONS HELD: (1) no trim until the lever was chosen; (2) the ceiling's provenance verified, not asserted; (3) non-human readers enumerated first; (4) commit shape claim -> deliverable -> trim ALONE -> close-out, receipt `status: pending` across the trim; (5) carve-out verified mechanically; (6) NO OUTWARD-FACING ACTION -- no PR, no push, no issue, no comment, no tag. Upstream issue #75's prepared answer remains unsent.
what_was_done: Four commits -- `29c5b02` (1B claim + ledger), `5490015` (THE DELIVERABLE), `7fee8bd` (the trim alone), this close-out. **(1) THE MEASUREMENT, AND MY INSTRUMENT WAS WRONG TWICE BEFORE IT WAS RIGHT.** Across **81 transcripts** (mine excluded, heredoc bodies stripped) the live root ledger was read **WHOLE into context exactly ONCE** and read **in PART 593 times** -- median span 25 lines, largest ever requested **220**, against a 452-line file. No read in the corpus asked for even half of it. That matches the mandated reads exactly: Phase 0 step 6 takes a FRONTIER (`git log -1`, no content) and Phase 3A reads THE PREDECESSOR'S RECEIPT IN FULL -- one record -- which `.context-budget.json` already stated in its own note while setting a whole-file ceiling. **Revision 1 scored the 4.5 KB SEED `starter-kit/HANDOFFS.md` as the live ledger (8 of its 9 "whole reads" were the seed) and scored `awk '/^```handoff/{n++} n==1'`, which extracts ONE receipt, as whole.** Revision 2 classified per SEGMENT, so `git show f | grep x` counted as a whole read plus a grep; fixed by classifying per PIPELINE. First answer 9, true answer 1. Caught by printing raw matched commands each time -- no wrong number reached the operator. **(2) THE FOUR REMEDY SHAPES WENT TO THE OPERATOR WITH NUMBERS AND HE CHOSE** rate+trim over trim-only, over also editing the distributed gate, over removing Test 34's floor. **(3) THE FIX.** `bin/check-handoff` (CANONICAL-ONLY) gains `RECORD_BUDGET_BYTES = 18432` and `check_record_budget()`, scoped to the record being WRITTEN -- the newest, when its text differs from its counterpart at git HEAD. Comparing record TEXT rather than "is this session id new" is what stops a close-out from growing its own already-committed 1B stub past the budget unseen. Derivation written beside the constant: `(65,536 - 8,000 header allowance) / 3 = 19,178 -> 18,432`, 2,240 B slack. **THE UNIT IS THE RECORD, NOT THE FENCED BLOCK** -- trailing prose below the closing fence counts, because the byte ceiling counts it and the trimmer moves it as part of the record; budgeting the fence alone would put the guard on a different axis from the ceiling it is derived from. **(4) DELIBERATELY NOT RUN UNDER `--all`,** and asserted rather than merely intended: Test 34's presence control asserts on check-handoff's EXIT CODE against the live ledger, a union over every check, so routing the budget through `--all` would redden an unrelated assertion whenever an in-flight receipt ran long. **(5) MY OWN TEST FIXTURE WAS WRONG TWICE, IN THE CLASS THE TEST EXISTS TO CATCH.** It prepended at byte 0, absorbing the file's 5,569 B header into the record under test; and its size check measured the string it had just built rather than the record as the checker parses it -- an identity, not an assertion. Six rows were red on first run and could have been read as a code defect; the unmutated fixture controls are what separated fixture from guard. **(6) LEARNING #35 APPENDED** at 1,219 B against the 1,500 B row budget.
next_steps: (a) **THE BREACH IS CLOSED AND THE GUARD IS THE DURABLE HALF, BUT ITS DERIVATION HAS A STATED SHELF LIFE.** The 8,000 B header allowance covers ~4 more trims: the front matter is 6,017 B and grows ~450 B per archive notice, and there are now FIVE near-identical "Archived N record(s)" paragraphs. Compacting them to a table is the obvious next reduction and is the drift term in my own arithmetic -- I budgeted for it rather than removing it, deliberately, to keep this session one deliverable. When the allowance is spent, re-derive rather than raising it. (b) **`docs/planning/BACKLOG.md` IS NOW THE ONLY FILE OVER A CEILING: 103,755 B against 65,536, unchanged and untouched this session.** S89 measured its residual as LIVE WORK, so reducing it is an operator call about what to abandon, not a trim. It is the strongest single candidate for the next session. (c) **BL-36's RESIDUAL IS SMALLER THAN S97 RECORDED, AND I RE-RAN IT.** Nine shipped `.verify.sh` proofs, **4 FAIL / 5 PASS** -- the four reds are the two oldest shards of each ledger (`*-through-2026-08-02`, `*-through-2026-08-09`), all frozen artifacts needing a go-ahead to regenerate. **The proof this session wrote PASSES** (`added by the trim commit: 0`), which is direct evidence S93's v1.2.0 code repair holds for new proofs -- so S97's (d), "trimming again writes another proof of a kind already known not to hold", is now FALSE and should not be carried forward. (d) `upstream/main` was still 1 ahead (`512c2ed`) at this close-out, fork 363 ahead; `git merge-tree --write-tree --name-only HEAD upstream/main` conflicts in exactly `CHANGELOG.md` and `HANDOFFS.md`, and I wrote to both, so RE-DERIVE. (e) **Upstream [issue #75](https://github.com/KJ5HST/methodology/issues/75) IS STILL OPEN** -- S92's fork-side answer remains prepared, vetted and unsent; six sessions of waiting on a go-ahead for the maintainer's own issue. (f) The `CHANGELOG.md` entry in `.context-budget.json` still cites `methodology_trim.py:69` for `DEFAULT_BUDGET_BYTES`, now `:80`; I corrected the `HANDOFFS.md` entry's copy because I was re-deriving that entry, and left the sibling (FM #17). (g) Telemetry ownership unchanged: `.context-budget-history.jsonl` and `dashboard_history.jsonl` go dirty from Phase 0 alone and no protocol step owns them; committed with this session's work rather than passed on. (h) BL-37 half (b), BL-39 and the long tail BL-11/12/13/14/16/17/18/19/20-residual/21/22/23/26/30/31/32 unchanged, NOT re-verified. **BL-16 has no heading of its own by design** (`docs/planning/BACKLOG.md:18`).
key_files: `bin/check-handoff` (CANONICAL-ONLY, not in `bin/_manifest.py`) -- `:606` `RECORD_BUDGET_BYTES = 18432` with the full derivation and the 81-transcript measurement above it, `:609` `record_extents()` and why the unit is the record and not the fence, `:623` `frozen_newest_record()` and the text-vs-identity argument, `:652` `check_record_budget()` and its stated-skip contract. `bin/tests.sh` (CANONICAL-ONLY) -- `:2698` Test 38's header, `:2742` `add_record38()` carrying BOTH fixture bugs recorded beside their fixes, `:2762` the line-anchored insertion point and the 86-byte error a plain `text.index()` caused, `:2942` the M5 axis mutant. `.context-budget.json` (FORK-ONLY, not distributed) -- the `HANDOFFS.md` entry: the measurement with its population and caveats, why the ceiling is RETAINED rather than lowered, and its own `:69`->`:80` correction. `starter-kit/FRAMEWORK_LEARNINGS.md:63` (**THE ONE DISTRIBUTED FILE TOUCHED**) -- Learning #35 at 1,219 B. `docs/archive/HANDOFFS-through-2026-08-17.md` and its `.verify.sh` -- the new shard and the proof that passes.
gotchas: **A FIXTURE THAT INSERTS INTO A REAL FILE MUST ANCHOR ON THE SAME PATTERN THE CODE UNDER TEST USES.** `text.index("```handoff")` finds the first occurrence ANYWHERE -- this ledger's front matter mentions the fence inline in prose -- so my record went in mid-header and 86 stray header bytes landed inside the record under test, putting every edge assertion off its boundary. The checker uses a LINE-ANCHORED regex; the fixture now does too. **AND A SIZE CHECK WHOSE OPERANDS BOTH COME FROM THE BUILDER ASSERTS AN IDENTITY.** Mine verified the string it had constructed, not the artifact re-parsed. It passed while the record was 5,569 B larger than requested. Re-read the file and measure what the tool will measure. **THE RECORD BUDGET IS SCOPED TO THE RECORD BEING WRITTEN, SO A CLEAN TREE EXERCISES ALMOST NOTHING** -- `bin/check-handoff` on a committed ledger prints `0 unwritten record(s)`. It only really runs between editing a receipt and committing it. Deliberate, but a green run on a clean tree is NOT evidence the budget works; Test 38 is. **AND IT DOES NOT RUN UNDER `--all` AT ALL** -- close-out must invoke the default mode explicitly or the guard never sees the receipt. **`check-learnings` READS "34 row(s), contiguous 1..34" WHILE THE MAXIMUM ROW IS 35** -- `#14` is reserved, so count and max differ by one. Pre-existing; do not "repair" it. **A TRIM MUST BE ITS OWN COMMIT WITH THE RECEIPT LEFT `pending`** -- v1.2.0 excuses a trim commit's ADDED records and not an EDITED one; this is why the two oldest proofs are red and why this session's is green. **RE-RUN `bash bin/tests.sh` AFTER ANY TRIM** -- Test 34's anchors move with the cut. **`bin/check-*` ARE PYTHON, NOT SHELL.** **DO NOT `git reset --hard`** -- two tracked `.jsonl` telemetry files go dirty from Phase 0 alone.
runtime_smoke: **FOUR SURFACES.** This repo ships no application; the build-equivalent is the suites plus the tools, RUN. **SURFACE 1 -- the canonical checkout, AFTER the trim** (Test 34's anchors moved, so the order matters): `bash bin/tests.sh` **279 passed / 1 failed / 0 skipped** -- zero SKIPs confirms the 3-receipt floor is satisfied. Diffed ROW-FOR-ROW against a clean `git worktree` at HEAD using `comm` over sorted row sets, BOTH populations asserted non-empty (262 and 280): **ZERO ROWS LOST**, 18 added, all Test 38's. Sole failure is Test 9's pre-existing `--source=github` 404, confirmed BY NAME. `tools.test_methodology_dashboard` + `_trim` + `_context_budget` **444/444**. `check-learnings` OK (34 rows, row budget 1 unfrozen / 0 over -- so the budget really ran on my own row); `check-links` OK (88/22); `check-handoff --all --allow-pending` OK. `context_budget.py --selftest` green; the gate now reports `HANDOFFS.md` **ok at 219 ln / 1,200**, off the byte-over list entirely. **SURFACE 2 -- the guard proven RED in situ, then restored.** Before Test 38 existed, an oversized record was injected into the LIVE tracked ledger from a committed state with a backup held outside the repo: the PRE-CHANGE checker (`git show HEAD:bin/check-handoff`) exited **0** on that input and the new one exited **1** with `record S98 is 22,069 B, over the 18,432 B per-record budget by 3,637`. Restore verified byte-identical by `cmp` AND by `git diff --quiet`. **SURFACE 3 -- mutation.** 5 mutants, each verified to APPLY before running, **5/5 KILLED** against an 13-assertion passing control: `>` widened to `>=` at an exactly-at-budget record; the frozen comparison degraded to a presence test; the SKIP arm rewritten to look like a completed check; the budget raised past the record under test; and **M5, the axis** -- the record extent narrowed to the fenced block, killed by a record whose overage lives entirely in trailing prose. **SURFACE 4 -- the archive proof.** `bash docs/archive/HANDOFFS-through-2026-08-17.md.verify.sh` PASSES, reporting `7 before = 3 retained + 4 archived; added by the trim commit: 0`. **WHAT NONE OF THESE ENFORCE, stated rather than left to be discovered:** the guard is CANONICAL-ONLY and cannot bind an adopter -- `bin/check-handoff` is not in `bin/_manifest.py`, so every adopter's receipt ledger is still guarded only by the inherited whole-file ceiling, on the axis this session showed is the wrong one. And nothing here verifies delivery over the network; Test 9 stays red on a 404 that closes only when these files exist upstream. **CARVE-OUT, verified mechanically at close-out:** 26 manifest SOURCE rows vs the changed set, both populations asserted non-empty, intersection **exactly** `starter-kit/FRAMEWORK_LEARNINGS.md` -- the Phase 3C row, which my claim's declaration (5) did not name in advance and should have. **FM #28 GATE:** `HANDOFFS.md` ok; `CLAUDE.md` ok; `CHANGELOG.md` ok; `starter-kit/FRAMEWORK_LEARNINGS.md` ok at 61,689 / 65,536; `docs/planning/BACKLOG.md` **103,755 B OVER**, unchanged.
changelog_ref: CHANGELOG.md "2026-08-18 · [ad hoc] The receipt ledger gets a guard on the axis its cost is actually paid on — a per-record budget, derived from the collision it resolves"
commit: 29c5b02 (1B claim) + 5490015 (the deliverable) + 7fee8bd (the trim) + this close-out
```
<!-- claim stub written at session start; completed at close-out -->
Model: Claude Opus 5 (1M context).

**Predecessor S97 scored 8/10.** **+** Its `next_steps` (b) is why this session existed and was
well-posed: it named `HANDOFFS.md` as the strongest candidate, gave the cut options, and told me the
floor was three receipts and why. **+** Its gotchas changed what I did, concretely and repeatedly. I
captured every command's output into a variable before grepping it throughout Test 38 — the
`pipefail`/SIGPIPE mechanism it documented is the reason, and it is now written above my own mutation
block. Its "a `cat` is not always a read" and "exclude your own session" are the reason I printed raw
matched commands rather than trusting a counter, which is what caught both of my instrument defects
before either number left the session. I hit the `check-learnings` count-vs-max quirk exactly as
described and did not "repair" it. **+** Its `key_files` line numbers all resolved. **−** **Its
central recommendation's headline number was refutable at write time, and refuting it was most of my
session.** (b) states *"`--cut 3` now clears the ceiling with real room, which was not true one
session ago"* on the strength of `retaining 3 receipts = 49,724 B`. That figure was computed on the
pre-append population; S97 knew its own receipt was about to land, knew receipts run ~20 KB, and knew
`--cut 3` retains the newest three. The arithmetic that its own close-out would invalidate the number
was available to it, and it hedged the *total* elsewhere while stating (b) as a conclusion. That is
Phase 3D's own rule about requirement 3 — a forward-looking claim that was computable and not
computed. Not a 9 for that; not a 7, because everything else it handed me was accurate, and because
it warned me to re-derive even as it drew the wrong conclusion itself.

**Self-assessment: 8/10.** **+** I refused to run the trim until I knew which lever moved, and that
refusal is the whole difference between this session and a fourth consecutive one-session clearing.
The collision — floor × mean vs ceiling − header — is the finding; the trim is bookkeeping. **+** My
instrument was wrong twice and **no wrong number reached the operator**, because I printed the raw
matched items before publishing either time. That is the one thing my predecessor got wrong and the
one thing I most wanted not to repeat. **+** I put the policy fork up with four costed options
instead of picking inside an implementation. **+** I held my own receipt under the 18,432 B budget I
had just created, and made close-out run the guard that binds it. **−** **My test fixture was wrong
twice, both times in exactly the class of error the test exists to catch.** It inserted at byte 0 and
swallowed the file's 5,569 B header into the record under test, and its size check compared the
string it had just built against the number it had just been given — an identity that cannot fail. Six
assertions were red on the first run and I was one wrong inference from "the guard is broken" rather
than "the fixture is". The unmutated fixture controls, which I wrote because Test 37 has them, are the
only reason that inference was cheap. **−** I did not pre-declare `starter-kit/FRAMEWORK_LEARNINGS.md`
in my claim's carve-out and then edited it at Phase 3C. Protocol-mandated, one row, verified
mechanically — but my declaration said *any* distributed file I touch is pre-declared, and this one
was not. **−** I left the header's five near-identical archive notices in place and budgeted 8,000 B
around them instead. That is a defensible scope call and I stated its shelf life, but it means my
derivation carries a drift term I chose not to remove. Not a 9 because of the fixture defects; not a
7 because every one was caught in-session by a control I had put there on purpose, and the central
claim was measured three ways — transcript mining, the mandated-read audit, and the growth curve —
before it was written down.

```handoff
session: S97
date: 2026-08-17
status: complete
self_score: 7
predecessor_score: 9
active_task: Bring `starter-kit/FRAMEWORK_LEARNINGS.md` (DISTRIBUTED) under its ceiling durably -- S96's `next_steps` (b), the most imminent size breach in the repo because every adopter receives the file. COMPLETE, as the OPERATOR-CHOSEN option of four put to him with measurements. **THE REMEDY WAS CHOSEN FROM A MEASUREMENT, NOT FROM THE FILE'S SIZE.** Across **80 transcripts** of this repo (this session's own EXCLUDED after it was caught contributing a `sed -n '1,60p'`), the file was read **WHOLE once**, read **in PART 243 times**, written to 18 times, and never touched at all in **47 of the 80**. A partial read returns whole ROWS, because a row is one physical line -- so the cost a session actually pays here is per-row, and rows have grown **4.9x** (#1-#6 average 510 B; #24-#33 average 2,510 B; largest 3,451 B). The 60,000 B whole-file ceiling was guarding a cost paid once in 80, and `.context-budget.json` already admitted the number was "INHERITED FROM THE SEED AND NOT LOCALLY DERIVED". So the GUARD MOVED AXIS rather than the number moving. ALL SIX CLAIM DECLARATIONS HELD: (1) the oldest-first trim axis was refuted before anything was built -- all **24** `Learning #N` tokens in the distributed corpus cite a row **<= 16**; (2) the inverse was refused for cause, citing Learning #29 (an uncited rule is orphaned, not worthless); (3) the ceiling was re-derived, not silenced, and the raise is LABELLED as a raise; (4) the four remedy shapes went to the operator with numbers and he chose; (5) my Phase 3C row was budgeted at **<= 604 B** -- the figure from the OLD ceiling -- and came in at **603 B**, rather than spending headroom I had just created; (6) carve-out verified mechanically against the staged set: 26 manifest SOURCE rows vs 5 changed files, both populations asserted non-empty, intersection **exactly** `starter-kit/FRAMEWORK_LEARNINGS.md`. NO OUTWARD-FACING ACTION: no PR, no push, no issue, no comment, no tag.
what_was_done: Three commits -- `d8ff005` (1B claim + ledger + inherited telemetry), `452908c` (THE DELIVERABLE), this close-out. **(1) THE MEASUREMENT IS THE DELIVERABLE'S FOUNDATION AND MY FIRST INSTRUMENT WAS WRONG.** The first pass counted `cat >> file` (an APPEND) and heredoc payloads inside `git commit -m "$(cat <<'EOF' ...)"` (a filename merely NAMED in a commit message) as whole-file reads, and reported "13 of 81 opened it whole, 11:1 partial". **Those numbers were void and I had already put them in front of the operator.** Found by printing the raw matched commands instead of trusting the counter. Rebuilt with heredoc-body stripping and segment-level classification, then re-run with this session excluded. **(2) THE ARCHIVE REMEDY IS ACTIVELY WRONG HERE, AND THAT IS THE NON-OBVIOUS FINDING.** `methodology_trim.py` trims `CHANGELOG.md`/`HANDOFFS.md` oldest-first. Re-derived with `bin/check-learnings`'s own `CITATION_RE` over `bin/_manifest.py`'s DISTRIBUTED population: every one of the 24 `Learning #N` tokens in that corpus points at a row numbered <= 16 -- {7,8,10,13,15,16} outside the file, {6,7,8,10,12,16} inside it. In a changelog age means stale; in a learnings table it means FOUNDATIONAL. **(3) THE FIX.** `bin/check-learnings` gains `ROW_BUDGET_BYTES = 1500` and `check_row_budget()`, scoped to rows **not yet frozen in git HEAD** -- the population an append-only table actually leaves editable. The comparison is row TEXT against HEAD, not "is this number new", so growing an existing row cannot route around it. When the frozen population cannot be read the tool prints `row budget SKIPPED (<reason>)` and does NOT print a checked-population figure -- a skip that reads as a pass is the defect, not the fix. `max_bytes` 60,000 -> **65,536**, this repo's own ledger ceiling (`starter-kit/methodology_trim.py:80`), with the whole measurement written into the config so a successor re-runs it rather than re-argues it. `max_line_bytes` deliberately NOT declared: 20 of 32 rows exceed 1,500 B, so it would be permanently red against rows the file's own front matter forbids editing. **(4) THREE OF MY FOUR MUTANTS WERE SCORING THEMSELVES KILLED WHILE ASSERTING NOTHING.** With `set -o pipefail`, `producer | grep -q` makes grep exit at the first match and close the pipe; the producer takes SIGPIPE and the PIPELINE reports failure even though the pattern matched. On a `&& fail || pass` polarity that lands on `pass`. Only M1, whose polarity is the other way round, failed honestly -- and chasing that one failure is what exposed the other three. Every assertion now captures output first, as the rest of the suite already did, and the mechanism is recorded above the block. **(5) THE FULL SUITE CAUGHT A DEFECT THE ISOLATED RUN COULD NOT.** Test 37's fixture is a copy of the LIVE table, and I had hardcoded the injected row as `#34` -- which became a duplicate the moment my own Phase 3C row landed at 34. Row numbers are now DERIVED as max+1 from the fixture, with an empty-population guard. **(6) LEARNING #34 APPENDED** at 603 B against a 604 B budget.
next_steps: (a) **THE LEVEL IS UNFIXED AND THAT WAS THE OPERATOR'S DELIBERATE CHOICE, NOT AN OVERSIGHT -- BUT THE CLOCK IS SHORT.** `starter-kit/FRAMEWORK_LEARNINGS.md` is 60,469 / 65,536 B: **5,067 B of headroom, about THREE more rows at the 1,500 B budget** (not four -- my own row spent one of them; re-derive with `python3 starter-kit/context_budget.py`, do not trust this figure). This session moved the RATE lever only. The two shapes that move the LEVEL were measured and presented: a ceiling derived from tail risk (25% of the 42,033-tok floor = 29,423 B, so the file must shed ~30,000 B), or a DISTRIBUTED archive split (keeping the 15 rows <= #16 leaves ~18,514 B live). **Re-open that choice when the three rows are spent, and note the archive split still has no principled axis** -- do not default to oldest-first, which is what the citation data refutes. (b) **`HANDOFFS.md` IS 97,861 B AGAINST 65,536 AND I MADE IT WORSE BY ~20 KB.** The trim S96 unblocked is still owed and is the strongest single candidate for the next session. Re-derived at THIS close-out, before this receipt was appended: retaining 3 receipts = 49,724 B, retaining 4 = 65,860 B (just over). **`--cut 3` now clears the ceiling with real room, which was not true one session ago** -- S96 measured 62,379 B for the same cut. Test 34's floor is three receipts and, since S96, going below it prints six stated `SKIP` rows rather than five misattributed failures. (c) `docs/planning/BACKLOG.md` 103,755 B, unchanged and untouched; S89 measured its residual as LIVE WORK, so reducing it is an operator call about what to abandon. (d) **BL-36's residual is UNCHANGED and NOT re-run this session**: 8 shipped `.verify.sh` proofs, 4 of them failing, the reds being frozen artifacts that need a go-ahead to regenerate. This matters for (b): trimming again writes another proof of a kind already known not to hold. (e) `upstream/main` was 1 ahead (`512c2ed`) at this close-out with `git merge-tree --write-tree --name-only HEAD upstream/main` conflicting in exactly `CHANGELOG.md` and `HANDOFFS.md`; I wrote to both, so RE-DERIVE. (f) **Upstream [issue #75](https://github.com/KJ5HST/methodology/issues/75) IS STILL OPEN** -- S92's fork-side answer remains prepared, vetted, and unsent, and this is now five sessions of waiting on a go-ahead for the maintainer's own issue. (g) **TWO STALE FIGURES FOUND IN `.context-budget.json` AND DELIBERATELY NOT FIXED (FM #17):** the `CHANGELOG.md`/`HANDOFFS.md` entries cite `methodology_trim.py:69` for `DEFAULT_BUDGET_BYTES`, which is now at **`:80`**; and `_deliberate_exclusions` records `starter-kit/SESSION_RUNNER.md` at 52,386 B, which is now **54,363 B**. My own entry cites `:80` and says why the siblings differ. (h) The telemetry ownership gap is unchanged -- `.context-budget-history.jsonl` and `dashboard_history.jsonl` go dirty from Phase 0 alone and no protocol step owns them. I committed the rows rather than passing the dirt on, as S96 did. (i) BL-37 half (b), BL-39, and the long tail BL-11/12/13/14/16/17/18/19/20-residual/21/22/23/26/30/31/32/36 unchanged, NOT re-verified. **BL-16 has no heading of its own by design** (`docs/planning/BACKLOG.md:18`); a naive re-derivation reports it as a phantom.
key_files: `bin/check-learnings` (CANONICAL-ONLY, not in `bin/_manifest.py`) -- `:94` `ROW_BUDGET_BYTES = 1500` with both derivations beside it, `:214` `frozen_rows()` and its stated-skip contract, `:242` `check_row_budget()` and the argument for scoping to unfrozen rows. `bin/tests.sh` (CANONICAL-ONLY) -- `:2517` Test 37's header and why the fixture is a throwaway git repo, `:2547` `add_row37()` carrying BOTH bugs it had (the newline off-by-one and the hardcoded row number) recorded beside the fix, `:2649` the M1 boundary mutant and, above the mutation block, the `pipefail`/SIGPIPE explanation. `.context-budget.json` (FORK-ONLY, not distributed) -- the `starter-kit/FRAMEWORK_LEARNINGS.md` entry: `max_bytes` 65,536, the 80-transcript measurement with its unit and caveats, and the stated non-declaration of `max_line_bytes`. `starter-kit/FRAMEWORK_LEARNINGS.md:10` (**THE ONE DISTRIBUTED FILE**, pre-declared) -- the row-budget sentence; `:62` Learning **#34** at 603 B. `CHANGELOG.md` -- the claim entry and the deliverable entry.
gotchas: **`producer | grep -q` UNDER `set -o pipefail` REPORTS A FAILED PIPELINE EVEN WHEN THE PATTERN MATCHED.** grep exits at the first match, the producer takes SIGPIPE, pipefail propagates it. On an assertion written `&& fail || pass` that lands on **pass**, so the check scores itself green while asserting nothing. Three of my four mutants did exactly this. The rest of `bin/tests.sh` captures into a variable first (`OUT="$(...)"; echo "$OUT" | grep -q`) and that is WHY -- follow it, it is not a style preference. **A TEST FIXTURE COPIED FROM A LIVE FILE MUST NOT HARDCODE AN IDENTIFIER FROM IT.** Test 37 injected row `#34`; my own Phase 3C row then landed at 34 and made it a duplicate. The isolated run could not see this -- only the full suite, run AFTER the append, could. Derive identifiers from the fixture. **A `cat` IS NOT ALWAYS A READ.** Any transcript-mining instrument must strip heredoc BODIES before classifying, or `git commit -m "$(cat <<'EOF' ...)"` counts a filename mentioned in a commit message as a file access, and `cat >> f` counts an append as a read. Mine did both. **AND EXCLUDE YOUR OWN SESSION** -- my `sed -n '1,60p'` of the file under study appeared in my own results. **THE ROW BUDGET IS SCOPED TO UNFROZEN ROWS, SO AN ORDINARY RUN EXERCISES ALMOST NOTHING.** Once a row is committed it is frozen and `check-learnings` reports `0 unfrozen row(s)`. The check only really runs during the window between appending a row and committing it. That is deliberate, but it means a green `check-learnings` on a clean tree is NOT evidence the budget works -- Test 37 is. **`check-learnings` READS "33 row(s), contiguous 1..33" WHILE THE MAXIMUM ROW IS 34** -- `#14` is reserved, so count and max differ by one and "1..N" is the COUNT. Pre-existing; do not "repair" it. **`bin/check-*` ARE PYTHON, NOT SHELL.** **DO NOT `git reset --hard`** -- two tracked `.jsonl` telemetry files go dirty from Phase 0 alone. **AND DO NOT RE-RUN A STALE EXTRACTED TEST SCRIPT** -- I regenerated a scratch harness from `bin/tests.sh` once, edited the real file, re-ran the stale copy, and briefly believed my fix had not worked.
runtime_smoke: **FOUR SURFACES.** This repo ships no application; the build-equivalent is the suites plus the tools, RUN. All figures measured AFTER the last non-receipt edit and BEFORE this receipt was appended, so the `HANDOFFS.md` byte number understates by roughly this block -- re-derive rather than trusting it. **SURFACE 1 -- the canonical checkout.** `bash bin/tests.sh` **261 passed / 1 failed / 0 skipped**, diffed ROW-FOR-ROW against the 248-row Phase 0 baseline with `comm` over sorted row sets and BOTH populations asserted non-empty (248 and 262): **ZERO ROWS LOST**, 14 added (all Test 37's). Three further rows re-worded by their own embedded counts and verified as such, not as losses -- `handoff_anchors ... (5)` -> `(6)` and `live ledger receipt-id population is non-empty (5)` -> `(6)` from my claim stub becoming a sixth receipt, and Test 31's `**Model:**` equality 11 -> 12 as my ledger entry joined the population. Sole failure is Test 9's pre-existing `--source=github` 404, confirmed BY NAME. `tools.test_methodology_dashboard` + `tools.test_methodology_trim` + `tools.test_context_budget` **444/444**. `bin/check-learnings` OK (33 rows, all citations resolve, row budget 0 unfrozen / 0 over); `bin/check-links` OK (88/22, unchanged); `bin/check-handoff --all --allow-pending` OK (6 receipts). **SURFACE 2 -- the guard proven RED in situ, then restored.** Before any test existed, an oversized row was injected into the LIVE tracked file from a committed state, with a backup held outside the repo: `check-learnings` failed with `row #34 is 1,622 B, over the 1,500 B per-row budget by 122`, and the restore was verified byte-identical by `cmp` and by `git diff --quiet`. **SURFACE 3 -- mutation on the guard this session added.** 4 mutants, each verified to APPLY before running (`DID NOT APPLY` kept distinct from `survived`), **4/4 KILLED** against a 10-assertion passing control: `>` widened to `>=` at an exactly-at-budget row; row-text comparison degraded to number-membership; the SKIP arm rewritten to look like a completed check; the budget itself raised past the row under test. **SURFACE 4 -- a real adopter tree.** `bin/sync --mode=commit --source=local` into a fresh `git init`: the delivered `FRAMEWORK_LEARNINGS.md` is **byte-identical** to canonical (`diff -q` clean), carries the row-budget sentence and Learning #34, and the canonical-only `check-learnings`/`tests.sh` correctly do NOT land. The adopter's `.context-budget.json` is the SEED (`bin/_manifest.py:60`) and is byte-identical to `starter-kit/context-budget.json`, NOT to this repo's own edited config, and contains none of this session's numbers -- checked after my first version of this assertion wrongly called the seed a leak. **WHAT NONE OF THESE ENFORCE, stated rather than left to be discovered:** nothing here verifies delivery over the network -- Test 9's `--source=github` path is still red on a 404 that closes only when these files exist upstream. And the row budget cannot bind an ADOPTER at all: `bin/check-learnings` is canonical-only by design, and adopters never append to this table, so the guard protects the canonical repo's writers only. **FM #28 GATE:** `starter-kit/FRAMEWORK_LEARNINGS.md` **ok at 60,469 / 65,536** (was 59,396 / 60,000 with 604 B); `CLAUDE.md` ok; `CHANGELOG.md` ok (679 ln / 2,000); `HANDOFFS.md` **97,861 B OVER** and rising with this receipt; `docs/planning/BACKLOG.md` **103,755 B OVER**, unchanged. `growth run 21/10` is not evidence about anything here -- it is computed on `resident_bytes`, which in this repo is `CLAUDE.md` alone.
changelog_ref: CHANGELOG.md "2026-08-17 · [ad hoc] The Learnings-table ceiling moves onto the axis the cost is actually paid on — a per-row budget, and a whole-file ceiling derived instead of inherited"
commit: d8ff005 (1B claim) + 452908c (the deliverable) + this close-out
```
<!-- claim stub written at session start; completed at close-out -->
Model: Claude Opus 5 (1M context).

**Predecessor S96 scored 9/10.** **+** Its `next_steps` (b) is the reason this session existed and
was well-posed: *"604 B of headroom ... the most imminent size breach in the repo, ahead of
`HANDOFFS.md`, because it is DISTRIBUTED."* I re-derived that figure at Phase 0 and it was exact —
59,396 / 60,000. Naming *why* it outranked the larger breach, rather than just ranking it, is what
made the operator's choice a real choice. **+** Every number I independently checked held: 247/1/0
reproduced to the row, `BACKLOG.md` 103,755 B, `check-links` 88/22. Its `HANDOFFS.md` figure looked
wrong at 77,723 B against my 94,380 B — and it had already said the number was taken before its own
receipt was appended and would understate by roughly that block. It was right, and it warned me
rather than leaving me to find it. **+** Its gotchas changed what I did, concretely: I never piped a
suite population through `tail`, I left the two dirty `.jsonl` files alone until I committed them
deliberately, and I hit the `check-learnings` count-vs-max quirk exactly as described and did not
"repair" it. **+** It corrected its own receipt-size claim at the final gate rather than letting it
stand. **−** Its budgeting guidance published a range without its population: *"Recent rows run
2,259–2,770 B."* Those are rows #31 and #32. The last ten actually run **1,431–3,451 B** — a spread
more than three times wider, and the stated range excludes both the largest row in the file (#29) and
S96's own row (#33). A successor budgeting from that sentence would aim at the wrong target. That is
this repo's own recurring lesson — state the population beside the number — applied unevenly by the
session that was otherwise most careful about it. Not a 10 for that; not an 8 because the figure was
advisory rather than load-bearing, and because two of its lines measurably changed my work.

**Self-assessment: 7/10.** **+** I refused the obvious remedy and proved why before building
anything. Trimming oldest-first is what the sibling ledgers do and it is actively wrong here: all 24
`Learning #N` tokens in the distributed corpus cite a row ≤ #16, so age in this table means
foundational, not stale. **+** I then refused the inverse for cause too, rather than taking the free
70% of bytes it appeared to offer — Learning #29, ratified five sessions ago, says an uncited rule is
orphaned and not worthless, and using citation count as an archive criterion would have contradicted
the framework's own most recent finding. **+** I put the policy fork to the operator with numbers
instead of picking inside an implementation, as I had committed to at claim; his questions then
forced me to strip out metaphor I had no business using in a technical recommendation, and the
restated version was better. **+** I held my Phase 3C row to 604 B — the budget derived from the
ceiling I was replacing — rather than spending headroom I had just created. 603 B. **−** **My first
access measurement was wrong and I showed it to the operator before auditing it.** It counted
`cat >> file` appends and filenames appearing inside `git commit` heredocs as whole-file reads, and I
reported "13 of 81, 11:1" on that basis. I caught it one turn later by printing the raw matched
commands, and the decision was made on corrected numbers — but a computed value is a claim, and this
one reached a person before it reached a control. That is the single worst thing I did this session
and it is why this is not an 8. **−** Three of my four mutants were scoring themselves killed while
asserting nothing, for a mechanism I did not know: `pipefail` turning a short-circuited `grep -q`
into a failed pipeline. Only the one mutant with the opposite polarity failed honestly, and chasing
that single red is the only reason I found the other three. I was one assertion-polarity away from
shipping a mutation pass that proved nothing. **−** Two more of my own artifacts were wrong on first
construction: a fixture helper off by one byte, which put every edge assertion off the boundary it
existed to sit on, and an adopter-tree assertion that called the documented seed a leak. **−** I
re-ran a stale extracted copy of the test file and briefly believed a correct fix had failed. Not an
8 because of the unaudited measurement reaching the operator; not a 6 because every one of these was
caught in-session by a control, the full suite caught a defect the isolated run structurally could
not, and the central finding was measured three independent ways before it was written down.


```handoff
session: S96
date: 2026-08-17
status: complete
self_score: 8
predecessor_score: 9
active_task: BL-40 option (b) -- rewrite `bin/tests.sh` Test 34 so its mutation anchors are an ASSERTED population that SKIPS WITH A STATED REASON below three receipts. COMPLETE. Measured end to end on the REAL suite, not inferred: with the live ledger tampered to 2 receipts, the suite reports **241 passed / 1 failed / 6 skipped** where the same condition previously gave **229 / 6**. ALL FIVE CLAIM DECLARATIONS HELD: (1) RED first against COPIED fixtures -- the live ledger was never truncated for the RED, only later for one in-situ control, from a committed state, with a backup outside the repo and a `cmp`-verified restore; (2) the skip got its own counter and a place in the summary line, which now reads `N passed, M failed, K skipped` -- a format change announced at claim; (3) the population guard is PROVEN to fire, 9 mutants, 9 killed; (4) carve-out of ZERO distributed files except the pre-declared `starter-kit/FRAMEWORK_LEARNINGS.md` Phase 3C row -- verified mechanically at claim AND against the staged set (26 manifest SOURCE rows vs 5 staged files, both populations asserted non-empty, intersection exactly the one declared); (5) `HANDOFFS.md` NOT trimmed -- that is a second capability (FM #26) and it is now unblocked for the next session. NO OUTWARD-FACING ACTION: no PR, no push, no issue, no comment, no tag.
what_was_done: Three commits -- `e4da369` (1B claim, ledger co-staged), `8d38833` (THE DELIVERABLE), and this close-out. **(1) THE DEFECT WAS BIGGER THAN BOTH WRITE-UPS OF IT, AND ONLY REPLAYING SHOWED THAT.** BL-40 and [[31]] each say FIVE assertions stopped asserting. SIX did. The sixth -- `default mode still green on a destroyed older receipt` -- sits in the then-branch of the same `if mutate` block as one of the five, so when the mutation failed to build it printed NOTHING: not a pass, not a fail, no row. Both write-ups carried the discrepancy in plain sight (235 -> 229 passes is six fewer, against five new failures) and nobody subtracted. Found by extracting the PRE-CHANGE Test 34 body verbatim with `awk` and replaying it at 1/2/3/4 receipts, not by reading either document. **Learning #33** records the general shape: a failure count undercounts coverage loss, because an assertion nested in a failed guard leaves no row to count. **(2) THE FIX.** `handoff_anchors` returns `<count> <A1> <A2>` and cannot raise; `anchor_disposition` routes that count to MALFORMED / EMPTY / SHORT / ANCHORED. The non-numeric arm is FIRST, deliberately: an empty or garbled count falling through to `*)` would read as ANCHORED and silently reinstate the exact vacuum being repaired. EMPTY (zero receipts) FAILS rather than skips -- there is no cut of a valid ledger that produces it, so it is corruption, not rotation. Both helpers are functions rather than inline code specifically so the controls can drive them over shortened copies; the live ledger has held >= 3 receipts at every commit this test has existed, so an ordinary run exercises exactly one of the four arms. **(3) THE PROPERTY WITH NO COUNTER GOT AN ASSERTION.** Routing an unbuildable assertion to `pass()` instead of `skip()` would turn six real checks into six green no-ops and INCREASE the pass count -- nothing a reader watches would move. So the SHORT arm is asserted at the source: exactly six `skip` rows, zero `pass` rows, and the summary line must still name the skip count. **(4) DOCS FOLLOWED THE CODE.** `HANDOFFS.md`'s front-matter warning said *"This is avoided here, not fixed"* -- now false, corrected in place, and it still says `--cut 3` because a stated skip does not restore coverage. BL-40 moved from open prose to a completed row, `BACKLOG.md` 104,530 -> 103,755 B, and its hand-maintained `**Open:**` list dropped BL-40 (that list is exactly the kind of figure [[12]] warns about, and the file says so itself).
next_steps: (a) **THE `HANDOFFS.md` TRIM IS NOW UNBLOCKED AND IT IS THE OBVIOUS NEXT DELIVERABLE -- BUT RE-DERIVE EVERY NUMBER BELOW, INCLUDING MINE.** S95 handed forward "retaining 3 gives ~52 KB"; when I measured it was **57,842 B**, because S95's own receipt grew after it measured. That is the second consecutive session whose headroom estimate was stale at handoff, so treat this paragraph as a method, not as data. Method: `idx=[i for i,l in enumerate(lines) if l.startswith('```handoff')]`, then `len('\n'.join(lines[:idx[N]]).encode())` is the live size retaining N. **The tension is real and is NOT yours to resolve silently:** `--cut 3` clears the 65,536 B ceiling but not the seed's `<= 1/2 x budget` (32,768 B) stop condition, and no cut satisfies both. What CHANGED this session is the consequence of going below three: it is now six stated `SKIP` rows and a green suite, not five misattributed failures and a red one. **That is a licence to decide, not a licence to skip.** If you cut to 2, say in your receipt that you knowingly traded six whole-ledger invariants for bytes. (a2) **The breach is structural and I made it worse.** The file entered this session at 74,683 B and my claim + warning + this receipt push it further over. Four-to-five receipts at ~15-20 KB cannot fit under 65,536 B with a 5.3 KB header; even a `--cut 3` leaves under one receipt of headroom, so it re-breaches at the very next close-out. The durable question is whether the CEILING or the RECEIPT SIZE is wrong, and that is an operator policy call nobody has been asked. **Ask it.** (b) **`starter-kit/FRAMEWORK_LEARNINGS.md` HAS 604 B OF HEADROOM AND THE NEXT ROW OF NORMAL SIZE BREACHES ITS CEILING.** 59,396 / 60,000 B. Recent rows run 2,259-2,770 B; I budgeted mine to **1,432 B** BEFORE writing it, against S95's measured headroom, which is why it fit -- that is the only reason, and it is not repeatable twice. This is now the most imminent size breach in the repo, ahead of `HANDOFFS.md`, because it is DISTRIBUTED: every adopter receives it. S95 flagged it; I am flagging it harder with one session less runway. (c) `docs/planning/BACKLOG.md` 103,755 B is still over, reduced only incidentally by BL-40's closure. S89 measured its residual as LIVE WORK, so reducing it is an operator call about what to abandon. Ask first; do not quietly archive. (d) **BL-36's residual is UNCHANGED and untouched**: 8 shipped `.verify.sh` proofs, 4 passing, the 4 reds being the same frozen artifacts that need their own go-ahead to regenerate. Not re-run this session. (e) `upstream/main` was 1 ahead (`512c2ed`) at my Phase 0, with `git merge-tree --write-tree --name-only HEAD upstream/main` conflicting in exactly `CHANGELOG.md` and `HANDOFFS.md`. I wrote to both, so RE-DERIVE rather than trusting this line. (f) **THE TELEMETRY OWNERSHIP GAP IS STILL OPEN and I am committing the rows rather than leaving them, as S95 did.** `.context-budget-history.jsonl` and `dashboard_history.jsonl` are tracked and are appended to merely by running Phase 0. No protocol step owns writing them, so every session inherits a dirty tree it did not create. That is a real FM-shaped gap and nobody has raised it as a BL item -- I did not either, because `BACKLOG.md` is over its ceiling and adding to it is the wrong direction. Raise it if you disagree. (g) BL-37 half (b), BL-39 and the long tail BL-11/12/13/14/16/17/18/19/20-residual/21/22/23/26/30/31/32/36 unchanged, NOT re-verified. **BL-16 has no heading of its own by design** -- `BACKLOG.md:18` says so; a naive `grep '\*\*BL-N —'` re-derivation of the open set reports it as a phantom mismatch. Mine did. Read the paragraph above the list before believing your own script.
key_files: `bin/tests.sh` -- Test 34 IS the deliverable. **Line anchors are given because the checker's schema requires them, and the grep beside each one is what will still work after the next edit -- this session moved every one of these once already.** `:20` `skip()`, beside `pass()`/`fail()`; `:2096` `handoff_anchors` (grep `handoff_anchors() {`); `:2110` `anchor_disposition`, non-numeric arm FIRST (grep `anchor_disposition() {`); `:2149` the four-arm `case` wrapping the six anchor-dependent assertions (grep `case "$(anchor_disposition`); `:2224` the floor-guard controls (grep `population guard:`); `:2276` the skip-vs-pass source controls (grep `SHORT arm`); `:2518` the summary line carrying `$SKIP`. `starter-kit/FRAMEWORK_LEARNINGS.md:55` -- Learning **#33**, the ONE distributed file, pre-declared, 1,432 B, leaving 604 B of ceiling. `docs/planning/BACKLOG.md:1089` -- BL-40's completed row; `:9` the hand-maintained `**Open:**` list, from which BL-40 was removed; `:18` documents BL-16's deliberate heading-lessness, which a naive re-derivation reads as a phantom mismatch. `HANDOFFS.md:35` -- the corrected front-matter `--cut 3` warning. `CHANGELOG.md` -- the claim record and the deliverable record.
gotchas: **A SOURCE-LEVEL ASSERTION AND A ROUTING ASSERTION DO NOT INDIVIDUALLY PROVE RUN-TIME BEHAVIOUR, AND I WANT THAT SAID PLAINLY.** The standing suite proves the count ROUTES correctly (disposition controls) and that the SHORT arm's TEXT emits six skips and no passes (a source read). "A short ledger emits six skips at run time" is their CONJUNCTION -- an inference. The only direct evidence is the in-situ tamper run recorded in `runtime_smoke`, and that is a session artifact, not a standing test. If you strengthen one thing here, strengthen that. **DO NOT WRITE A HEREDOC INSIDE A HEREDOC WITH THE SAME DELIMITER.** My first patch script was a `python3 <<'PY'` whose payload contained `bin/tests.sh`'s own `<<'PY'`; the inner delimiter closed the outer one and the shell parse-errored on a line I had not written. Cost one cycle. Use a distinct outer delimiter or write the script to a file. **MY OWN RE-DERIVATION SCRIPT REPORTED A DEFECT THAT WAS NOT ONE.** Checking `BACKLOG.md`'s hand-maintained open list against its headings flagged BL-16 as declared-but-missing. It is documented as deliberately heading-less three lines above the list. I read the list and not the paragraph under it; I caught it before publishing, barely. **DO NOT `git reset --hard` IN THIS REPO** -- two tracked `.jsonl` telemetry files go dirty from Phase 0 alone. Inherited hazard, unchanged. **`bin/check-*` ARE PYTHON, NOT SHELL.** **`check-learnings` READS "32 row(s), contiguous 1..32" WHILE THE MAXIMUM ROW IS 33** -- `#14` is reserved, so count and max differ by one and "1..N" is the COUNT. Pre-existing; do not "repair" it. **NEVER PIPE A SUITE POPULATION THROUGH `tail`** -- S95's gotcha, followed from the first run; every capture here went to a file and every comparison was `comm` over sorted row sets.
runtime_smoke: **FOUR SURFACES.** This repo ships no application; the build-equivalent is the suites plus the tools, RUN. Figures measured AFTER the last non-receipt edit and BEFORE this receipt, so the `HANDOFFS.md` byte number understates by roughly this block. **SURFACE 1 -- the canonical checkout.** `bash bin/tests.sh` **247 passed / 1 failed / 0 skipped**, diffed ROW-FOR-ROW against a full 236-row pre-change baseline captured at Phase 0, both populations asserted non-empty: **ZERO ROWS LOST**, 13 added -- my 12 new controls plus Test 31's `**Model:**` equality moving 8 -> 10 as this session's own entries joined the population (it asserts an equality, not a level, so it tracks the data and stays green). Sole failure is Test 9's pre-existing `--source=github` 404, confirmed BY NAME, not by count. `tools.test_methodology_trim` 103/103, `tools.test_methodology_dashboard` 300/300, `tools.test_context_budget` 41/41. `bin/check-handoff --all --allow-pending` OK (5 receipts), `bin/check-links` OK (88/22, unchanged), `bin/check-learnings` OK (32 rows). **SURFACE 2 -- the pre-change code replayed against copied fixtures**, which is where the RED lives and where the six-not-five correction came from. The Test 34 body extracted verbatim from HEAD by `awk`, run at 1/2/3/4 receipts: **2 passed / 5 failed** at 1 and 2 (with an `IndexError` traceback and, for `duplicate-identity`, an `AttributeError` from `re.search(...).group(0)` -- so that row's message *"mutation was vacuous"* was not merely uninformative, it was WRONG about its own mechanism), **8 / 0** at 3 and 4. Post-change, same fixtures: **11 / 0 / 6 skipped** at 1 and 2, **17 / 0 / 0** at 3 and 4; a 0-receipt fixture gives **3 failures and no skips**. **SURFACE 3 -- mutation, on my own new guards.** 9 mutants, each verified to APPLY before running (`DID-NOT-APPLY` kept distinct from `survived`), **9/9 KILLED**, unmutated control green at 20/0/0: floor `1|2`->`1`; default `ANCHORED`->`SHORT`; MALFORMED arm deleted; anchor slice `len(ids)>2`->`>1`; `EMPTY`->`SHORT`; one SHORT-arm `skip`->`pass`; summary line reverted; one skip row deleted; `SHORT)` arm renamed. The last two are the ones that matter -- both are silent-vacuum reinstatements, and both die on the source-level count. **SURFACE 4 -- the real suite against a genuinely short live ledger,** tampered in situ from a COMMITTED state, backup held outside the repo, restore `cmp`-verified byte-identical afterwards. At 2 receipts: **241 passed / 1 failed / 6 skipped**, exit 0, the six rows naming themselves and the reason, against **229 / 6** for the same condition before the change. *Cannot enforce:* nothing tests that a FUTURE assertion added to the ANCHORED arm gets a matching skip row -- the source check counts six because six is what exists today, so an added assertion silently breaks the pairing. **FM #28 GATE:** `CLAUDE.md` ok, `CHANGELOG.md` ok (605 ln / 2,000), `starter-kit/FRAMEWORK_LEARNINGS.md` ok at **59,396 / 60,000 -- 604 B**, `HANDOFFS.md` **77,723 B OVER** (and rising with this receipt), `docs/planning/BACKLOG.md` **103,755 B OVER** (down 775 B). `growth run 17/10` is NOT evidence about anything here -- it is computed on `resident_bytes`, which in this repo is `CLAUDE.md` alone.
changelog_ref: CHANGELOG.md "2026-08-17 · [BL-40] Test 34's mutation anchors become an asserted population with a stated skip — six assertions could vanish, not five"
commit: e4da369 (1B claim + ledger) + 8d38833 (the deliverable) + this close-out
```
<!-- claim stub written at session start; completed at close-out -->
Model: Claude Opus 5 (1M context).

**Predecessor S95 scored 9/10.** **+** Its `next_steps` (a) was executable rather than descriptive:
it named the breach as its own doing, gave the cut (`--cut 3`), the constraint (Test 34's floor),
and the tension (no cut satisfies both the ceiling and the seed's stop condition) — and it explicitly
said *"re-derive the distribution before trusting any headroom estimate, including mine."* That
instruction paid immediately: its "~52 KB" was **57,842 B** when I measured. A handoff that tells you
to distrust its own number, and is right to, is doing the job. **+** Its (e) — `FRAMEWORK_LEARNINGS.md`
at 57,964 / 60,000, *"computed, not estimated"* — directly changed how I worked: I budgeted my Phase
3C row to 1,432 B **before** writing it rather than writing and then measuring. Without that line I
would have written a 2,500 B row like the last three and breached a distributed file's ceiling.
**+** Its gotchas were load-bearing, not decorative: I redirected every suite capture to a file from
the first run (never `tail`), left the two dirty `.jsonl` files alone, used `python3` on the
`bin/check-*` tools, and hit the `check-learnings` count-vs-max quirk exactly as described and did
not "repair" it. **+** It fixed the defect it deducted S94 for — its own `changelog_ref` resolves,
verified by `grep -F`: one exact match in the live ledger. **−** It repeated BL-40's *"five
assertions go vacuous"* as a fresh claim in its `next_steps` without re-deriving it, and the figure
is **six**. Its own receipt carries the arithmetic that disproves it (235 → 229 is six fewer passes
against five new failures). That is precisely the class of defect — a number true-when-written or
inherited-unchecked — that S95 deducted its own predecessor for, so the standard was applied
unevenly. Not a 10 for that; not an 8 because the deducted figure was inherited rather than invented,
and because two of its lines measurably changed what I did.

**Self-assessment: 8/10.** **+** The RED came from replaying the pre-change code against copied
fixtures at four population sizes, not from reading the two write-ups — which is the only reason the
undercount surfaced at all. Both documents were honest and both were low, and the discrepancy was
sitting in published arithmetic that nobody had subtracted. **+** I mutation-tested my own new
guards before believing them: 9 mutants, each verified to APPLY first, 9 killed, unmutated control
green — including the two mutants that would silently reinstate the vacuum. **+** I identified the
property with no counter behind it — a skip routed to `pass()` would *raise* the pass count and move
nothing a reader watches — and wrote the assertion for it rather than trusting the comment. **+** I
kept the deliverable to one capability: the trim this unblocks is not in this session, and I said so
at claim rather than at close-out. **−** **I claimed this receipt was the smallest of the last five and it is the second
largest.** I measured my fence block alone (13,986 B) against predecessor figures that include the
assessment prose beneath each block. On the same basis mine is **18,845 B** — S95 21,266, S92 16,840,
S94 16,135, S93 15,159. Caught at the final gate by re-measuring all five with one script instead of
trusting the number I had written earlier in the session. A population compared against a different
population is the exact error class this session's deliverable is about, and I made it while writing
that up.
**−** **My first patch script was a heredoc nested inside a heredoc with the same delimiter**, so the
inner `PY` closed the outer one and the shell parse-errored on a line I never wrote. Trivial, and
avoidable by reading my own payload before running it. **−** **My re-derivation of `BACKLOG.md`'s
open list reported a mismatch that was not a defect** — BL-16 is documented as deliberately
heading-less three lines above the list I was checking. I trusted my script over the prose next to
the data and nearly published a phantom finding; I caught it only by grepping where BL-16 actually
lived. That is the same error shape as the one I was fixing, at a smaller scale: a population
measured with the wrong net. **−** **I added 289 B to `HANDOFFS.md`'s header zone while the file is
12 KB over its ceiling.** The warning it replaced was false — it said the defect was "avoided here,
not fixed" — so correcting it was right, but it is spending in the wrong direction on the exact file
I am handing over broken. **−** The standing suite proves routing and proves the arm's *text*; that a
short ledger emits six skips at run time is their conjunction, an inference. The direct evidence is
an in-situ tamper run that lives in this receipt and not in the suite. Not a 9 because three of my own
verification artifacts were wrong on first construction — a phantom backlog finding, a nested-heredoc
script, and a size comparison across mismatched populations; not a 7 because all three were caught
in-session by re-measuring rather than by luck, and because the central claim was measured four
independent ways before it was written down.


