# Framework Learnings

The methodology's own framework learnings, recorded as the canonical repo dogfoods itself. This
file is the companion to [`SESSION_RUNNER.md`](SESSION_RUNNER.md) — Phase 3C routes every session
to the right learnings home, and for a framework-level learning that home is here.

**Canonical methodology repo:** append a new row at Phase 3C — **append only; never renumber.** A
row's NUMBER is fixed forever, because every `Learning #N` citation resolves by number. Its CONTENT
may be **compacted** — said shorter without saying less, keeping every mechanism, figure and
citation. That is the one edit an existing row permits. The rows below are real framework learnings,
not placeholders.

**Budget the row before writing it — 1,500 B, checked by `bin/check-learnings`.** A partial read of
this table returns whole rows, because a row is one physical line, so row size is what a session
actually spends here: measured over 80 transcripts of the canonical repo, this file was read whole
once and read in part 243 times. **The check covers every row, not only the one you are writing** —
an over-budget row is a finding with a remedy, not a permanent red mark. Say it shorter rather than
raising the budget.

> **`#14` is reserved, and the gap between 13 and 15 is deliberate — do not close it by renumbering.**
> It is held by `docs/operator-gated-review-plan`'s D3, a branch from the *upstream* session sequence
> that is not present in every clone. S34 verified the reservation is attributable and respected it;
> S35 appended at **15** for the same reason. Renumbering would break every existing `Learning #N`
> citation, which is exactly what "append only, never renumber" exists to prevent.

**Adopter projects:** do **not** edit this file. Like `SESSION_RUNNER.md` it is synced from
canonical and must stay byte-identical, or local edits will block future syncs (see
[`BOOTSTRAP.md`](BOOTSTRAP.md), "Customizations Go in CLAUDE.md, Not in Synced Files"). Record
project learnings in your `CLAUDE.md` → **Project-Specific Methodology Adaptations** →
**Project-specific Learnings** subsection instead.

| # | Learning | Source | When to Apply |
|---|----------|--------|---------------|
| 1 | Plan-mode output is a draft, not a verified plan. When a prompt contains a multi-phase plan with "implement," the deliverable is a plan document with evidence-based inventory, not Phase 1 code. The gap: Phase 1's task mapping had no entry for plan-mode handoffs, so the session defaulted to "implement." Structural fix: new mapping row + FM #19. | FM #19 discovery | When a prompt contains a multi-phase plan with "implement" — recognize this as a planning workstream. |
| 2 | **Protocol discipline is perishable.** 14 consecutive clean sessions can collapse to 1/10 deliveries within 12 hours of relaxed discipline. The protocol is perishable — it doesn't maintain itself. Each session must actively re-internalize it, not assume it's "already known." The compounding loop works only when every link in the chain is complete. | Field observation | Whenever you catch yourself thinking "I know the protocol, I don't need to re-read it" — re-read it. The fast-collapse case (FM #17 slow-drip's sibling) is real. |
| 3 | Plans should flag "here be dragons" areas where implementation is non-obvious — not all phases are equally risky. Call out which phases need extra caution, what assumptions are load-bearing, and where the executor should stop and re-orient. | Field observation | When writing any multi-phase plan. A plan that presents all phases as equally tractable lies to the executor about where the cost actually lives. |
| 4 | **Verify a plan's output against its completion criteria — not against session duration or count.** Execution speed is not evidence of plan quality, and "fits in one session" is not a planning goal. A plan whose phases each fit cleanly in one session and produce work that matches the completion criteria is high-quality; a plan that tempts the executor to bundle phases or skip close-out to "finish faster" is not. The "1 and done" rule does not bend for high-quality plans. | Field observation (refined from rmsharp feedback on issue #7) | When evaluating a plan or judging plan quality. Resist the temptation to read execution speed as plan quality — they are uncorrelated when the protocol holds. |
| 5 | Code review is a distinct deliverable, not overhead. Reviews that produce actionable plans (exact code snippets, line numbers, implementation order) have higher ROI than vague "this needs improvement" feedback. A review that doesn't identify specific changes a future session can execute is incomplete. | Field observation | When the session deliverable is a code review. Output an actionable plan, not a critique. |
| 6 | A plan written from memory of a file read is an assumption-level claim. Reading implementations before estimating complexity catches wrong assumptions early — estimating from a backlog description alone is unreliable. This is a special case of FM #11 (gaps from memory) and FM #20 (edit from memory) applied to planning. | Field observation | Before estimating effort or scope for any phase, read the actual implementation file. The backlog description is a hint, not a spec. |
| 7 | **Cross-reference completeness at self-review.** When a change adds a citation, an index entry, a glossary term, or any cross-reference between files — and especially when it modifies a numbered set (FMs, principles, phases, anti-patterns, learnings) — `git diff` confirms only the present-side edit; the defect is the *absence* on the destination side, which the diff cannot surface. Grep each cited destination to confirm the citation resolves, and grep nearby prose for set-size or named-membership assertions ("N failure modes", "the three workstreams") that may have drifted. Bidirectional: a new index entry needs its destination to cite back; a grown numbered set needs its size claims re-counted. | Three v2.6 instances: stale "24 failure modes" count, understated `/diagnose` + `/grill-with-docs` citation rows, aspirational `/init` citation (issue #19) | When a change touches any cross-file citation, index, glossary term, or numbered set. At self-review, grep destinations and size claims before committing — do not rely on `git diff` alone. |
| 8 | **A close-out gate is only as strong as the weakest checklist that summarizes close-out.** Session-type procedures (Planning, Review/Audit, Debugging, the vertical-slice revert, and campaign per-session checklists) each restate the close-out in condensed form. An agent following a condensed checklist never re-reads the full phase, so any cross-cutting gate the summary omits is silently exempted for that session type — exactly how an "authoritative" ledger acquires holes. When you add or change a close-out step, reconcile it into *every* checklist that restates close-out, not only the canonical Phase 3/Phase 6 text. This is Learning #7 applied to procedure summaries rather than citations. | FM #27 / escape #8 (CHANGELOG-ledger campaign, S7) | When adding or modifying any close-out or other cross-cutting gate: grep every session-type and campaign checklist that restates close-out and confirm the new step appears in each. |
| 9 | **A handoff is only dependable when it is a durable, machine-checkable artifact.** A transient narrative — spoken, or written only to the wholesale-overwritten `SESSION_NOTES.md` — leaves no evidence of its own absence, so a skipped close-out report is invisible until a human notices (the failure that motivated the receipt: an agent had to be *prompted* for its report). The fix mirrors the ledger (FM #27) exactly: **gate-on-write** (a `HANDOFFS.md` receipt whose structure `bin/check-handoff` verifies) **AND reconcile-on-read** (Phase 0 greps for a missing/`status: pending` receipt and backfills it `status: reconciled`) — neither is dependable alone. The honest ceiling is the same too: the check verifies presence and structure, never quality (a green check is not a good handoff), and a session that leaves no commit and no 1B stub still escapes. This is Learning #8 applied to the report itself, not just the ledger. | Close-out receipt slice (this session) | When adding any close-out artifact or gate: make it a durable, greppable artifact with a write-gate AND a reconcile-on-read backstop — never a transient note or a spoken summary. |
| 10 | **A review pass scoped to the diff has a blind spot: what the change made stale *outside* it.** Learning #7 (cross-reference completeness) and Learning #8 (close-out-gate propagation) are authoring-time self-checks — they catch drift the *author* introduces. But an adversarial review or audit is pointed at the diff by default, so "what did this change make stale elsewhere in the corpus?" is nobody's job. The v3.3 close-out-receipt work (#52/#53) shipped a clean 6-lens adversarial review, yet left `HOW_TO_USE.md`, the `README.md` repo tree, and three tutorials describing close-out *without* the new receipt — none were in the diff, so no diff-scoped lens looked at them. A human had to ask "has documentation been fully updated?" (fixed in #54). | Upstream issue #55 (completeness-critic proposal), motivated by the v3.3 doc-completeness gap fixed in PR #54 | Promote Learning #7/#8 from an authoring self-check to a review-time lens: when a change adds, renames, or removes a concept, artifact, file, step, or numbered-set member, grep the *whole corpus* (not just the diff) for file/artifact enumerations, worked examples/tutorials, indexes/cross-references, and count/size claims ("N failure modes", "the six requirements") that now lag. See `workstreams/AUDIT_WORKSTREAM.md`'s Verification Checklist for the operative step. |
| 11 | **Heterogeneous capability tiers within one slice are a division of labor, not a new gate.** Splitting a pre-declared vertical slice's layers across agents of different capability tiers — with the strongest tier reviewing every lighter-tier layer's output before that layer's checkpoint commit lands — satisfies the existing per-boundary verification gates (c)/(d); it does not add a fifth gate or substitute for the build/test matrix. The risk is a plausible-sounding misreading: treating cross-tier review as proof a layer is correct on its own, or batching review to the end of the slice instead of per boundary — either one reopens failure mode #26 (mega-session masquerading as a vertical slice) wearing a division-of-labor costume. | The close-out-receipt slice's own hybrid capability-tier build (`HANDOFFS.md` session S1; concrete agent/model names live in `RECOMMENDED_SKILLS.md`), formalized as the §Vertical Slice Sessions "capability-tiered review" addition (BL-7) | When a slice's layers will be built or reviewed by agents of different capability tiers: state the tier assignment in the gate (a) contract, delegate a layer only where an objective gate (test suite, exhaustive grep, mechanical spec) proves its correctness, and confirm review happens per boundary before each checkpoint commit — never as one pass at the end. |
| 12 | **When an invariant is mechanical, encode it as a test — a review-time grep is a human step that silently stops happening.** #7/#10 are that grep: apt for prose relations, weak between two enumerable sets. v3.3 added `HANDOFFS.md` to the distributed corpus, not the health scanner's methodology checklist, which silently stopped describing the corpus it scores, for *two* releases of those greps. As two lists ("every distributed adopter-root destination is on the checklist or explicitly exempt with a stated reason") it is a few lines of test, plus one guarding the guard: a stale exemption naming no real file hollows the invariant. So the class closes: the next such file fails the suite instead of going uncounted. Non-negotiable: drive it RED on unpatched code; watch it fail before trusting it. A mechanized invariant never seen to fail is that trap one level up, hit twice: an absence-check filtered a dict by `"Large files" in r`: keys, vacuous pass; a fixture whose render toolchain short-circuited the counts under test could not fail, yet looked like coverage. | Dashboard signal-integrity campaign (S9–S16): manifest-vs-checklist guard S9 → Layer 1; 2 vacuous tests, Layer 7 | When something joins an enumerable distributed set, or you lean on #7/#10 to align two lists: assert the relation, RED first. Pair every absence assertion with a presence control; ask what a fixture makes *unreachable*, not just what it asserts. |
| 13 | **A forward-looking claim cannot be checked by re-reading a file — it must be computed.** Learning #6 and FM #11 catch claims written *from memory*; both prescribe the same repair: re-read the confirming file. #7, #10 and #12 catch cross-refs gone stale *in the corpus*. Neither reaches a handoff's predictions. "Expect one merge conflict", "these are the files you will touch", "this should be quick" describe a state that does not exist yet, so no file confirms them; the only honest check is to derive it or call it a guess. The Source case did not go stale — the tempting diagnosis, and wrong: it was never true. A wrong prediction is worse than none: it licenses the successor to resolve *quickly*, not *carefully*, and a merge resolved on the belief that "only the ledger differs" discards work silently, with no failing check to catch it. | This repo's v3.6 release — a receipt predicted one conflicting file and the merge met seven; the sentence was written in a commit that had itself changed all seven | When you *write* a forward-looking claim — a merge outcome, the files the next session will touch, an effort estimate — derive it or label it a guess. For a merge, deriving is exact and touches no working tree: `git merge-tree --write-tree --name-only <ours> <theirs>` lists conflicting paths between line 1's tree id and the trailing `CONFLICT` block. When you *inherit* a prediction, re-derive it before acting. |
| 15 | **Prove "nothing lost" over the PARTITION, not the total: a whole-set check is structurally blind to misplacement.** Across two artifacts (ledger/archive, file/extraction, table/sibling), *reassemble, compare to the original* is necessary, never sufficient: **moving content between halves is exactly preserving under reassembly**. Here a byte-for-byte whole-file `md5` reconstruction shipped in the same commit that moved a file-scoped paragraph out of its declaring file: still *somewhere*, so it passed; still missing. Newest-on-top puts such text at the bottom, where an oldest-first cut takes, migrating *by position* unless pinned. **Repair:** zone *before splitting* (partitionable vs. file-scoped), assert each separately (*every byte of the pinned zones is in THIS file, absent from the other*), alongside reassembly, never instead; scope it to the partitionable zone only, or any legitimate regeneration trips it: refuses every run. **Publish the payload BOUNDARY and COMMAND with any digest:** a hash whose payload definition is unrecorded cannot be re-derived by a successor (five reconstruction attempts here failed): assertion, not proof. | S35 (2026-08-03), ledger-trimmer design: 3 hand-run archives, one lost content while its proof passed | Any split, extraction, archive, migration or de-dup moving records between artifacts. The tell: a check on totals (counts, sizes, digests, "N in, N out") not where each part landed. |
| 16 | **Wire the assertion to the ARTIFACT, not the inputs it came from; a predicate-only mutation score proves nothing.** Learning #12 (encode the invariant, drive it RED) can pass with an inert guard: it asks if the assertion *is correct*, never *what it is handed*. Operands derived from each other assert an identity: `records[:k] ++ records[k:] == records` holds for every input, so the guard cannot fail however badly the code between split and write corrupts it. Tell: operands computed, not read back. **Repair: re-parse what you will write with the grammar you will read it with and assert over THAT.** Mutation harnesses inherit the blind spot: killing every predicate mutant proves them correct *as functions*, not wired to anything. Mutate the **producer** (the code that builds the artifact), not only the **checker**; checker-only mutation scores 100% over tautologies. | S36 (2026-08-03), ledger trimmer: a 13-mutant harness killed 13/13 while all 3 losslessness assertions were identities at their only call site; a write path silently dropping a record printed `[L1_OK] [L2_OK] [L3_OK]`; only the independently generated proof script caught it | When checker and checked share a run: losslessness proofs, round-trip/idempotence, reconciliation, migration verifiers, transform invariants. Ask *where did each operand come from?* (one source for both = identity) and *does my mutation set include the producer, or only the checker?* |
| 17 | **A spec demanding both "read X's value" and "assert yours equals X's" is a contradiction, not two requirements; the owed test's falsifiability picks the half to build.** A **ratified** design put both **in one paragraph** — read the owning tool's number; prove the displayed number *equals* it — yet still read as two. Read the first literally and the second is an identity over one value: passes forever, cannot fail, certifies nothing — #16 one level up: tautology in the **spec**, not a test. Don't read harder or pick a favourite: **build the reading whose owed test can go red** — compute independently, reading only the one input that tool owns (a calibrated constant, regex-parsed from source). Force disagreement to prove it discriminates: three separate divergences did, each a real defect the agreement would have hidden. **Corollary (load-bearing):** an agreement holding only by this repo's history isn't one: every archive shard here shrank its ledger, so a missing filter agreed by luck; only a fixture built to break it diverged. Ask what accident of *this* corpus holds them together; build the case removing it. | S38 (2026-08-03), trim-trigger dashboard row: design §1.3 asked for both; agreement test broke the tie | A design that consumes another component's value AND must show they agree: dashboards over tools, caches over sources, denormalised fields over origins, re-implementations synced to a reference. |
| 18 | **Instructional prose asserts behaviour: RUN the artifact, name the command whose output you watched; "the design says so" cites an intention, not a behaviour.** Learning #16 (test wired to the artifact, not its inputs) has a prose sibling that bites harder: no red state to fail into. A ratified design promised *"staged-but-uncommitted"*; the tool never stages. In a distributed template it would have told every adopter their change was staged, so an ordinary `git commit -a` would land the shortened ledger, never its removed-records archive: exactly the loss its three losslessness assertions exist to prevent, invited by documentation, not code. The spec was stale, not lying: its rollback table two lines below already contradicted it. Same session, second instance, same file: a repo-history anecdote's causal order inverted from two same-day timestamps; `git merge-base --is-ancestor` settled it. | S40 (2026-08-04) ledger-doctrine seeds: `--write`'s seed copied "leaves the change staged for you" verbatim from `ledger-trimmer-design.md:722`; shipped tool: no `git add` anywhere, prints `WRITTEN (uncommitted — this tool never commits)`, shard untracked | Before shipping any behavioural sentence in a spec, seed, or distributed template (what a tool leaves staged, tracked, committed, or otherwise does), run the artifact and watch what it actually does. Causal order (*this before that*): one command, not inferred timestamps. |
| 19 | **A version tripwire keyed to something that never changes across versions cannot fire — and it fails by reporting *current*, worse than silence.** A stale-format detector matched each adopter-owned template against a marker string, its H1 title. Stable — why it was chosen — but stability is half a version marker; the other half is **absence from the versions it must detect**, and a title that never changed is absent from nothing. So when the format did change, every old-format project matched and read **current** — a confident wrong answer — while a promise elsewhere in the corpus ("the format lag is surfaced rather than silent") went false, no test failing. It is the unkillable guard in *data*: a key no version change can falsify is a constant shaped like a version check. Accept that upkeep rather than design around it — a discriminating marker must MOVE whenever the format materially changes, pin it with a case that reddens when it hasn't. | S41 (2026-08-04), the URL-update-path fix: `SEED_FORMAT_MARKERS` keyed each distributed seed's check on its own H1 title; 11 siblings holding `CHANGELOG.md` and 9 holding `HANDOFFS.md`, none carrying the new doctrine, all reported `present` | Designing or reviewing any version/format/staleness tripwire: name a past version the marker would actually have caught. If you cannot, it is decorative — re-key on something provably absent from the versions it must detect. |
| 20 | **Reviewing candidates independently does not review their SYNTHESIS: a graft of reviewed pieces is itself unreviewed.** The panel was sound (fresh agents, no shared state, one lens each) and its judges did verify their candidate against the real source, not trust its prose — but each *alone*: nobody scored the seam where two candidates' pieces meet, and a defect there is unreachable from either alone, in which those pieces never coexisted. **The tell: ask what the judges scored.** If it was "each candidate as written" *and* the shipped design matches no single candidate, judging has zero coverage of what ships. Repair: an independent pass over the **synthesis**, not a re-score of candidates, ideally shaped unlike the process that produced it and missed the seam: hand-trace, citation re-check. | S57 (2026-08-09), issue #67 fix plan: a 3-candidate/6-judge panel scored each candidate in isolation; the synthesis grafted one candidate's target-scoping onto another's `.gitignore`-aware gate; a second adversarial pass found a HIGH-severity graft defect: the gate silently went unconditional whenever target-scoping pointed at a directory not yet a git repo — invisible to any review of either candidate alone | When a design, plan, or fix grafts (not merely selects) pieces from independently-reviewed candidates, proposals, or PRs: review the combined artifact afresh — judge scores cover the ingredients, never the recipe. |
| 21 | **"Make it shorter" is met by moving a size-CORRELATED metric while the target stays flat, and an adversarial verifier correctly built for lost facts cannot flag a candidate that dropped nothing and shortened nothing real: lossless ≠ compacted.** Told "~40-60% of the original line count" (symptom: line cap), every candidate unwrapped hand-wrapped breaks into one paragraph: 27 lines to 3, chars down 0.0-0.6% on most receipts (one 2,458 to 2,457); 150 agents' work passed every check achieving almost nothing. A direct character count, not trust in "fewer lines", caught it pre-commit; the real constraint was a BYTE budget. **Ask which unit the downstream problem is measured in; instruct, measure and verify in THAT unit, not a proxy; where fewer lines meet the letter without moving what the consumer reads (bytes, distinct facts, tokens), ban reformat-only edits.** | S61 (2026-08-09), `HANDOFFS.md` trim: 39-receipt compact+verify Workflow pass, line-count target: ~1.4% real (character) reduction reported apparent success (fewer lines, zero verify failures on the reduction dimension); re-run on an explicit character target + "line-unwrapping isn't compaction": real reduction 4.7% | Any agent/workflow asked to make an artifact smaller/shorter/more compact, checked only for correctness/losslessness, never target-metric movement: compaction, summarization, log-trims, prose edits, boilerplate cuts; count that unit both sides. |
| 22 | **A completed item's own TEXT can be load-bearing for a sibling's meaning: deleting it silently orphans a reference; no test catches it.** Phase 3F's "remove a completed backlog item in the same commit" assumes independent deletability. Two adopters falsified it independently under different markers (wsfct `🟢 CLEARED`, nprcgenekeepr `- [x]`): both `BACKLOG.md`s had grown cross-item references, by relative position ("the item above", "immediately below") or quoted title, so a completed item was a still-OPEN reference's target. Marker-grep-and-delete would have corrupted live work in both: wsfct kept 2 of 15 flagged rows, nprcgenekeepr 5 of 9, found only by a pre-delete grep of the *whole file* for positional/quoted references, not local neighbors. nprcgenekeepr chained three, A→B→C by "immediately below": any proper-subset removal breaks it; one member was quoted from outside. **Repair:** remove a mutually-referencing cluster only whole; one quoted from outside cannot go alone. | Cross-repo adopter maintenance, 2026-08-10 — `BACKLOG.md` cleanups removing 13/15 rows (wsfct), 4/9 items (nprcgenekeepr), both ad hoc, in neither adopter's numbered sequence | Before removing any completion-flagged item from `BACKLOG.md` (Phase 3F) or a similarly list-shaped ledger with independently-authored entries — grep the whole file for relative-position phrasing and the item's own quoted title, not just its local neighbors. |
| 23 | **A backlog/issue item naming a defect is a claim frozen at filing time, not a live read of current behavior — unrelated work between filing and actioning obsoletes it, record unrevised.** `church_growth/BACKLOG.md` flagged `detect_doc_only()`'s self-referential source-LOC miscount (2026-07-21) as needing a canonical-repo fix; another campaign's four commits shipped it four days on (Layer 7, `ae9e5b7` et al., 2026-07-25). Two weeks on, cross-repo audit S70 still called it "ready-to-act", unre-tested. Re-running the CURRENT canonical scanner — not re-reading its prose, not trusting its own "not fixable from this repo" tag — found the defect gone: `source loc` 0 after vendor reclassification, `is_doc_only=True` even with the local override marker removed; the fix became a record correction. **The tell:** an item's age against `HEAD` is itself a signal — the longer it sits, the likelier something else touched that code path. **The repair:** re-derive against CURRENT code that the defect still reproduces. | S71 (2026-08-10) verifying `church_growth/BACKLOG.md:4-20`: the canonical-repo fix it asked for shipped 2026-07-25, unknown to the two-week-old backlog text still requesting it | Before spending a session on a fix a backlog item, issue, or handoff `next_steps` still claims is needed — re-run the check against current code first; do not treat its text as present behavior just because no one marked it resolved. |
| 24 | **A check keyed to the LIVE file has a population the protocol's own archiving moves out from under it; empty reads "failed", not "nothing to check".** FM #28 ceilings every mandated-read artifact, exceeding it a defect, so archiving is routine: a whole class of checks turns conditional on where its population lives. `bin/tests.sh` Test 29 body-line-budgets `CHANGELOG.md`'s `Reconcile-on-read` entries, pass `count > 0 && violations == 0`. An operator-directed trim moved the last 10 to a shard: count 0, `budget violated -- `, nothing after the dash. Nothing eroded: both correct, incompatible because the population was file location -- **make it the whole ledger, live + archives, never today's live shard.** The presence half reddens on the wrong defect; lacking one, a check greens vacuously forever. Learned twice, never wired in: `CHANGELOG.md`'s source-tag audit reads archives on purpose; `methodology_trim.py`'s `classify_empty` splits empty from unreadable (UAT F1). Prove the new arm can fail: inflating the count but structurally unable to violate is Learning #16's tautology; a synthetic overlong entry on a scratch-copy shard moved it 43/0 -> 44/1. | S87 (2026-08-15), both over-budget ledgers archived: `CHANGELOG.md`'s trim (68 records) emptied Test 29's live population | Archiving/splitting/sharding/rotating what a test, checker, audit grep, or dashboard reads: enumerate consumers, re-point each at the whole ledger. |
| 25 | **A correlation perfect across every artifact you have can be a confound: collinear causes separate only at the off-diagonal cell.** Six proof scripts split by generator version (old fail, new pass) and identically by whether its commit bundled edits beyond the proved one: two variables, one population, none separating them. The filing disclaimed causation, blamed the version anyway: six artifacts make a perfect split look total. Build the missing cell, usually far cheaper than the reasoning it replaces: two swaps, two runs (new script, old artifact; then reverse) inverted it: new failed all four old cases byte-identically, old passed new. It had fixed a real, different second defect, hence the clean split; regenerating provably fails, effort wasted unnoticed. A failing proof is evidence about the proof, not the data: re-running repeats it, regenerating greens unexamined content. Re-derive by other machinery (identity, not position), mutation-proved able to fail before trusting its zero (Learning #16). | S88 (2026-08-15), auditing four archive losslessness proofs FAIL five days: archives intact, same-commit bundling broke their positional identity, every generator version | A report blames a correlated cause (version, flag, directory, author, date): what else splits your actual population identically? Never argue it out: copy the check, swap in the other case, run it; re-derive a failing check by unshared machinery. |
| 26 | **Archiving everything FINISHED can still leave a file over its ceiling: the excess may be the LIVE work, which no reduction can remove.** A backlog ran 2.10x its own ledgers' budget; the agreed diagnosis was true: 45,288 B in 11 closed items sat in the open-work-only section, unremoved at close-out against Phase 3F. Archiving all of it verbatim recovered 33%, correct and worth doing, yet missed the ceiling — unreachable by it or any housekeeping: measured before the session's own writes, the OPEN items alone were 1.04x the budget. The tell precedes the work and costs one measurement: **sum the part you may NOT remove and compare that to the ceiling first.** If that remainder exceeds it, ship the reduction plus that finding, not the ceiling; the real question is policy (how much live work may this file hold?) that no amount of archiving can answer. Also: no file can accurately state its own byte size (writing it in changes it); keep it elsewhere with a re-derivation command. | S89 (2026-08-15), `docs/planning/BACKLOG.md`, after FM #28 was measured to cost a session: 137,440 -> 91,857 B (-33.2%), all 11 archived, still 1.40x the 65,536 B yardstick; 16 inherited open items: 68,195 B | Handed a size target for a grow-and-must-be-read artifact: before agreeing it is reachable, sum what you may not touch and compare to the ceiling; if that alone exceeds it, re-scope at claim time to reduction plus finding, not close-out |
| 27 | **A derived number and its goodness-of-fit statistic are TWO claims, only the first read: the number carries authority, the caveat is scenery.** A tool emitted `opening_tokens ≈ 46,319 + 0.0692 × bytes, R² = 0.0513 ⇒ 14.45 B/token` — both tells on screen: R² 0.05 = 5% of variance explained; 14.45 B/token is implausible on its face for dense markdown. No fit gate: 0.05 printed as confidently as 0.81. Same shape as the dashboard failure that motivated it: `Large files detected` every Phase 0, unread by 15+ consecutive sessions. **Surfacing a signal is not gating on it.** **Vary one cause at a time and re-run**: four fits (as-shipped, A, B, both defects fixed) told causal from cosmetic: A moves R² 0.05->0.69, B alone 0.05->0.08. Then **corroborate before adopting on an instrument sharing none of its machinery**: a natural experiment on one large commit, same quantity by arithmetic not regression. | S90 (2026-08-15), calibrating this repo's own `.context-budget.json` (BL-37): the shipped `--calibrate` failed: `git log -- <file>` walks all merged ancestry, interleaving the file's upstream and fork lineage by date; that fixed, with an ISO-string-vs-mixed-offset comparison, the same 75 sessions gave R² = 0.8083 ⇒ 2.80 B/token, independently corroborated at 2.52, 2.93 (partial fix) | A derived number beside a fit statistic, n, CI, or coverage: read the second BEFORE the first; a bad one is a refusal, not a caveat |
| 28 | **Repairing a defect silently falsifies every record that DESCRIBED it — and they sit in the high-authority files a later session reads to judge that repair.** Close-out logs what a session *found*; no step re-checks what an earlier one *wrote about that defect*. They pile up — config key, backlog item, ledger entry, handoff gotcha — each a measurement frozen at diagnosis time; the fix inverts all at once. Worst survivor: a WARNING, written to be obeyed, not re-derived — *"14.45 is noise; do not adopt it"* protects while the defect lives, IS the noise once fixed. Like a constraint's release, the repair is an unlogged event. Cheap countermeasure: the wrong number is its own search key — grep `14.45` finds a claim naming it, never a paraphrase. | S91 (2026-08-15), fixing BL-38's five defects in the distributed `context_budget.py`: `--calibrate` returned 2.81 B/tok at R²=0.8054, immediately falsifying the `_bytes_per_token` warning S90 put in this repo's own `.context-budget.json` — a key existing only to tell the next session which number to trust — plus the backlog item and BL-37's forward pointer to it | When the deliverable is a FIX not a finding, grep the defect's own identifiers at close-out — item ID, tool and function names, above all the WRONG NUMBER — and repair every description in the session that falsified it. A document warning about the defect is the highest-priority hit, not the safest |
| 29 | **A rule reaches only as far as its section: one corpus hit, its own definition, is zero INBOUND CITATIONS — nothing general routing there.** Opt-in section, general-sounding text, scoping heading above the fold: it binds whoever wanders in, not whoever it's about. **Quote from the general site, never restate:** restatement drifts into a rival; a quote makes the count ≥1. **And in TIME:** even where it bound it fired only once an artifact existed, past the last actionable moment. Both coordinates — *who* enters, *when* it fires: too late is as inert as never. | S92 (2026-08-15), upstream issue #75: §Vertical Slice Sessions gate (d) — "Faithful verification, per surface", "faithfulness is established, never assumed" — was what an adopter's ordinary 3-phase plan needed; grepped across the entire distributed corpus: **one line, its own definition** (`gate d` recurs in-section at `:176`; outside it exactly one fork citation, a planning doc applying it, not routing to it). It bound only opted-in slices, only at a layer boundary, so the plan's Phase 1 simulator run called "verified live against prod" was out of reach. | When a general failure recurs that some rule covers, or you add one to a doc with elective sections: grep its most distinguishing phrase (every spelling) corpus-wide **first**: one hit = orphan, route to it. Pin the quote with a SOURCE-mutating test: a citing-side guard can't see the referent move |
| 30 | **When a check is EXPORTED, ask which operands were FACTS at write, INFERENCES at run.** In memory the tool added: *records this op adds* (0/1) was a fact. It baked that into a script for a later reader, re-deriving from a COMMIT that may bundle other writes: an inference, right for commits holding only the op (every case anyone had checked), wrong BY CONSTRUCTION for any bundling more. 4 of 6 shipped proofs read as loss, none lost | S93 (2026-08-16), BL-36. `.verify.sh` skipped `INJECTED` records POSITIONALLY; S88: fault in the proof, archives intact (`docs/audits/2026-08-15-bl36-archive-losslessness.md`). Fix: set from record CONTENT at verify time; 2 of 4 green, 2 red for cause: a receipt edited in the trim commit, pre-trim bytes gone | For any artifact a checker emits (proof script, lockfile digest, assertion), enumerate and mark each operand. (1) At verify time measure inferred ones FROM THE ARTIFACTS, by a DIFFERENT function than the assertion they feed; from the asserted difference it is an identity that cannot fail (Learning #16). (2) One tolerated shape doesn't license its neighbours: narrowed controls green before AND after, not just the fixed case. (3) A generated script is a FLAT namespace: a new binding SILENTLY rebinds another clause's same name, so the check gets that (usually empty) value, flagging a downstream symptom, not its finding. Grep each name you add; assert the SPECIFIC finding's wording, never the exit code — a union over every clause |
| 31 | **Fixture anchors computed from a live artifact couple the test to that artifact's SIZE, invisibly to the tool that shrinks it.** A suite mutated the receipt ledger to prove a checker catches orphans, duplicate ids, stale `pending`. Its authors had defended the obvious hazard — anchors read at run time as `ids[1]`/`ids[2]`, never hardcoded, commented that a literal id *"would itself go vacuous the next time the ledger rotates."* — covering WHICH records rotate, not HOW MANY survive: a correct budget-driven trim left two records, no `ids[2]`, both anchors empty, five assertions stopped asserting. **The partial defence is what hid the rest:** its reader concludes rotation is handled | S94 (2026-08-16), BL-40. `bin/tests.sh` Test 34 on `HANDOFFS.md`; the trimmer's default cut left 2 receipts: 235 passed / 1 failed → 229 / 6. Nothing was lost, the proof passed — *record* fine, *fixture* gone. Seen only because the harness scores `mutation was vacuous` apart from *survived*; folded, those five pass as silent no-ops, the suite stays GREEN | Before shrinking any artifact ask **what reads this file that is not a human**: grep the test tree for its path. **(1)** A test with computed anchors must assert `len(ids) >= 3` or SKIP WITH A STATED REASON; an empty anchor is no pass, no fail. **(2)** Keep `did-not-apply` distinct from `survived`. **(3)** Defend one dimension of a hazard, name in that comment those you did NOT |
| 32 | **A count-based cut cannot see a CONTENT-population floor; whether it trips is a fact about data layout, not an invariant.** Counts find [[31]]'s dimensional floor (`ids[1]`/`ids[2]` → ≥3 records); a content floor asks *which* records: a live ledger's "`**Model:**` non-empty" needs one carrier in the RETAINED half; a dozen non-carriers fail it; a positional cut cannot say *"keep ≥1 with P."* 16 carriers fell to 2, safe only because 2 sat atop a newest-first ledger; without the bullet in recent entries the same count empties the population and reddens a green test | S95 (2026-08-17), `CHANGELOG.md` 84,765 → 31,539 B. `bin/tests.sh:1886` needs that population non-empty; only records #4 and #13 of 41 carried it — unstated N ≥ 4, default retains 13. Count floors held (`.context-budget.json` structure guard: ≥ 5 records); neither implies the other. `bin/tests.sh:1957` spans it, green: an EQUALITY of derivations, not a level; equality never substitutes for a population check | Before any trim, list the readers; classify each floor: **count** (how many records survive) or **content** (which property). Check content only by simulating the cut BEFORE writing (`retained = lines[:idx[N]]`), re-running the predicate on it; dry-run bytes, counts and the losslessness proof (reconstruction, not the live half) are blind. Record **by how much and why**: *"2 survive by positional accident"*, not *"the default is safe"* |
| 33 | **A failure count undercounts coverage loss, because an assertion nested inside a guard that fails emits no row at all.** A trim shortened a live fixture below a test's floor. The defect was written up twice — a backlog item and a framework learning — and both said **five** assertions had stopped asserting, five being how many `FAIL` rows appeared. Six had stopped. The sixth sat in the then-branch of the same block as one of the five, so when its mutation failed to build it printed nothing: not a pass, not a fail, no row. Both write-ups carried the discrepancy in plain sight — 235 → 229 passes is **six** fewer against **five** new failures — and nobody subtracted. | S96 (2026-08-17), BL-40, while implementing the skip [[31]] prescribed. Found by replaying the *pre-change* test body against 1-, 2-, 3- and 4-receipt fixtures and diffing the row sets — not by reading either write-up, both of which were honest and both of which were low | **Diff the row SET, not the counts.** A red row is an assertion that ran and lost; a vanished row is one that never ran, and only the first is counted anywhere. Capture a test's full output at each input size and compare row-for-row. And when any write-up states how many checks a defect broke, check it against the pass delta: **if passes fell by more than failures rose, the difference is rows that disappeared** — coverage lost with no evidence left behind |
| 34 | **A ceiling guards a cost — measure whether that cost is ever paid, and in what UNIT, before trusting the number.** A file-size ceiling only bites when the file is read WHOLE. Measured over 80 transcripts, one such file was read whole once and in part 243 times, and a partial read returns whole ROWS. The cost was per-row; the guard was per-file. A budget on the wrong unit is not conservative, it is unmeasured. | S97, this file at 59,396/60,000 B | When setting or inheriting a ceiling in `.context-budget.json` — derive it from how the file is actually read, not from the seed's number. |
| 35 | **Two limits set independently on the same artifact can multiply into a constraint nobody checks — and the breach then recurs while every individual fix looks correct.** A ledger carried a 65,536 B ceiling and, separately, a test fixture that floored retention at three records. Neither is wrong. Their PRODUCT is: three records at the current mean already exceeded ceiling minus header, so *no* cut satisfied both, and the trim each session ran cleared the file for exactly one close-out. Successive sessions measured a cut, reported real room, and handed on a state that was over again within a day — every figure honest when taken, because each was computed against one limit while the other moved. The tell is that a remedy's arithmetic depends on a second limit the remedy never names. Fix it by deriving one FROM the other so they cannot drift apart silently: here the per-record budget is `(ceiling − header allowance) ÷ floor`, written beside the constant, so a successor re-runs the division instead of re-arguing the number. | S98 | Whenever the fix for a RECURRING breach is a cut, a cap, or a quota — before trusting the new figure, find the other limit the artifact is under and multiply. |
| 36 | **A uniqueness guard needs a reachable alternative, or it is a deadlock wearing a safeguard's clothes — and the tell is that its own advice names an input that cannot do the job.** A ledger trimmer derived each archive shard's name from a record DATE while its cut was POSITIONAL, so the name was not injective: when two records share a date, distinct cuts derive one name. That name was then a unique key behind a correct write-once refusal. What nobody checked was whether an operator who hit it had anywhere to go. The tool said *"Disambiguate with `--cut`"*, and `--cut` could not: a date cut key both SELECTS the records and BECOMES the name — one input, two jobs. Every admissible cut derived the taken name, so the file sat over its ceiling for two sessions with the remedy for overshoot unreachable. **When a guard refuses, ask what the caller does NEXT and try it** — an untested escape hatch may not exist. Distinct from [[35]], two independent limits multiplying; this is ONE limit with no exit. The repair is not to weaken the guard (write-once stayed, still refusing past a bound) but to make the refused name reachable another way. | S100, BL-41 | When a guard REFUSES — write-once, a unique constraint, a quota. Run the remedy it advises before shipping it, and check it covers every file the operation writes: this one wrote a shard AND a `.verify.sh` proof, and a shard-only check overwrites the proof. |
| 37 | **A losslessness proof must model the permitted TRANSFORM, not only the population and the declared growth — otherwise a correct, necessary edit reads as corruption.** An independent proof re-extracted all 31 records from the pre-trim commit and byte-compared each to its destination. Retained: 8/8 identical. Archived: **2/23** — it reported 21 records altered. Nothing was lost. The trimmer rebases root-relative link targets by `../../` so they resolve from the shard's depth (`starter-kit/methodology_trim.py:99`, `:421-443`), and refuses the rebase unless it round-trips to the identity (`:486-500`); 21 records carried such a link, 32 targets × 6 B. `L3`'s "byte-identical across the move" is identity **modulo** that proven-invertible transform — the tool was right and the proof was wrong. Sibling of *losses FAIL, growth is REPORTED*: a check that reddens on a declared, necessary edit is a false-positive generator, and invites distrusting the artifact rather than the check. **The repair:** enumerate the population, the declared growth AND the declared transform, then assert identity by inverting that transform with your own implementation, never the mover's, in both directions. | S108 (2026-08-25), verifying the `CHANGELOG.md` trim `95ac6ea` | Verifying that a move, split or archive lost nothing — especially where the mover may normalize paths, links, indentation or encoding |
| 38 | **A budget, reserve or allowance that lives only in a COMMENT is not a constraint — nothing fails when it goes wrong, so it can be silently consumed to the brink.** A header reserve stated as `8,000 B` in a derivation comment had no constant and no assertion; the thing it reserved space for had crept to **92.0%** of it, two increments from a silent breach. **The fit check among constants would never have caught it** — `floor × budget + reserve ≤ ceiling` was true throughout. Only an assertion that MEASURES THE LIVE ARTIFACT can. [[16]]'s sibling: there, operands computed from each other; here, an operand compared to nothing at all. | S109, Phase 2 of the record-budget plan | When a design doc or comment states a limit. Ask: what fails if this is wrong? If the answer is nothing, it is documentation, not a limit. Name it as a constant, then write the assertion that reads the real artifact — not just the one that checks the numbers against each other. |
| 39 | **A tool's refusal is a CLAIM TO TEST, not an obstacle to route around — and a routine remedy is owed the same question: what does it deliver to whatever actually consumes the artifact?** A ledger trim was due as close-out housekeeping, the trimmer refused it, and the session went straight to costing how deep to cut and whether to `--force` — never asking whether cutting remedied anything. It did not: measured, a whole-file read already delivered only the front matter and the 4 newest records, so the cut on offer would have archived records **already outside the delivered prefix**, in a file the protocol reads one record of. **The refusal was right for a STRONGER reason than the one it gave.** The tell is doing arithmetic about a remedy's *size* before establishing its *value*. | S112 (2026-08-26), declining a trim on evidence after the operator asked whether the value of trimming had been adjudicated | Before overriding any refusal, and before performing maintenance because a threshold says to. Ask what the remedy delivers to the actual consumer, and measure it. "The ceiling is red" is a trigger, not a justification. |
| 40 | **Narrowing a two-part guard to one part does not leave a narrower guard — it leaves the bug the conjunction was introduced to fix.** A disclosure fired only when NEITHER half could measure a file; its own comment recorded that an earlier draft had fired on one half alone and put a permanent unactionable row on every repo in the fleet. Removing the other half for unrelated reasons and "narrowing" the condition restored that draft verbatim. **24 tests caught it; the author did not**, having read the comment that predicted it. **The correct move was to delete the STATE, not keep half the test:** the removed half could abstain, its replacement always answers, so the conjunction can no longer be true and there is nothing left to detect. | S112 (2026-08-26), replacing a rate metric with a level in the dashboard | Whenever a guard's comment says "the conjunction was the fix for X". Ask whether the state it detects can still occur; if not, delete it AND assert the deletion, because restoring half of a deleted guard is the likeliest way it returns. |
| 41 | **A test whose fixture is the LIVE repository is green as a function of that repository's current size — so it can pass every run you make and fail on the run after your close-out writes.** An assertion required an advisory to quote the byte budget, true while a firing row always carried the byte reason. A change gave the second metric a LOWER threshold, so a file could fire on it alone. The test passed every targeted suite run; then the session's own ledger entries pushed a live file past the new threshold and the full suite went red. **The condition under test was never the code — it was the file's size at that moment.** | S112 (2026-08-26), re-denominating a size guard, caught by the full suite after the close-out writes | Any test that reads the working tree rather than a fixture. Run the full suite AFTER the close-out has written its ledger and receipt, not only before — and never edit a live artifact while a suite that reads it is in flight. |
| 42 | **A ratio measured on one class does not transfer to the class you have just separated it from — and the error hides best inside the document that draws the distinction.** A plan split a watched population into two classes precisely because they behave differently, then computed the second class's "x over the limit" column using a ratio measured on the FIRST class's content. Measured directly, the second class ran 2.55-2.91 bytes per token against the first's 2.27-2.52, so every multiple was **overstated by 6-20% — in the direction that flattered the argument**. Caught only by writing the weakness into a self-assessment and then measuring it rather than shipping it labelled. | S113 (2026-08-26), planning a per-class guard, in the plan that argues the classes differ | Any derived multiple applied across a population you have just partitioned. If the split is worth making, the ratio is a property of each side and must be measured on each side. |
| 43 | **Deriving a set from its own parts is usually the right design, and it disarms the assertion that those parts still cover it.** A refactor split a six-name watched population into two classes and DERIVED the union from them, deliberately, so that *"the population did not change"* would be provable rather than asserted. The coverage test written beside it compared the union of the two classes against the derived constant — which expands to comparing an expression with itself. Both mutants that NARROWED a class stayed green: dropping a name shrinks the union in lockstep, so the one assertion written to catch exactly that could not. **A derived set's coverage is only checkable against an operand that does not move with it** — a frozen literal of the membership as it stood BEFORE the derivation, written into the test, never imported from the module. The tell is syntactic: write both sides out and ask whether either is spelled from the other. | S115 (2026-08-26), Phase C1 of the read-cap correction: a 10-mutant round scored 8, both survivors tracing to one tautological line — in a test class whose other docstrings cite Learning #16 against this very trap | Whenever a constant becomes derived rather than listed. Derive it — that is the improvement — then immediately ask what the test compares it to, and freeze that operand outside the module. Sibling of Learning #16: there the operands became each other's by accident, here by a design decision that was correct |
| 44 | **A threshold that governs both when a guard FIRES and where its remedy STOPS is two constants wearing one name, and moving one of them is silently green.** A phase brief said *"replace the read arm's threshold"* — singular. The fire compared the file's size to it; the stop, in a different method, compared each candidate cut to the same constant. Moving only the fire left the trigger correct and the remedy cutting to the OLD value, three times deeper than intended — and the whole suite stayed green, because every assertion was about **what fires**, and none about **what a remedy converges to**. The resting size is the only observable that separates the two implementations: both trim, both produce a lossless shard, both pass their proofs. **Ask of any threshold: does something also stop at it? Then assert the resting value of a real run, not just the verdict of the trigger.** | S116 (2026-08-26), Phase C2 of the read-cap correction: caught before the commit only by reading `stops()` while implementing the fire, and pinned afterwards by an end-to-end test asserting the post-trim file size lies inside the intended band | Any change to a level-with-hysteresis guard, a retry ceiling, a backoff cap, or any rule of the form *fire above X, recover to Y* — especially when X and Y are spelled with the same identifier today. Sibling of Learning #43: there a derived operand disarmed an assertion, here a shared one hid a second edit site |
| 45 | **A budget made of per-file ceilings cannot see the cost that is actually paid — the aggregate.** Every file a protocol mandated reading sat under its own limit while the SET was 115% of one read, and the tool could not express the constraint: it totals one hardcoded class, and that class had a single member. The ceilings had also drifted onto the files that could afford to grow — the two flagged were read at a frontier, newest-first, so truncation cost them their OLDEST records, while the unwatched lost their NEWEST. | S117, measuring the framework's own Phase 0 read set | When any budget is per-item. Sum the set the procedure actually requires, not only its members; then check each item's ORDER, because a truncated read returns a PREFIX and an oldest-first artifact silently drops the content that matters most. |
| 46 | **A byte floor overstates any remedy that lowers bytes-per-token.** At 2.27 it credited abbreviation 4.13x its metered saving; deletion 1.23x. | S118, metered | Costing a size remedy: meter tokens, do not convert bytes. |
| 47 | **A transform can preserve every property you can check and still destroy the one the artifact exists for.** Compacting 20 rows here cleared every automated gate — byte budget, columns, row number, citation set, two live `bin/tests.sh` anchors. An adversarial read against each original then found **33 blocking losses in 16 of them**: dropped causal mechanisms (*why* a failure happens, leaving a rule nobody can transfer), deleted counter-examples, quantifiers softened from "every" to a bare plural. None is checkable — each is a claim about what a future reader can reconstruct. Any *lossy* transform (compaction, summarisation, migration, schema narrowing) has its acceptance criterion in meaning, so a gate of countable properties licenses exactly the loss it cannot see. | S119 — 20 rows compacted under the 1,500 B budget: 20/20 through the mechanical gate, 19/20 DEGRADED on an independent read | When asked to make X shorter/simpler/migrated and X's worth is its meaning: build the mechanical gate, then add a reader given original and result and asked only what was LOST — never the author, never the pass that produced it. Budget a repair round. |
| 48 | **A blocker's warrant has to name a consumer — "nobody has refuted it" is an argument from absence wearing the clothes of a finding.** Four sessions skipped a mandatory close-out step for a constraint no code reads. Its warrant was recorded as *"six refutation lenses missed it; a critic found it"* — evidence about six lenses — and promoted to *"correct, load-bearing, and unrefuted."* One grep settled it: the branch and blob it named appear in **zero** files under `bin/ tools/ .githooks/ starter-kit/ workstreams/`, there is no CI, and the mechanism it invoked (`bin/sync --source`) reads a working tree or a GitHub repo, **never a local ref**. **It was believable because a REAL rule with the same words binds a different pair of operands:** `SESSION_RUNNER.md:227` requires byte-identity between **adopter and canonical**, and that one is enforced — a `modified` tracked file aborts the adopter's whole sync, exit 2. Five sessions reasoned about "the byte-identity" without opening either copy of the sentence. Sibling of *an unattributed blocker is a defect*: this one was attributed, and absence of disagreement was its evidence. | S123 (2026-08-29), adjudicating a four-session hold on Phase 3C | When a plan, receipt or predecessor hands you a constraint. Name the file and line that would go red if you broke it, and check which operands the rule actually binds — before you honour it, and before you re-publish it. |
| 49 | **A guard that refuses when regrowth ≥ relief cannot be satisfied by any steady-state maintenance policy.** Trim at a high-water mark X back to a low-water mark Y and relief is `X−Y`; the trigger next fires exactly when the file has regrown to X, so regrowth is *also* `X−Y`. The ratio is **1.0000 by construction** and the test was `>=`. The rule therefore refused every on-schedule trim and was satisfiable only by trimming *late* — it **rewarded overshoot and punished maintenance**, and a ledger reached 4.3× its ceiling and stopped being readable at all while a correct rate rule sat in its own front matter reporting −31 entries of headroom. **The tell that it was a rule defect and not a file defect:** the tool computed the alternative boundary its governing spec actually names, printed it in every report, and never voted with it. | S132, derived then executed | Before treating a threshold's refusal as a finding about the artifact, solve the threshold for its own steady state. If the answer is "refuses always", the defect is in the rule — then ask whether the tool already computes a boundary that lacks it, and whether choosing the other one was ever ratified or merely written. |
| 50 | **A true ratio can carry a false inference for six sessions when each session takes its premise from the previous receipt.** Six consecutive receipts called a file port *"the one with no clean starting point"* and one glossed it as *"the two versions diverged rather than one being a prefix of the other"* — from a 2.47× size ratio that IS true. `git rev-parse <ref>:<path>` on both sides showed the upstream blob was the exact ancestor of the fork's seven linear commits; the end-state patch and all seven per-commit patches applied cleanly. What had diverged was the surrounding TREE, and the sentence conflated the two. **The tell: the claim named no command.** A size is a measurement; "diverged" is a relationship with its own instruments (`rev-parse`, `merge-base`, `log --graph -- <path>`), and nobody ran them because the number beside the word looked like evidence for it. | S145 (2026-09-02), planning PR 4 of the read-set series | When a receipt or plan characterises two versions of an artifact ("diverged", "no clean start", "rewritten"): re-derive the relationship with the instrument that measures relationships, state file-level and tree-level ancestry as two separate facts, and quote the command beside each. |
